# R5 — consumatorii lui `ofertare_cantitati`: cantitățile nevalidate folosite ca aprobate

Cerința (Copilot, R5): „Verifică punctual consumatorii: calcul financiar, F3/centralizator, grafic, generator PT și poartă finală. Existența rândului cu status='extras' nu demonstrează singură că a intrat în oferta aprobată. Dacă este consumat ca valoare aprobată, oprește acea utilizare și marchează rezultatele derivate pentru reverificare."

- Ramura: `claude/cantitati-nevalidate-consumatori`, **reașezată (rebase, 26.09) peste `claude/r4-rezervare-zone` @ 8db5587** (reparațiile R4 + deduplicarea pe identitatea rândului), apoi **a doua oară peste … @ ab5c449** (R4 runda 6; §2.3), apoi **a treia oară peste … @ 7a7bf86** (R4 rundele 7–9; §2.4), apoi **a patra oară peste … @ 3039f2a** (capul final R4, rundele 10–12; §2.5). Nepushată, fără PR. Nimic deployat, nimic aplicat în BD.
  - `e5273f5` (fost 0686331 / 233753a / 990a6b1) = commit-ul R5 inițial, cu conflictul din `treciInCantitati` rezolvat păstrând ambele semantici (§2.2, §2.4);
  - `5a47658` (fost 67e423c / 48f52d6; conflictele cu R4 runda 6 și rundele 7–9 rezolvate aici, §2.3, §2.4), `31ed808`, `5303ee4` + commit-ul de documentație `f2e3082` = **runda 4** (problemele verificatorului rundei 3, §2.1);
  - `f78afa8` = testele interacțiunii R4 × R5 după rebase-ul 2 (§2.3, §5.1); `828aa4e` = adaptarea testului R4 „runda 9 TOTAL-a” după rebase-ul 3 (§2.4);
  - `c68c8e7` = **condiția 1 a lui Copilot** (invalidarea pe orice atribut relevant + istoric, §8), `5ab80c1` = **condiția 2** (niciun rând nu dispare tacit din consumatori, §9), plus commit-ul acestui document (`f6fdd51`);
  - **runda 5** (verificatorul celor 2 condiții, 26.09): 2 MAJORE + 6 minore — „invalidat” nu mai depinde de textul notei, pragul se măsoară față de valoarea APROBATĂ, `um` exact, TRUNCATE închis, poarta propunerii reverifică graficul înghețat, plasa transferului testată, NBSP aliniat (§10);
  - **runda finală (6)**, după rebase-ul 4 (`d167e94` = adaptarea testelor R4 rundele 10–12) — `10e107d`: **decizii luate în audit pe principiile Copilot, reversibile** (§11): marcajul pasului B (documentat + vechi, `cantitate` = valoarea verificată), varianta B pe ramurile „doar de verificat” / MY-T4 / „total cu Dn”, unitatea normalizată în regulă și în filtrele de rețea + semnalul „unitate schimbată”, rândurile TOTAL invalidate tratate la fel în JS și view, istoricul citit descrescător și paginat.
- BD: doar SELECT (25.09.2026 după transferul planșei 470 din 16:51:08 UTC; recontrolat 26.09.2026). Migrările și SQL-ul pentru lic. 95 și lic. 3 sunt **propuse, neexecutate**.
- Regula folosită peste tot: **aprobat = `status='validat'`** (bifa ✓ din 📋 Cantități). `extras`, `diferenta` și `revizuit_clarificare` (0 rânduri în BD, nescris de niciun cod) sunt date de lucru. Regula stă într-un singur loc: `src/ofertareCantitatiAprobare.js`.
- **Runda 4: regula acoperă și `cantitate_plansa`.** Un scriitor automat (transferul din planșă, citirea CAD) care pune pe un rând o cifră diferită cu ≥ 1 m de cea pe care rândul o avea scoate rândul din `validat` (`diferenta`): validarea se reface pe cifra nouă.
- **Runda 1b (Copilot, închiderea R4/R5, 26.09 — condiția nr. 1, „pragul”): fără prag.** Orice cifră cu altă valoare (canonic: round(x, 6); 100 = 100,000, dar 100 ≠ 100,8), pe orice unitate, scoate rândul din `validat`; pragurile (1 m pe lungimi, 1 % relativ) dau doar SEVERITATEA din notă. Migrarea `docs/R5_MIGRARE_1b_prag_exact.sql` (+ `_ROLLBACK.sql`), **neaplicată**; codul (toate căile) pe ramură — §12.
- **Sarcina 2 (Copilot, închiderea R4/R5, 26.09 — condiția nr. 2 + observațiile): „nimic scris ≠ nicio problemă”.** Conflictele transferului planșă → cantități care NU produc niciun rând sunt persistate pe document (`analiza.transfer_cantitati`, fără schemă nouă) și văzute de poartă chiar cu zero rânduri nevalidate; se închid doar prin recitire fără conflicte sau confirmare umană explicită (funcție SQL, notă, `auth.uid()`). Citirea eșuată a view-ului / istoricului = „nu putem verifica”; mesajele de lipsă grupate pe unitate; v6 nu mai rescrie / retrage o ciornă aprobată de om. Migrarea 2 extinsă (neaplicată), coexistă cu codul publicat — **§13 (Condițiile de închidere Copilot, punct cu punct)**.
- **Reparația rundei 1 (verificatorii + ADDENDUM 2 Copilot, 26.09):** jurnalele codului vechi = `legacy_partial` DESCHIS (+ reevaluare fără AI); v6 recunoaște ciorna mașinii după amprenta textului generat și notifică o dată pe eveniment; confirmarea = rezolvare / excepție justificată, cu drept de decizie, legată de rularea exactă; recitirea parțială nu închide (ancore + acoperire); TOTAL multiplu fără valoare nouă; restanțe distincte, fără clarificări către AC din erori interne; aprobarea finală blocată în UI și în trigger-ul pachetului; cheile serverului nu mai pot fi scrise direct din client — **§14**.
- **Verdictul Copilot 26.09 („aprobat = validat” e direcția corectă, cu 2 condiții) — tratat aici:** (1) invalidarea nu privește doar cifra — unitatea, Dn, materialul, SDR-ul, tronsonul / etapa și sursa aplicabilă contează și ele; valoarea și aprobarea veche rămân în istoric (§8: regula comună JS + trigger propus în BD cu tabel de istoric, neaplicat); (2) rândul invalidat / nevalidat nu dispare tacit prin filtrul `status='validat'`: fiecare consumator numără și listează ce lipsește, își marchează rezultatul „INCOMPLET, de reverificat” și, unde e poartă, blochează (§9).

## 0. Pe scurt

1. **Lic. 95 nu are încă niciun rezultat derivat din rândurile 1751–1756.** Au fost verificate 10 surse, toate cu 0 rânduri (§3): grafic (parametri, versiuni, activități), poarta propunerii, pachetul, capitolele PT, verificarea finală, RFQ, clarificările născute din cantități și apelurile AI de după transfer. Și `v_ofertare_pt_stare` are `lista_f3_m`, `memoriu_m`, `plansa_m` și `grafic_fronturi_m` pe NULL, pentru că toate cele 6 rânduri au `tip_sursa` NULL. Singurul derivat e un text: `handoff_activ` spune „NU aplicat în ofertă” și „+3.840 = Dn200 din afara UAT” (§3.2).
2. **Riscul era latent, dar real, în 4 consumatori din cod, plus un scriitor care se aproba singur.** Pe starea de azi:
   - Poarta graficului dădea „ok” pe cele 6 rânduri `extras`.
   - „Propune din cantități” punea fronturi Dn200 17.785 m, adică inclusiv cei 13.765 m din afara UAT.
   - Generatorul de clarificări ar fi trimis modelului `lista: 17785`, deci o F3 care nu există.
   - O recitire a planșei ar fi scris „Planșa 1 confirmă” pe 1752–1755.
   - Citirea CAD scria direct `status='validat'`.
   Toate cinci sunt reparate în cod, cu teste. Pe cele verificabile pe codul vechi (clarificări, transferul din planșă) testele noi pică, adică prind defectul.
3. **Nu există consumatori pentru calculul financiar, devize, prețuri sau F3/centralizator.** Platforma nu calculează bani și nu generează F3 din `ofertare_cantitati` (grep, §1 rândurile 14–15). Verificarea finală și generatorul PT nu citesc tabela. Generatorul PT o atinge doar indirect, prin grafic, care e acoperit de fix-ul 1.
4. **Două schimbări SQL sunt propuse în `docs/`, neaplicate:**
   - un view nou, `v_ofertare_cantitati_nevalidate`, pentru poarta propunerii (H2);
   - `ofertare_clarificare_planse_auto` v6, care nu mai citează autorității un total F3 luat din rânduri nevalidate; din runda 4, cu **același set F3 ca H2** (filtrul `qm`) și cu numărul scris ro-RO fără ambiguitate.
   Ordinea e: **întâi migrarea, apoi merge-ul**. Invers, H2 blochează cu „nu putem verifica” pe licitațiile care au F3 (fail-closed, ca la P0c). Azi doar lic. 5 are F3.
5. **Runda 4 (verificatorul rundei 3): „validat” nu mai poate rămâne peste o cifră pe care n-a văzut-o nimeni.** Cazul real e lic. 3: rândurile 2, 3 și 4 au fost validate **înainte** de transferul planșei 1.1 din 15.09, care le-a scris apoi `cantitate_plansa` (Dn180: 1.100 validat, 2.210 din planșă). Codul nou o împiedică pe viitor (§2.1); pentru cele 3 rânduri existente e propus un SQL separat (`docs/R5_LIC3_VALIDARI_INAINTE_DE_TRANSFER_PROPUS.sql`, preview = 3 rânduri). Tot în runda 4: poarta graficului ia totalul declarat ca referință doar dacă e validat, se recalculează întreagă la îngheț și blochează fronturile salvate care nu vin din rânduri validate (lic. 3: 6 fronturi / 29.985 m din 04.09).

## 1. Inventarul consumatorilor

Surse: grep pe `src/`, `api/`, `supabase/functions/`, `worker/`, `supabase/migrations/`, plus catalogul BD (`pg_views`, `pg_matviews`, `pg_proc.prosrc`, `pg_trigger` care conțin `ofertare_cantitati`). Rezultatul din BD: 2 view-uri (`v_ofertare_pt_stare`, `v_ofertare_contradictii`), 2 funcții (`ofertare_clarificare_planse_auto`, `ofertare_transfer_plansa_cantitati`) și 1 trigger (`trg_categorie_cantitate`). Niciun view și nicio funcție nu depind de cele două view-uri. Liniile „HEAD” se referă la 8a6fbbb, iar „acum” la ramură.

| # | Consumator | Fișier:linie | Citește | Filtru status | Tratează `extras` ca aprobat? | Derivate lic. 95 (SELECT 25.09) | Acțiune |
|---|---|---|---|---|---|---|---|
| 1 | **Graficul**: poarta graficului + generatorul (`PoartaGrafic`) | `src/GraficPoarta.jsx` HEAD l.239 (load), l.45–53 `randuriFront`, l.261–276 „Propune din cantități”, l.290–294 rândul „cant”, l.316–330 înghețul; acum (runda 4) l.216 (load), l.236 „Propune”, l.260 poarta (`graficPoartaCalcul.js`), l.263–285 generarea | `cantitate` sau `cantitate_plansa` (după „Baza cantităților”), `status`, `um`, `categorie`, `obiect` | niciunul; doar `diferenta` dădea warn/block | **DA.** „cant” = ok pe rânduri `extras`, iar fronturile se propuneau din orice rând. Lanțul: fronturi → `grafic_parametri` → `motorPEHD` → `grafic_versiuni` (snapshot + poartă) → `grafic_activitati` → capitolele PT. Pe lic. 95: „6 rânduri rețea” ok, 6 fronturi Σ 48.195 m | 0 `grafic_parametri`, 0 `grafic_versiuni`, 0 `grafic_activitati` | **FIX**: „cant” = BLOCK cât timp un rând de rețea e nevalidat; fronturile se propun doar din rânduri validate; cantitățile se recitesc din BD înainte de îngheț. **Runda 4**: „front” ia totalul declarat ca referință doar dacă e validat; poarta se recalculează ÎNTREAGĂ pe cantitățile recitite; fronturile poartă rândul-sursă și sunt BLOCK dacă acesta nu mai e validat cu aceeași cifră (sau dacă sunt salvate înainte de R5) |
| 2 | **Poarta propunerii / poarta finală**: rândul H2 „Cantitățile rețelei” (card în fișă, panou, semnătura `ofertare_pt_poarta`) | view `v_ofertare_pt_stare`, CTE `qm`; `src/ofertareControale.js` HEAD l.30–52 (`controlCantitati`), acum l.37–70; `src/ofertarePoarta.js` l.150; `src/OfertarePropunere.jsx` l.1202 / l.1841; `src/OfertareLicitatii.jsx` l.3353 | Σ `cantitate` pe `tip_sursa` ∈ {lista_f3, lista_c6, memoriu}, Σ `coalesce(cantitate_plansa, cantitate)` pe `plansa`; rețea = `um='m'`, categorie conductă/rețea, fără „total” | **niciunul** | **DA.** F3 e referința „pe ea punem banii”. O F3 transcrisă de AI și nevalidată trecea „ok” dacă era egală cu fronturile | 0 `ofertare_pt_poarta`. `tip_sursa` NULL pe toate 6, deci H2 = block „lipsește F3”, fără cifre. Latent: pasul A din R5 v2 (`tip_sursa='plansa'`) → „există doar planșe 13.765 m” | **FIX JS**: F3 cu rânduri nevalidate = BLOCK; câmp absent = „nu putem verifica” = BLOCK; memoriul, planșele și C6 nevalidate poartă eticheta „(nevalidat)”. **View nou propus** (§4). Notă (runda 4): cardul din fișă (`PropunereRezumat`, `OfertareLicitatii.jsx` l.3353) citește doar `v_ofertare_pt_stare`, fără view-urile suplimentare (nici `v_ofertare_pt_cerinte_neconfirmate`, nici cel nou) — deci H2 acolo e „nu putem verifica” = block; cardul era oricum block din lipsa câmpurilor de documentație (divergență preexistentă card ↔ panou, nereparată aici) |
| 3 | **Clarificarea automată „planșe”** (RPC, apelat din `plansa-citeste` l.599/903 și `api/plansa-felii.js` l.131) | funcția `ofertare_clarificare_planse_auto`, `SELECT round(sum(cantitate),1) INTO v_f3` | Σ `cantitate` pe F3 de conductă (`tip_sursa='lista_f3'`, um m/ml, denumire conductă/țeavă/tub) | **niciunul** | **DA**: cifra intră în textul către AC („lungimea totală de conductă din F3: X m”) | #63 (`auto_planse_1`, `de_trimis`) e text editat de om (v5 nu-l mai rescrie) și nu citează F3; lic. 95 n-are F3. Nicio ciornă `auto_planse` nu citează F3 azi | **v6 propusă** (§4): totalul vine doar din rânduri validate; fără total cât există F3 nevalidată. Runda 4: setul F3 = filtrul `qm` (ca H2), format ro-RO |
| 4 | **Generatorul de clarificări** (edge + worker NAS, același `core.ts`) | `supabase/functions/ofertare-clarificari-propune/core.ts` HEAD l.69 (select), l.95 (payload); acum l.91, l.118, funcția `randCantitatePentruAI` l.60 | rândurile cu `diferenta_nota` (max 40): `cantitate`, `cantitate_plansa`, `tip_sursa`; **fără** `status` | niciunul | **PARȚIAL.** Nu le aprobă, dar pune `cantitate` sub cheia `"lista"`: 1751 ar fi plecat ca `lista: 17785, plansa: 17785`, adică o F3 inexistentă „confirmată” de planșă | 0 apeluri AI pe lic. 95 după 16:51:08, 0 clarificări `platforma` | **FIX**: `status` în select; `sursa_cantitate` și `status_validat` (runda 4: redenumit din `validat_de_om` — „validat” n-are autor) în payload; regula explicată în prompt („marcat validat în platformă”). Cere deploy edge; workerul o preia la pull |
| 5 | **Transferul planșă → cantități** (la recitire) | `supabase/functions/ofertare-plansa-citeste/handler.ts` HEAD l.268–270, l.301–310, l.317; acum (după rebase + runda 4) l.717–742 (reguli), l.789 (select), l.804–904 (potrivire, patch), l.930–952 („doar de verificat”), l.960–975 (TOTAL) | rândurile existente: `cantitate`, `status`; acum și `sursa`, `tip_sursa` | păstra doar rândurile `validat` | **DA, la recitire.** `cantitate` unui rând scris de un transfer anterior devenea „dinMemoriu”. Rezultatul: „Planșa 1 confirmă: 2.275 m” (planșa confirmată de ea însăși) și „Memoriu 13.140 m vs planșa 13.740 m” | 1 transfer (doc 470, `transfer.stare='facut'`, 16:51:08), nicio recitire, deci 0 note false | **FIX**: rândurile din planșă nu mai sunt „memoriu”. Nota spune „recitire … nu confirmare”; o recitire diferită → `diferenta`. **Runda 4**: cifră nouă ≥ 1 m pe un rând `validat` (și pe TOTAL) → `diferenta`; `tip_sursa` bate sursa; „recitire” doar pe aceeași planșă, altfel ambiguu; două grupuri (Dn, material) pe aceeași poziție → ambiguu |
| 6 | **Citirea CAD** (scriitor care se auto-aproba) | `api/cad-parse.js` HEAD l.118–128 (`status: 'validat'` la l.123); acum l.121–125 + `api/_cadCantitate.js` | — | — | **DA, auto-aprobare**: o măsurătoare automată intra `validat` și ar fi trecut prin toate porțile de mai sus | 0 pe lic. 95. Lic. 3, rândul 9 (35.620,59 m): `validat`, cu `created_at = updated_at` = 28.08 20:19:55 | **FIX**: rând nou → `extras`. **Runda 4**: măsurătoare nouă ≥ 1 m față de `cantitate_plansa` (sau `cantitate`) → `diferenta`, și pe rândul validat (înainte primea tăcut cifra nouă în `cantitate_plansa`). Rândul 9 = decizie (§6) |
| 7 | 📋 Cantități (editorul) | `src/OfertareCantitati.jsx` l.50 / 63 / 72 / 76 / 82 | tot | — (arată statusul) | NU (afișează, nu calculează) | — | Contor „N nevalidate” + tooltip pe ✓ |
| 8 | `v_ofertare_contradictii` | view (pg_get_viewdef) | toate rândurile | niciunul | NU. E un detector de diferențe; valorile se compară, nu se aprobă. Niciun cod nu-l citește (grep) | 0 rânduri pentru 95 | nimic |
| 9 | Extragerea cantităților (scriitor) | `supabase/functions/ofertare-cantitati-extrage/handler.ts` l.266 (citește `obiect`), l.375 (insert `extras`) | `obiect` | — | NU | — | nimic |
| 10 | RPC `ofertare_transfer_plansa_cantitati` (scriitor) | `supabase/migrations/20260927_…atomic.sql` l.34–44 | — | aplică `status` din patch **fără gardă pe statusul curent** | NU, dar are o fereastră de cursă cu o validare umană făcută între citirea handler-ului și RPC | — | **Runda 4, închis în cod, fără schimbarea RPC-ului**: patch-ul poartă `status='diferenta'` ori de câte ori cifra din planșă se schimbă, deci o validare dată în fereastră pe cifra veche e scoasă (§2.1). RPC-ul NU trebuie să primească garda propusă în runda 3 (§7) |
| 11 | Trigger `trg_categorie_cantitate` | `fn_trg_categorie_cantitate` | `denumire` | — | NU | — | nimic |
| 12 | **Verificarea finală** (`ofertare-verificare-finala`) | `index.ts` l.68–85: citește `ofertare_licitatii`, `ofertare_cerinte`, `v_dovezi_stare`, `ofertare_formulare_registru` | **nu citește** `ofertare_cantitati` | — | NU | 0 `ofertare_verificari` | nimic |
| 13 | **Generatorul PT** (`ofertare-genereaza-capitol`) | `index.ts` l.90–100: citește `grafic_activitati`, acoperirea, garanția, participanții, … | **nu citește** tabela; indirect prin `grafic_activitati` (nume de front cu metri) | — | indirect, prin #1 | 0 capitole, 0 activități | acoperit de FIX #1 |
| 14 | **Calcul financiar / devize / prețuri** | `Financiar.jsx`, `devizParser.js`, `CatalogDevizPanel.jsx`, `OfertareRFQ.jsx` | **niciunul** nu citește `ofertare_cantitati` (grep). Materialele RFQ se introduc manual (`ofertare_rfq_materiale`) | — | NU | 0 `ofertare_rfq` | nimic |
| 15 | **F3 / centralizator** | `exportCentralizatorXlsx.js` (Transgaz, execuție), `Izometrie.jsx`, `OfertareExport.js` (propunere / borderou / F23 / F9) | **niciunul** nu generează F3 din `ofertare_cantitati` | — | NU | — | nimic |
| 16 | Pachetul (depunerea) | `src/ofertarePachet.js`, trigger `fn_ofertare_pt_pachet_poarta_documentatie` | nu citește cantități; depinde de semnătura porții | — | indirect, prin #2 | 0 `ofertare_pt_pachet` | acoperit de FIX #2 |
| 17 | `controlTronsoane`, `controlGraficSursa`, `GraficLucrare.jsx` | `src/ofertareControale.js` l.433+ | nu citesc `ofertare_cantitati`; `controlTronsoane` nu e apelat nicăieri | — | NU | — | nimic |
| 18 | Worker NAS (ingest, SEAP) | `worker/ofertare/ingest.ts` l.150, `seap.ts` l.40 | doar tipul de document `lista_cantitati` | — | NU | — | nimic |

## 2. Fix-urile (cod, pe ramură)

| Fișier | Ce face acum | Test |
|---|---|---|
| `src/ofertareCantitatiAprobare.js` (nou) | Conține regula (`esteAprobata`), `randuriFront` mutat neschimbat, `controlCantitatiGrafic`, `fronturiDinCantitati` și `campuriCantitatiNevalidate`. Funcțiile sunt pure. | `src/ofertareCantitatiAprobare.test.js`: 15 teste, pe rândurile reale 1751–1756 |
| `src/GraficPoarta.jsx` | Rândul „cant” e BLOCK cât timp un rând de rețea e nevalidat (lic. 95: „6 din 6 rânduri de rețea NEVALIDATE (48.195 m…)”). „Propune din cantități” ia doar rândurile validate: pe lic. 95 azi propune 0 fronturi și are 6 excluse; după validarea lui 1752–1756 propune 5 fronturi / 30.410 m, iar Dn200 rămâne pe dinafară. Generarea recitește cantitățile din BD și refuză dacă între timp s-a redeschis un rând. În `grafic_versiuni` se îngheață cantitățile proaspete. **Schimbare de comportament**: „diferență memoriu/planșă + baza aleasă” nu mai trece ca warn; rândul trebuie validat. | idem |
| `src/ofertareControale.js` (H2) | F3 cu rânduri de rețea nevalidate → BLOCK („F3 … include N rânduri de rețea NEVALIDATE (X m) — transcrise automat…”). Câmpul lipsă sau invalid → BLOCK „nu putem verifica” (control indisponibil ≠ zero). Memoriul, planșele și C6 nevalidate → „(nevalidat)” în text. Cu F3 validată, regula veche rămâne neschimbată. | `ofertareControale.test.js`: +8 teste; cazurile vechi primesc explicit `lista_f3_nevalidate: 0`. `ofertarePoarta.test.js`: +2 teste; fixture-ul VERDE are câmpurile noi |
| `src/OfertarePropunere.jsx` | Lipește `v_ofertare_cantitati_nevalidate` peste `v_ofertare_pt_stare` la încărcare (l.1238) și la recitirea din `semneaza` (l.1845), ca neconfirmatele | build + testele porții |
| `src/OfertareCantitati.jsx` | Contor „N nevalidate” (galben) cu explicație; tooltip pe ✓ | build |
| `src/graficPoartaCalcul.js` (nou, runda 4) + `src/GraficPoarta.jsx` | Checklist-ul porții = funcție pură `calculeazaPoartaGrafic`, mutată din componentă; la generare se recalculează ÎNTREAGĂ pe cantitățile recitite și asta se îngheață. Rândul „front” vine din `controlFronturiGrafic` (§2.1). ＋ creează front `manual`; butonul „✋ Le asum ca manuale” (cu confirmare) marchează explicit fronturile vechi fără legătură | `graficPoartaCalcul.test.js` 4 teste; `ofertareCantitatiAprobare.test.js` +10 (25) |
| `supabase/functions/ofertare-clarificari-propune/core.ts` | `status` în select; `randCantitatePentruAI`: `cantitate` + `sursa_cantitate` (tip_sursa sau „planșă (citire automată…)”) + `status_validat` (runda 4; era `validat_de_om`); cheia `lista` dispare; regula e în prompt: `status_validat: true` = „marcat validat în platformă”, fără autor/dată, nu cifră de citat autorității. Runda 4: adnotări de tip pentru cele 4 erori TS preexistente (fără schimbare de comportament) — `deno check` / `deno test` trec acum fără `--no-check` | `core_test.ts`: 4 teste (unul capăt-la-capăt cu fetch simulat și `dry_run`). Contra-probă pe core.ts de la HEAD: testul capăt-la-capăt **pică** |
| `supabase/functions/ofertare-plansa-citeste/handler.ts` | `randDinPlansa` (runda 4: `tip_sursa` declarat decide; doar fără tip contează sursa „… citit automat din scanare”). Un astfel de rând nu mai e „dinMemoriu”. Recitirea identică dă nota „valoare din planșă, nu confirmare din memoriu”. Recitirea diferită dă „rândul are X m din citirea anterioară … verifică” și status `diferenta`. **Runda 4**: `cifraSchimbata` — și rândul `validat` (și TOTAL) trece pe `diferenta` când cifra din planșă se schimbă cu ≥ 1 m; „recitire” doar pe aceeași planșă (`dinAceeasiPlansa`), altfel ambiguu și neatins; ordine deterministă a grupurilor. Coliziunea a două grupuri (Dn, material) pe aceeași poziție urmează, după rebase-ul 2, regula R4 runda 6 (§2.3): o notă pe poziție, `cantitate_plansa` neatinsă. `treciInCantitati` e exportat pentru test. | `cantitati_nevalidate_test.ts`: 16 teste (5 + 11 în runda 4) + 3 după rebase-ul 2 (§2.3). Contra-probă pe HEAD 8a6fbbb (runda 3): **2/5 pică comportamental**, al treilea doar pentru că exportul lipsea (corectat; „3/5” din runda 3 era greșit). Contra-probă runda 4 pe 233753a: 8/8 teste noi de comportament pică. Suita planșei: **192/192** (26.09, runda 6, după rebase-ul 4 peste `3039f2a`; istoric: 168/168 la `6313184`, 164/164 la `5ab80c1`, 116/116 în runda 4, 128/128 după rebase-ul 2). **Condiția 1 / decizia varianta B / R5 pas B**: §8 |
| `api/_cadCantitate.js` (nou) + `api/cad-parse.js` | Rândul CAD nou intră `extras`. **Runda 4**: măsurătoarea nouă diferită cu ≥ 1 m de `cantitate_plansa` (sau de `cantitate`) → `diferenta`, și pe rândul validat (a cărui `cantitate` a omului rămâne neatinsă); aceeași cifră (< 1 m) nu atinge statusul. `cad-parse.js` citește și `cantitate_plansa`. | `api/_cadCantitate.test.js`: 6 teste (4 noi în runda 4 pe rândul 9 real al lic. 3), fără nicio ramură care să scrie `validat`. Contra-probă pe 233753a: 4/4 noi pică (fișierul cu „_” nu devine funcție Vercel) |

### 2.1 Runda 4 — problemele verificatorului rundei 3

**MAJOR: `cantitate_plansa` rescrisă după validare.** Regula „aprobat = `validat`” acoperea rândul, dar nu și coloana pe care graficul o folosește cu baza „planșe”. `treciInCantitati` (patch fără status pe rândul `validat`) și `api/_cadCantitate.js` (ramura „validat”) scriau o cifră nouă, automată, peste o validare dată pe altă cifră. Am aplicat **varianta (a)**, în ambii scriitori, cu aceeași regulă (`cifraSchimbata`, `handler.ts` l.734 și `_cadCantitate.js` l.21):

- **referința** = cifra pe care rândul o avea deja: `cantitate_plansa`; dacă n-are, `cantitate`; dacă n-are niciuna, orice cifră nouă contează;
- cifra nouă diferă cu **≥ 1 m** de referință ⇒ `status='diferenta'`, iar nota începe cu „Rândul era VALIDAT cu … ; planșa X dă acum Y m — validarea se reface.”; `cantitate` (cifra omului) nu se atinge;
- aceeași cifră recitită (< 1 m) nu atinge statusul: o validare dată **după** transferul anterior rămâne. De aceea referința e `cantitate_plansa` existentă și **nu** și `cantitate`: altfel un rând validat cu memoriu ≠ planșă (omul a văzut ambele cifre și a ales baza) ar fi redeschis la fiecare recitire a aceleiași planșe;
- regula se aplică și rândului TOTAL (e referința „front” când e validat);
- **cursa citire → RPC**: statusul pleacă în patch **oricare** ar fi statusul citit. RPC-ul `ofertare_transfer_plansa_cantitati` (neschimbat) aplică statusul din patch fără gardă, deci o validare dată între citirea handler-ului și RPC, pe cifra veche, e scoasă. La CAD (UPDATE direct, tot fără gardă) la fel. Ce rămâne în fereastră e doar cazul „aceeași cifră”, inofensiv.

Cazul real, lic. 3 (SELECT 26.09.2026; în toată BD există doar 4 rânduri `validat`, toate la lic. 3): rândurile 2 (Dn180: validat 1.100, planșa 1.1 = 2.210, +1.110 m), 3 (Dn160: 5.250 / 5.245, −5 m) și 4 (TOTAL: 37.320 / 41.920, +4.600 m) au `updated_at` 15.09 15:19:53.008 / .032 / .053 — **batch-ul transferului planșei 1.1** (rândul 1, `diferenta`, e în același batch la 15:19:52.967, toate cu nota „Memoriu X vs planșa 1.1 Y”). Cum ✓ (`valideazaC`) scrie și el `updated_at`, ultima scriere a fost transferul: **validarea s-a dat înainte, pe cifra din memoriu**. Pe codul nou, transferul acela ar fi pus 2, 3 și 4 pe `diferenta` (test „runda 4 (MAJOR) lic. 3 real”). Pe datele de azi, o recitire cu aceleași cifre NU le redeschide (referința e 2.210 etc. — testul-pereche o arată), deci pentru ele e propus separat `docs/R5_LIC3_VALIDARI_INAINTE_DE_TRANSFER_PROPUS.sql`: preview (rulat ca SELECT pe 26.09 → exact 2, 3, 4), aplicare cu gardă pe `updated_at` (un rând revalidat de om după 15.09 iese din filtru), rollback exact (status, notă, `updated_at`); PGlite 7/7. Rândul 9 (CAD) rămâne decizia separată din §6.

**Minore:**
- `randDinPlansa` = `r?.tip_sursa ? r.tip_sursa === 'plansa' : /citit automat din scanare/i.test(r?.sursa)` (ADV4: un rând reclasificat F3 nu mai e „citire anterioară”; nota spune „F3 2.300 m vs planșa 1 2.275 m”, nu „Memoriu …”).
- „Recitire” doar când sursa rândului începe cu eticheta **aceleiași** planșe (`dinAceeasiPlansa`: „Planșa 1 — …” sau „Planșa „PL1.pdf” — …”; „Planșa 1” ≠ „Planșa 12”). Rândul altei planșe (ADV3) nu se suprascrie și nici nu se golește pe ramura „doar de verificat”: apare în `ambigue` cu motivul. **Limită**: sursa nu poartă id-ul documentului, deci două documente diferite cu același nr. de planșă nu se pot deosebi. **Vizibilitate**: `ambigue` apărea doar în răspunsul transferului / `analiza.citire_ai.sumar.cantitati` — **din 26.09 și în UI** (insigna „⚠ transfer” pe document, §9).
- Două grupuri (Dn, material) care ajung la aceeași poziție (Dn63 PE + Dn63 OL, o singură poziție „Dn63”): defect preexistent (ADV5, reprodus și pe 8a6fbbb). În runda 4 R5 le trata ca ambigue, **fără niciun update**; **după rebase-ul 2 rămâne regula R4 runda 6** (§2.3): o singură notă pe poziție, cu toate grupurile, `cantitate_plansa` neatinsă. Din R5 rămâne ordinea deterministă (Dn desc, apoi material); testul cu ordinea inversată e păstrat.
- **GraficPoarta** (`controlFronturiGrafic`, `ofertareCantitatiAprobare.js` l.100; `calculeazaPoartaGrafic`, `graficPoartaCalcul.js`):
  - referința „front” = totalul declarat **doar dacă e validat**; altfel suma rândurilor de rețea validate (ADV6: un total `extras` de 50.000 m nu mai e referință);
  - la generare, **toată poarta** se recalculează pe cantitățile recitite și asta se îngheață (înainte: doar „cant” era actualizat, „front” rămânea pe starea veche);
  - „Propune din cantități” salvează pe fiecare front `cantitate_id`, `baza` și `lungime_sursa` (`grafic_parametri.parametri` e jsonb — **schema permite, fără migrare**; `motorPEHD` și `v_ofertare_pt_stare.gp` citesc doar `lungime_m`). Poarta e **BLOCK** pe fronturile fără legătură (salvate înainte de 26.09), cu rândul-sursă nevalidat / șters, propuse pe altă bază sau cu cifra rândului schimbată de la propunere — până la re-propunere. Un front adăugat cu ＋ e `manual` (decizia omului, numărat în detalii). Lic. 3 (singura cu `grafic_parametri`, SELECT 26.09): 6 fronturi / 29.985 m din 04.09, baza „planșe”, fără legătură ⇒ acum block. Fronturile par scrise de om (localități, cifre = memoriul: 6.200 + 8.015 + 6.255 + 3.165 = 23.635 ≈ 23.630 Dn250; 1.100; 5.250), de aceea există butonul „✋ Le asum ca manuale” (confirmare explicită) — alternativ, re-propunere din cantități după validare.
- `core.ts`: `status_validat` în loc de `validat_de_om`; promptul: „marcat validat în platformă”, marcajul n-are autor/dată.
- Documentație: antetul SQL (PGlite 18.3, nu Postgres 16); preview-ul `ai_usage_log` extins la documentele licitației (§3.1); contra-proba „3/5” corectată (§2); instabilitatea `concurenta_test` notată (§5); cardul din fișă (§1, rândul 2).

### 2.2 Rebase-ul peste `claude/r4-rezervare-zone`

Conflictul a fost doar în `treciInCantitati` (`handler.ts`), modificat de ambele ramuri. Rezolvare, păstrând ambele semantici:
- semnătura R4 (`rest?: RestTransfer` — rândurile „de verificat” pe (Dn, material)) + exportul R5;
- select-ul citește `cantitate_plansa` (R4, pentru nota „cifra veche”) **și** `sursa`, `tip_sursa` (R5);
- `diferenta` pe `extras` când recitirea diferă de cifra din rând (R5) **sau** când pe (Dn, material) există rânduri de verificat (R4).
Testul R4 „identitate 470: retransfer peste 1751–1756” aștepta „Planșa 1 confirmă” pe 1751–1755 — exact defectul R5 (planșa confirmată de ea însăși); acum așteaptă nota „(recitire) … nu confirmare din memoriu”. Cifrele și statusurile sunt aceleași (doar 1756 → `diferenta`, 13.740). După rebase, înainte de runda 4: vitest 401/401, deno `supabase/functions/` 126/126 (cu `--no-check` din cauza celor 4 erori TS preexistente din `core.ts`), `test-cas-felii` 34/34, build OK.

### 2.3 Rebase-ul 2 peste `claude/r4-rezervare-zone` @ ab5c449 (R4 runda 6)

Conflictul a fost tot în `treciInCantitati` (`handler.ts`), la commit-ul rundei 4 (fost 48f52d6). R4 runda 6 (f1c4b66) repară **același defect** ca ADV5 din R5 (două grupuri sigure pe o singură poziție ⇒ două update-uri pe același id, ultimul câștigă), dar cu altă semantică pe poziție. Rezolvare:
- **coliziunea = regula R4**: o singură scriere pe id, doar `diferenta_nota` (toate grupurile cu metrii și rândurile lor, suma, „nu se adună și nu se suprascriu automat”, cifra veche numită veche) + `extras` ⇒ `diferenta`; `cantitate_plansa` **neatinsă**; o intrare în `ambigue` cu `grupuri`. Varianta R5 („ambele ambigue, nimic pe poziție”) lăsa urma doar în JSON-ul transferului, pe care UI-ul nu-l arată (§7), deci poziția își păstra tăcut nota și statusul citirii anterioare;
- **din R5 rămân**: ordinea deterministă a grupurilor (Dn desc, apoi material) — acum și lista de operații trimisă RPC-ului e identică în orice ordine a tronsoanelor, nu doar starea finală; `candidatiDn` (același filtru pe material în ambele ramuri); `randDinPlansa` / „recitire” doar pe aceeași planșă; `cifraSchimbata` (validarea se reface doar când cifra din planșă se schimbă — la coliziune cifra nu se schimbă, deci „validat rămâne validat”, ca în R4);
- **interacțiunea nouă**: poziția cu cifra **altei planșe** nu primește nici nota coliziunii (regula R5: planșa de acum nu scrie pe rândul altei planșe); apare o intrare în `ambigue` cu `grupuri` și ambele motive. Mulțimea `neatinse` din R5 a dispărut: după regula R4 ea ar fi conținut doar pozițiile altei planșe, pe care le prinde direct `cifraAlteiPlanse`. Restul „doar de verificat” care cade pe poziția unei coliziuni din aceeași planșă se adaugă la nota coliziunii (comportamentul R4), nu mai e „neatins”.

**Testul ADV5 a fost adaptat** (singurul test modificat la rebase-ul 2): aserțiunea „1755 nu primește nimic” contrazicea direct testul R4 „runda 6 MAJOR transfer: Dn110 PE 500 + Dn110 OL 90 … => ambiguu” pe același scenariu, deci nu se putea repara din cod fără să pice unul din ele. Intenția ADV5 e păstrată: nicio cifră scrisă sau adunată, ambele grupuri numite, aceleași operații și același raport în orice ordine. Teste noi (în `cantitati_nevalidate_test.ts`, secțiunea „Rebase R5 peste R4 runda 6”): coliziune pe poziția altei planșe (0 operații, o intrare cu `grupuri`); rest „doar de verificat” pe poziția unei coliziuni (notă adăugată, `actiune: 'nota'`); coliziune pe rând validat (fără status în patch — **înlocuit pe 26.09 de varianta B, §8.5**: rândul iese din „validat”). Contra-probe (copii în scratchpad): fără poarta „altă planșă” în ramura coliziunii (doar R4) pică primul; cu coliziunea „neatinsă” pentru rest (doar R5) pică al doilea.

### 2.4 Rebase-ul 3 peste `claude/r4-rezervare-zone` @ 7a7bf86 (R4 rundele 7–9)

Conflictele au fost tot în `treciInCantitati`, la commit-urile R5 inițial și runda 4. Rezolvare, păstrând ambele semantici:
- **din R4 7–9**: `unde()` (unde a ajuns fiecare grup sigur — pentru notele MY-T4 și „total cu Dn”), `rest.incomplet` (B3 / NFL / DN0) în condiția „extras” ⇒ „diferenta”, `preferaFaraTotal` (pus acum în `candidatiDn`, deci și ramura „doar de verificat” o folosește), rândul TOTAL care nu mai e candidat pe Dn;
- **din R5**: `randDinPlansa` / `cifraAlteiPlanse` / `cifraSchimbata`, ordinea deterministă a grupurilor; poziția cu cifra altei planșe apare și în `unde` („cu cifra altei planșe (nescris)”).
Singurul test adaptat: R4 „runda 9 TOTAL-a” cerea ca poziția VALIDATĂ cu cifra din planșă 480 m să rămână „validat” după o recitire de 500 m — sub regula R5 (`cifraSchimbata`) iese din „validat”, cu aprobarea veche numită; intenția R4 (poziția primește cifra, subtotalul doar notă, un singur update pe id) e verificată în continuare. Suita după rebase: deno 181/181 (cu type-check), înainte de lucrul pe condiții.
### 2.5 Rebase-ul 4 peste `claude/r4-rezervare-zone` @ 3039f2a (R4 rundele 10–12)

Un singur conflict, în `treciInCantitati` (commit-ul R5 runda 4): R4 rundele 10–12 au introdus `candidatiPe` (prioritatea pozițiilor non-„total” înaintea filtrului pe material, fail-safe pe „total” nerecunoscut, compatibilitatea de material) și `candidatiAmbigui`; R5 avea `candidatiDn` (vechea ordine: material, apoi „total”). Rezolvare: **`candidatiPe` (R4) înlocuiește `candidatiDn`**, care e scos; R5 păstrează ordinea deterministă a grupurilor (Dn desc, apoi material) și `dv` calculat o dată. Commit-ul `d167e94` adaptează 5 teste R4 (ADV10-1, ADV11-1, ADV12-1, ADV12-2 recunoscute, ADV12-4): poziția / TOTAL-ul VALIDAT (sau „extras”) a cărui cifră din planșă se schimbă iese pe „diferenta” (regula R5 `cifraSchimbata`), cu aprobarea veche numită pe cel validat; intenția R4 (ce poziție primește cifra, subtotalul doar notă, un update pe id, `ambigue`) rămâne verificată neschimbată (helper `dupaR5`). După rebase: deno 208/208, vitest 478/478, cas-felii 34/34.

## 3. Lic. 95: derivatele și marcarea lor

### 3.1 Starea din BD (SELECT, 25.09.2026, după transferul din 16:51:08 UTC; preview-ul recontrolat pe 26.09.2026 — identic)

- Sursa: `ofertare_cantitati` 1751–1756, toate `status='extras'`, `tip_sursa` NULL, `extras_de_ai` true, sursa „Planșa 1 — tabel de dimensionare, citit automat din scanare”. Σ `cantitate` = Σ `cantitate_plansa` = 48.195 m. Pe diametre: Dn200 17.785, Dn125 2.275, Dn110 780, Dn90 4.545, Dn63 9.670, Dn40 13.140.
- Doc 470: `citire_ai.transfer` = `facut` (rulare `250693c3…`), iar `sumar.cantitati` = 6 adăugate, 0 actualizate, `total_m` 48.195. E jurnalul scriitorului: nu îl citește niciun consumator (grep `pe_diametre` / `sumar.cantitati`). **Nu se marchează**: `analiza.citire_ai` e scris cu CAS pe `rev`, deci o scriere manuală ar concura cu handler-ul.

| Derivat posibil | n | Observații |
|---|---|---|
| `grafic_versiuni` / `grafic_parametri` (fronturi) / `grafic_activitati` | 0 / 0 / 0 | — |
| `ofertare_pt_poarta` / `ofertare_pt_pachet` | 0 / 0 | `pt_versiune` NULL |
| `ofertare_pt_capitole` (cu cifrele 1751–1756) | 0 | lic. 95 n-are niciun capitol |
| `ofertare_verificari` | 0 | — |
| `ofertare_clarificari` legate (`cantitate_id` 1751–1756) sau `platforma` după transfer | 0 | #63 (automat, 11:50, editat 16:43) și #64 (manual, 16:03) sunt create **înainte** de transfer. Cifrele din #63 (44.355, 11.525, 32.830, 29.555) vin din memoriu / CS, nu din rânduri. |
| `ofertare_rfq` | 0 | — |
| `ai_usage_log` după transfer — pe licitație (`ofertare_licitatii`, 95) / pe documentele ei (`ofertare_documente_atribuire`, doc-urile lic. 95) | 0 / 1 | pe licitație: ultimul apel 14:14:52. Pe documente: **#4632** (`ofertare-plansa-citeste`, doc 470, 16:52:38) = pasul „note lipite” al **aceleiași** citiri, nu un derivat al rândurilor. În runda 3, „0 apeluri AI după transfer” era adevărat doar pentru filtrul `ref_table='ofertare_licitatii'`; preview-ul acoperă acum ambele |
| `v_ofertare_pt_stare` (95) | — | `lista_f3_m`, `lista_c6_m`, `memoriu_m`, `plansa_m`, `grafic_fronturi_m` = NULL |
| `v_ofertare_contradictii` (95) | 0 | — |

### 3.2 SQL propus (NEEXECUTAT): `docs/R5_REVERIFICARE_LIC95_PROPUS.sql`

- **(0) Preview**: un singur SELECT pe cele 10 surse. Rulat pe producție pe 25.09 și 26.09 (e doar SELECT): **0 în cele 9 surse de derivate; `ai_usage_log` = 1 (#4632, vezi mai sus)**. Runda 4: linia `ai_usage_log` numără și `ref_table='ofertare_documente_atribuire'` pe documentele lic. 95.
- **(1) Aplicare** (bloc DO, o tranzacție, idempotentă). Nu face nimic dacă 1751–1756 au fost între timp validate. Pune marcajul „[R5-reverificare 25.09.2026: calculat pe cantități NEVALIDATE…]” pe:
  - `grafic_versiuni.nota` (snapshot cu un rând 1751–1756 nevalidat);
  - `ofertare_pt_poarta.nota` (semnată după transfer);
  - `ofertare_pt_pachet.nota` (doar pachetul nedepus);
  - `ofertare_pt_capitole.nota` (capitole care citează cifrele; `nota` nu creează versiune nouă);
  - `ofertare_clarificari.sursa` (doar ciornele `propunere` / `de_trimis`).

  Ce e depus sau trimis, `grafic_parametri` și verificările doar se **raportează** în NOTICE. Pe starea de azi blocul nu modifică nimic: e o plasă pentru fereastra de până la merge. Nu atinge rândurile 1751–1756, ca să nu strice gărzile SQL-ului R5 v2 pe `diferenta_nota`.
- **(R) Rollback** exact: scoate aceeași constantă, iar `NULL` redevine `NULL`.
- **(3) Opțional, la ritualul de final de sesiune**: linia din `handoff_activ` (v5, 25.09 17:47), „470 re-citită complet: … 48.195 m … (candidat; +3.840 m vs 44.355 CS = Dn200 din afara UAT …). NU aplicat în ofertă.”, conține două afirmații de reverificat:
  - „NU aplicat în ofertă”: rândurile există în `ofertare_cantitati`, cu Σ `cantitate` 48.195. Sunt nevalidate, deci neaprobate, dar prezente.
  - „+3.840 = Dn200 din afara UAT”: R5 v2 a respins atribuirea (Nr 1–4 = 13.765 m; diferența candidată e +4.440…+4.560).

Testat local pe PGlite (Postgres 18.3 în proces, de unică folosință): runda 3 8/8; runda 4 (`test_rev95_runda4.mjs`, cu `ofertare_documente_atribuire` și #4632 în schemă) **9/9** — în plus: preview-ul numără exact #4632, nu un apel pe documentul altei licitații și nici unul de dinainte de transfer:
- preview 0 și aplicare fără efect pe starea de azi;
- pe derivate sintetice se marchează exact v1, poarta, pachetul nedepus, capitolul și ciornele; cele depuse sau trimise doar se raportează;
- rularea e idempotentă, iar #63 și #64 rămân neatinse;
- rollback-ul readuce starea identică;
- cu rândurile validate, blocul nu face nimic.

## 4. Migrarea propusă: `docs/R5_MIGRARE_PROPUSA_cantitati_nevalidate.sql` (+ `_ROLLBACK.sql`)

1. **`v_ofertare_cantitati_nevalidate`** (26.09: **extins la condiția 2** — metri pe surse, rândurile fără tip, rândurile invalidate ieșite din rețea; §9) (`security_invoker = on`; REVOKE anon/PUBLIC; GRANT SELECT authenticated/service_role). Numără, pe licitație, rândurile de rețea nevalidate pe `tip_sursa`, cu **exact** filtrul `qm` din `v_ofertare_pt_stare`. Preview pe producție (corpul view-ului rulat ca SELECT, 25.09):

   | lic. | F3 nevalidate (m) | C6 | memoriu | planșă | fără tip | rețea nevalidate / total |
   |---|---|---|---|---|---|---|
   | 3 | 0 | 0 | 3 | 0 | 0 | 3 / 5 |
   | 5 | **47 (6.520)** | 5 | 11 | 20 | 0 | 94 / 94 |
   | 95 | 0 | 0 | 0 | 0 | **6** | 6 / 6 |
   | 102 | 0 | 0 | 0 | 0 | 2 | 2 / 2 |

   Efect după aplicare și merge: pe lic. 5 (Domnești, termen 18.09 trecut) H2 devine block până se validează cele 47 de rânduri F3. Celelalte licitații n-au F3, deci H2 rămâne block „lipsește F3”, ca azi.
2. **`ofertare_clarificare_planse_auto` v6.** Baza e funcția **live**: repo v5 plus linia `v_nou := v_noi > 0 AND v_standard`, verificată prin md5 al corpului fără comentarii și spații = `0875c2200e072289cd5b972b49cabb0c`. Față de live se schimbă:
   - totalul F3 vine doar din `status='validat'`;
   - cu rânduri F3 nevalidate, punctul 2 devine „Corespondența fiecărui tronson cu poziția din lista de cantități (F3) în care este cuprins;”, fără total;
   - `f3_nevalidate` apare în rezultat.
   - **runda 4**: setul F3 = **exact filtrul `qm` / `lista_f3_m`** din `v_ofertare_pt_stare` (`um='m'`, categorie conductă/rețea, fără „total” în obiect/denumire/sursa) — același set ca view-ul nou și H2. Filtrul vechi (um m/ml/M, denumire conduct/țeav/tub) lua și articole de deviz și rândurile TOTAL: la lic. 5 (SELECT 26.09) 62 de rânduri / 7.747,68 m față de 47 / 6.519,79 m pe `qm` (diferența = 15 rânduri cu um „M”, 1.227,89 m, ex. „MONTAREA PARAPETELOR SI PODETELOR…”; 0 rânduri TOTAL azi);
   - **runda 4**: numărul se scrie ro-RO fără ambiguitate, independent de `lc_numeric` („,” și „.” din șablonul `to_char` sunt fixe, apoi `translate`): 6.519,8 / 6.520 / 1.234.567,8. Live scria „7.747.7” (mii și zecimale cu același punct).
   Azi n-ar schimba nicio ciornă: singura `auto_planse` e #63, text de om.
3. **Rollback**: DROP VIEW și funcția live exactă (md5 identic, verificat local). Se face **împreună cu revert-ul codului**.

Testat pe PGlite (Postgres 18.3 compilat WASM, **nu** Postgres 16 cum scria antetul în runda 3 — corectat):
- runda 3 (`test_r5.mjs`), 12/12 atunci: rollback = live (md5); live citează „1.600.0 m” din rânduri nevalidate (bug reprodus); view-ul (lic. 95 = 6 fără tip, lic. 5 = 1 F3 nevalidat, 500 m, TOTAL exclus); `security_invoker` și grant-urile; v6: F3 nevalidată → fără total, validată → total, fără F3 → varianta veche; rollback. Pe v6 din runda 4, două verificări ale lui pică **intenționat** („2.000.5 m” și „1.600.0 m” după validare — exact defectele raportate);
- runda 4 (`test_r5_runda4.mjs`), **11/11**: live pe o F3 validată cu TOTAL validat și un articol de deviz „M” citează „2.250.0 m” (dublare + deviz + format); v6 citează „1.000 m”; lic. 7 „2.000,5 m”; „1.234.567,8 m”; lic. 5 după validare „800 m” (TOTAL exclus; runda 3 dădea „1.600.0”) și ciorna rămâne `de_trimis`; `f3_nevalidate` (v6) = `lista_f3_nevalidate` (view) pe toate licitațiile de test; `lc_numeric=C` nu schimbă formatul; rollback = live (md5), view șters.

## 5. Teste rulate

Runda 4 (26.09.2026, după rebase peste `claude/r4-rezervare-zone` și commit-urile rundei 4):

| Comandă | Rezultat |
|---|---|
| `npx vitest run` | 18 fișiere, **417/417** (runda 3: 401; +2 CAD, +10 aprobare/fronturi, +4 `graficPoartaCalcul`) |
| `npm install && npx vite build` | build OK (doar avertismentul obișnuit de mărime a chunk-urilor) |
| `deno test --node-modules-dir=none --no-lock -A supabase/functions/` | **137/137**, **cu** type-check (runda 3 avea nevoie de `--no-check` pentru `core.ts`; cele 4 erori TS preexistente au primit adnotări de tip) |
| — din care `ofertare-plansa-citeste/` | **116/116** (R4 105 + R5 11 noi în runda 4); rulat de 10 ori: 10/10 verde |
| — din care `ofertare-clarificari-propune/core_test.ts` | **4/4** |
| `node scripts/test-cas-felii.mjs` | **34/34** |
| `deno check` pe `ofertare-plansa-citeste/index.ts`, `ofertare-clarificari-propune/index.ts`, `worker/ofertare/clarificari.ts` | curat (0 erori; runda 3: cele 4 preexistente) |
| contra-probe runda 4 (testele noi pe codul de dinainte, 233753a, copii temporare în scratchpad) | planșă: **8/8** teste noi de comportament pică (validat + cifră nouă, lic. 3 real, ADV1, cursa → RPC, ADV4, ADV3 ×2, ADV5); controalele negative (aceeași cifră ⇒ validarea rămâne; lic. 3 azi fără recitire diferită) trec pe ambele. CAD: **4/4** noi pică, 2 vechi trec |
| testele adversariale ale verificatorului (`scratchpad/adv3/r5cons/adv_test.ts`, scrise să treacă pe defect) | ADV1–ADV4 **pică acum** (defectul nu se mai reproduce); ADV5 (ordinea) **trece**; ADV6 testează `controlCantitatiGrafic.totalRetea`, nu referința „front” — acoperit în vitest (`controlFronturiGrafic`) |
| contra-proba din runda 3, corectată | pe HEAD 8a6fbbb **2/5** teste ale planșei pică comportamental („Planșa 1 confirmă” fals); al treilea pica doar pentru că exportul `treciInCantitati` lipsea — „3/5” din runda 3 era greșit |
| PGlite: `test_r5_runda4.mjs` (v6), `test_rev95_runda4.mjs` (lic. 95), `test_lic3_r4.mjs` (lic. 3) | **11/11, 9/9, 7/7** |
| `md5sum deno.lock` înainte / după | `875e293d073b359f7433064914937c86` neschimbat |

**Instabilitate preexistentă**: `concurenta_test.ts` „R4: două rulări concurente pe zone diferite” (temporizări de 5 și 30 ms) a picat o dată din 7 rulări la verificatorul rundei 3. Testul vine din #475 (af02588, pe `main`), fișierul nu e atins de R5 decât în testul „identitate 470” (§2.2); aici 10/10 rulări verzi. De tratat separat (temporizările → promisiuni explicite), nu în R5.

Runda 3 (25.09.2026, pe 990a6b1, înainte de rebase): vitest 401/401; build OK; planșă 49/49 (majoritar — vezi instabilitatea); `core_test.ts` 4/4 cu `--no-check`; PGlite `test_r5.mjs` 12/12, `test_rev95.mjs` 8/8.

### 5.1 După rebase-ul 2 (peste R4 runda 6, ab5c449)

| Comandă | Rezultat |
|---|---|
| `deno test --node-modules-dir=none --no-lock -A supabase/functions/` | **149/149**, cu type-check |
| — din care `ofertare-plansa-citeste/` | **128/128** (R4 runda 6 + R5 runda 4 + 3 teste noi ale interacțiunii) |
| `concurenta_test.ts`, de 10 ori | 10/10 verde (59/59 la fiecare rulare) |
| `npx vitest run` | 18 fișiere, **417/417** |
| `npm install && npx vite build` | build OK |
| `node scripts/test-cas-felii.mjs` | **34/34** |
| `deno check` pe `ofertare-plansa-citeste/index.ts`, `ofertare-clarificari-propune/index.ts`, `worker/ofertare/clarificari.ts` | curat |
| `md5sum deno.lock` înainte / după | `875e293d073b359f7433064914937c86` neschimbat |

### 5.3 Runda 5 (verificatorul condițiilor 1–2, 26.09.2026) — pe codul final

| Comandă | Rezultat |
|---|---|
| `deno test --node-modules-dir=none --no-lock -A supabase/functions/` | **190/190**, cu type-check (+4: `ofertare-plansa-citeste/aprobare_runda5_test.ts`) |
| `concurenta_test.ts`, de 10 ori | 10/10 verde (73/73 la fiecare rulare) |
| `deno check` pe `ofertare-plansa-citeste/index.ts`, `ofertare-clarificari-propune/index.ts`, `worker/ofertare/clarificari.ts` | curat |
| `npx vitest run` | 20 fișiere, **478/478** (+12 `ofertareCantitatiInvalidare.test.js`, +6 `ofertareCantitatiAprobare.test.js`, +2 `api/_cadCantitate.test.js`) |
| `npm install && npx vite build` | up to date; build OK; `package-lock.json` neschimbat |
| `node scripts/test-cas-felii.mjs` | **34/34** |
| PGlite `test_aprobare_istoric.mjs` (default privileges emulate ca în Supabase; rândurile reale lic. 3 + lic. 95) | **61/61** (+13: pașii cumulați 5 × 0,99 pe rândul 3 → invalidat la al doilea; nota SQL = JS cu referința citită din istoricul SQL; revalidare = referință nouă; cifra din planșă cumulat; `um` m → M / „m ”; `ofertare_norm_text` = `normText` pe 9 texte; NBSP = sub prag în ambele; TRUNCATE / INSERT / UPDATE / DELETE refuzate pentru authenticated) |
| același test pe migrarea de la `f6fdd51` (`inainte_aprobare_istoric.mjs`) | **pică** 11 verificări noi (TRUNCATE permis, 5 × 0,99 → „validat, validat, validat, validat, validat”, m → M rămâne validat …), apoi se oprește la `ofertare_norm_text` inexistentă |
| control negativ (trigger-ul dezactivat după aplicare, `neg_aprobare_istoric.mjs`) | pică de la primul scenariu (≥ 18 verificări) |
| PGlite `test_view_conditia2.mjs` | **13/13** (+6: ordinea migrărilor, drepturile view-ului, lanțul recitire → tot numărat din istoric, H2 tot BLOCK, revalidare, repro A5 „m” → „M”) |
| PGlite `test_r5_runda4.mjs` (v6) / `test_r5.mjs` | **14/14** (+3: lic. 11 F3 cu un rând „m” → „M” invalidat ⇒ v6 fără total; trigger-ul pe 800,5 → 300,4); `test_r5.mjs` 10/12 — cele 2 eșecuri intenționate din runda 4 |
| mutații (fiecare pe codul final, revenit după): plasa fără `pastreazaInvalidarea` / fără referință / fără regulă; `esteInvalidat` fără istoric; regula care ignoră referința; `um` normalizat; poarta care ignoră reverificarea; CAD fără prefix / fără referință | **9/9 prinse** |
| `md5sum deno.lock` | `875e293d073b359f7433064914937c86` neschimbat |

### 5.4 Runda finală (6) — pe capul `10e107d` (cifrele reale, rulate pe worktree)

| Comandă | Rezultat |
|---|---|
| `deno test --node-modules-dir=none --no-lock -A supabase/functions/` | **214/214**, cu type-check |
| — din care `ofertare-plansa-citeste/` | **192/192** (`agregare_test.ts` 57, `aprobare_runda5_test.ts` 5, `cantitati_nevalidate_test.ts` 28, `concurenta_test.ts` 88, `poarta_test.ts` 14) |
| — restul | `ofertare-cantitati-extrage` 12, `ofertare-clarificari-propune` 5, `ofertare-seap-import` 5 |
| `concurenta_test.ts`, de 10 ori | **10/10** verde (88/88 la fiecare rulare) |
| `deno check` pe `ofertare-plansa-citeste/index.ts`, `ofertare-clarificari-propune/index.ts`, `worker/ofertare/clarificari.ts` | curat |
| `npx vitest run` | 20 fișiere, **494/494** (+16: unitatea normalizată, „unitate schimbată”, `citestePaginat`, TOTAL invalidat, H2 runda 6) |
| `npm install && npx vite build` | up to date; build OK (avertismentul obișnuit de mărime); `package-lock.json` neschimbat |
| `node scripts/test-cas-felii.mjs` | **34/34** |
| PGlite `test_aprobare_istoric.mjs` (default privileges emulate; rândurile reale lic. 3 + lic. 95) | **66/66** (aplicare fără schimbări de date, rollback curat, RLS / grant / search_path, TRUNCATE refuzat; runda 6: m → M / „m ” / NBSP = sub prag, m → ML = invalidare cu nota SQL = JS, „unitate_schimbata” pe rândul nevalidat, referința nu vine din ea) |
| PGlite `test_view_conditia2.mjs` | **18/18** (A5 runda 6: F3 validat m → M rămâne validat, `um_de_normalizat_f3`, H2 BLOCK; F3 nevalidat m → ml ⇒ `unitate_schimbata_in_afara_retea`; TOTAL invalidat numărat separat; EXECUTE pe `ofertare_norm_text` pentru authenticated, nu pentru anon) |
| PGlite `test_r5_runda4.mjs` (v6) | **16/16** (lic. 9: deviz „M” ⇒ fără total; lic. 11 m → M validat ⇒ fără total, M → ml invalidat ⇒ fără total; lic. 12: F3 nevalidat m → ml ⇒ fără total) |
| PGlite `adv_r6.mjs` (adv_r5r5 al verificatorului, pe worktree) | **13/13** — fuzz 600 de pași (um m / M / ml / „ m” / NBSP / null / ML): decizia trigger-ului = JS, note identice; repro-ul minorului 1 (rândul 6 niciodată validat, 700 m, m → ml) e acum semnalat în view și în `randuriLipsa`; TOTAL invalidat: același tratament în view și JS |
| control negativ: aceleași 4 scripturi PGlite pe copia `6313184` | pică: 5 (`test_view_conditia2`), 5 (`test_aprobare_istoric`), 3 (`test_r5_runda4`), `adv_r6` se oprește după 2 eșecuri (`normUm` inexistentă) |
| mutații (`scratchpad/mutatii_r6.py`, fiecare pe codul final, restaurat după) | **12/12 prinse**: um exact; `unitate_schimbata` care șterge „invalidat”; paginare cu pas fix; fără „unitate schimbată” / TOTAL în lipsă; H2 fără TOTAL; `randuriFront` exact; varianta B scoasă; doar marcajul vechi; referința pas B = cifra din planșă; pas B fără actualizarea extragerii; istoric crescător |
| `md5sum deno.lock` | `875e293d073b359f7433064914937c86` neschimbat |

### 5.2 După rebase-ul 3 (peste 7a7bf86) și condițiile Copilot (26.09.2026, cap `5ab80c1` + documentul)

| Comandă | Rezultat |
|---|---|
| `deno test --node-modules-dir=none --no-lock -A supabase/functions/` | **186/186**, cu type-check |
| — din care `ofertare-plansa-citeste/` | **164/164** (`cantitati_nevalidate_test.ts` 23/23: +2 varianta B, +3 R5 pas B) |
| — din care `ofertare-clarificari-propune/core_test.ts` | **5/5** (+1: trunchierea la 40 spusă modelului) |
| `concurenta_test.ts`, de 10 ori | 10/10 verde (73/73 la fiecare rulare) |
| `npx vitest run` | 20 fișiere, **458/458** (+27 `ofertareCantitatiInvalidare.test.js`, +3 `ofertareTransferRaport.test.js`, +5 grafic / +1 câmpuri în `ofertareCantitatiAprobare.test.js`, +1 `graficPoartaCalcul.test.js`, +5 H2 în `ofertareControale.test.js`) |
| `npm install && npx vite build` | up to date; build OK (avertismentul obișnuit de mărime a chunk-urilor); `package-lock.json` neschimbat |
| `node scripts/test-cas-felii.mjs` | **34/34** |
| `deno check` pe `ofertare-plansa-citeste/index.ts`, `ofertare-clarificari-propune/index.ts`, `worker/ofertare/clarificari.ts` | curat |
| PGlite 0.5.8 (PG 18.3): `pglite/test_aprobare_istoric.mjs` (migrarea condiției 1, rândurile reale lic. 3 + lic. 95) | **48/48**; control negativ (trigger-ul dezactivat după aplicare): 17 pică |
| PGlite: `pglite/test_view_conditia2.mjs` (view-ul extins + trigger-ul, capăt-la-capăt cu H2) | **7/7** |
| PGlite: `test_r5_runda4.mjs` / `test_r5.mjs` (v6 + view, după extinderea view-ului) | 11/11; `test_r5.mjs` 10/12 — cele 2 eșecuri intenționate din runda 4 (§4) |
| mutații (fiecare pe codul final, revenit după): H2 fără post-procesare; rândurile invalidate ieșite nescăzute din lipsă; „front” verde pe referință incompletă; denumirea ignorată la invalidare; `doar_de_verificat` ignorat în raport; varianta B scoasă; R5 pas B scos; marcajul pas B nepăstrat; golirea pe pas B | **9/9 prinse** (1–5 teste pică la fiecare) |
| `md5sum deno.lock` înainte / după | `875e293d073b359f7433064914937c86` neschimbat |

## 6. Rămâne / de decis (Razvan)

1. **GO pentru migrările propuse**, înaintea merge-ului ramurii: (a) `docs/R5_MIGRARE_PROPUSA_cantitati_nevalidate.sql` (view extins la condiția 2 + v6) — altfel H2 = „nu putem verifica” pe licitațiile cu F3; (b) **`docs/R5_MIGRARE_PROPUSA_aprobare_istoric.sql`** (condiția 1: istoric + trigger; rollback `_ROLLBACK.sql`, păstrează istoricul). **Runda 5: ordinea contează — ÎNTÂI (b), apoi (a)** (view-ul și v6 citesc istoricul; (a) fără (b) se oprește cu mesaj, fără să creeze nimic). Rollback: (a) înainte de R2-ul lui (b). După aplicare: `get_advisors` + actualizarea `registru_automatizari` (trigger nou: nu citește conținut extern, scrie doar în istoric și statusul / nota rândului atins).
2. **Push, PR, merge** pentru `claude/cantitati-nevalidate-consumatori` (conține acum și `claude/r4-rezervare-zone` — se merge-uiește după / împreună cu ea), plus **deploy** pentru `ofertare-clarificari-propune` și `ofertare-plansa-citeste`. Workerul NAS ia `core.ts` la pull. `api/cad-parse.js` pleacă la Vercel cu merge-ul.
3. **Lic. 3, rândurile 2, 3, 4** (validate înainte de transferul planșei 1.1): GO pentru `docs/R5_LIC3_VALIDARI_INAINTE_DE_TRANSFER_PROPUS.sql` (preview 3 rânduri → `diferenta` + notă, rollback exact) sau le revalidează direct un om, văzând ambele cifre (Dn180: 1.100 vs 2.210).
4. **Lic. 3, rândul 9** (CAD, 35.620,59 m, `validat` automat pe 28.08): îl trecem pe `extras` (preview → GO → UPDATE cu RETURNING) sau îl lăsăm validat? N-am scris SQL de aplicare.
5. **Schimbări de comportament de confirmat** (runda 4): (a) o recitire a planșei / o re-măsurare CAD cu altă cifră redeschide un rând validat; (b) fronturile salvate înainte de 26.09 blochează poarta graficului până la re-propunere sau „✋ Le asum ca manuale” (lic. 3 — dacă butonul e prea permisiv, se scoate și rămâne doar re-propunerea); (c) referința „front” nu mai folosește un total nevalidat.
6. **Validarea cantităților de lic. 95** (Oana Nica / responsabilul), după R5 v2 și #63. Până atunci graficul și fronturile rămân blocate, iar asta e comportamentul dorit. Dn200 1751 (17.785, dintre care 13.765 din afara UAT) nu trebuie validat în forma de azi.
7. **SQL-ul pentru lic. 95** (`docs/R5_REVERIFICARE_LIC95_PROPUS.sql`) se rulează doar dacă apar derivate înainte de merge. Azi preview-ul dă 0 derivate (plus #4632, care nu e derivat).
8. **Lic. 5**: 47 de rânduri F3 de rețea de validat, dacă licitația mai e în lucru (termen 18.09 trecut, status `in_lucru`).
9. **Decizii luate pe principiul Copilot, fără să aștepte (de confirmat sau răsturnat):** (a) coliziunea a două grupuri sigure pe un rând VALIDAT ⇒ varianta B (§8.5); (b) schimbările sub prag (cifre < 1 m pe lungimi, majuscule / spații în text) nu invalidează, dar se scriu în istoric; (c) regula de invalidare în editorul 📋 Cantități cere confirmare; refuzul anulează editarea; (d) H2 dă WARN (nu block) pe rândurile de rețea fără tip de sursă — lic. 95 (6 / 48.195 m) și lic. 102 (2 / 167.200 m) — și BLOCK pe rândurile invalidate ieșite din rețea.
10. **Decis în audit (runda 6, pe principiile Copilot, reversibil — de confirmat sau răsturnat, §11):** (a) ramurile „doar de verificat” / MY-T4 / „total cu Dn” pe un rând VALIDAT ⇒ varianta B; (b) `um` normalizat (trim + lower) în regulă și în filtrele de rețea — „m” → „M” nu mai invalidează (runda 5 îl invalida), „m” → „ml” da; (c) rândul neaprobat cu unitatea schimbată din / în „m” ⇒ semnal „unitate schimbată”; (d) rândul TOTAL invalidat listat ca referință (JS) și numărat separat (view), BLOCK în ambele porți; (e) pasul B: `cantitate` = valoarea verificată, ambele marcaje recunoscute. Istoricul nu are încă ecran (se citește din BD).
11. **Runda 5 — de confirmat (neschimbate; punctul (b) și 9 (b) sunt înlocuite de runda 1b, §12 — nu mai există prag de 1 m):** (b) pragul de 1 m se măsoară față de valoarea de la ultima validare (după revalidare, referința e cifra nouă); (c) poarta propunerii dă WARN (nu block) când graficul înghețat nu mai corespunde cantităților de acum. Punctul (a) al rundei 5 („`um` exact”) e înlocuit de §11.3.
12. **Lic. 5 — efect al unității normalizate (§11.3):** 15 rânduri F3 „M” (1.227,89 m, `extras`) sunt articole de deviz (borduri, parapete) încadrate de dicționar în „Conducte și montaj”; după migrare intră în setul de rețea al view-ului (F3 nevalidate 47 → 62) și sunt semnalate (`um_de_normalizat_f3` = 15, H2 BLOCK); v6 nu citează total. Corecția e categoria lor (nu unitatea), la cine lucrează lic. 5.

## 7. Constatări secundare (nereparate aici)

- **„validat” nu are autor și nici oră.** Tabela n-are `validat_de` / `validat_la`, deci `status='validat'` nu dovedește că a validat un om (rândul 9 al lic. 3 a fost marcat automat de CAD). **Corectat în runda 4**: în runda 3 scria aici că `updated_at`-urile rândurilor 2, 3, 4 ale lic. 3 (15.09 15:19:53.008 / .032 / .053, „3 actualizări în 45 ms”) ar fi validări automate. Nu sunt: sunt **scrierile transferului planșei 1.1** (rândul 1 e în același batch, la 15:19:52.967, toate cu nota „Memoriu X vs planșa 1.1 Y”). Validările au fost **anterioare** transferului, iar transferul a scris apoi cifre noi peste rânduri validate — exact problema MAJORĂ din §2.1. Concluzia „validat n-are autor” rămâne; o schemă nouă (coloane + UI) e decizia lui Razvan.
- **Transferul din planșă pune cifra planșei în `cantitate` și lasă `tip_sursa` NULL** pe diametrele noi (1751–1756). Orice consumator care citește `cantitate` drept „memoriu/F3” vede o cifră de planșă; exemplu: baza „memoriu / F3” din grafic. Cu regula „doar validat” riscul e închis, dar semantica rămâne ambiguă. De decis împreună cu pasul A din R5 v2 (care propune `cantitate` NULL pentru Nr 1–4).
- **RPC-ul de transfer aplică `status` din patch fără gardă** pe statusul curent. **Runda 4**: fereastra de cursă e închisă în codul care construiește patch-ul (patch-ul poartă `diferenta` când cifra se schimbă, §2.1). Garda propusă în runda 3 (`… AND status = 'extras'`) ar fi **greșită** acum: ar lăsa o validare dată în fereastră peste o cifră nouă. RPC-ul rămâne neschimbat.
- **`ofertare_clarificare_planse_auto` (live) are două defecte**, rezolvate în v6 propusă (runda 4, §4) și încă prezente live până la migrare:
  - setul F3 ia și articole de deviz (lic. 5: +15 rânduri „M”, 1.227,89 m) și ar dubla un rând TOTAL; live 62 rânduri / 7.747,68 m față de 47 / 6.519,79 m pe `qm`;
  - formatul numărului iese „7.747.7 m” (`lc_numeric` = en_US.UTF-8; `replace(',', '.')`).
- **Fișierul `supabase/migrations/20260926b_…_v5.sql` din repo ≠ funcția live.** Repo are `v_nou := v_noi > 0`, live are `… AND v_standard`. Migrarea propusă pornește de la live.
- **Transferul: sursa rândului nu poartă id-ul documentului** — două planșe din documente diferite cu același nr. („Planșa 1”) se tratează ca aceeași planșă la recitire. Pozițiile ambigue din „altă planșă” (și `doar_de_verificat`) apăreau doar în rezumatul JSON al transferului — **reparat pe 26.09** (insigna „⚠ transfer”, §9). Coliziunea grupurilor sigure pe aceeași poziție se vede acum și pe poziție (nota R4 runda 6), cu excepția poziției altei planșe, care rămâne neatinsă.
- **Nota transferului scrie numele fișierului cu minuscule** („planșa „pl1.1.pdf””, `eticheta.toLowerCase()`, preexistent R4 / R5, asertat în testele R4). Cosmetic; de reparat odată cu testele R4.
- **„validat” tot n-are autor / oră în tabelă** (neschimbat). Istoricul propus (§8) are `autor` = `auth.uid()` al scrierii care a **ieșit** din validare, nu al celei care a validat.
- **Lic. 3**: `grafic_parametri` are 6 fronturi / 29.985 m, pe baza „planșe” (04.09), iar 3 din 5 rânduri de memoriu sunt nevalidate. Pe codul nou, „Propune din cantități” și generarea cer validarea întâi, iar fronturile vechi blochează poarta până la re-propunere / asumare ca manuale.

## 8. Condiția 1 (Copilot 26.09): invalidarea aprobării nu privește doar cifra

> „Schimbarea relevantă a unității, diametrului, materialului, SDR-ului, tronsonului/etapei sau sursei aplicabile poate face aprobarea anterioară nevalabilă chiar dacă lungimea e identică. Păstrează valoarea și aprobarea veche în istoric.”

### 8.1 Inventar pe BD (doar SELECT, 26.09.2026)

- `information_schema.columns` pe `ofertare_cantitati`: `id, licitatie_id, obiect, categorie, denumire, um, cantitate, specificatii, sursa, cantitate_plansa, diferenta_nota, status, extras_de_ai, created_at, updated_at, tip_sursa, cod_articol, ordine`. **Dn, material și SDR n-au coloane proprii** — stau în `denumire` („Țeavă PE100 SDR11 Dn180 — extravilan Mănăstirea→Coconi”) și `specificatii` („PE100 SDR11”). Corespondența atributelor Copilot: unitate = `um`; Dn / material / SDR = `denumire` + `specificatii`; tronson / etapă = `obiect` (+ textul din `denumire`); sursa aplicabilă = `tip_sursa` + `sursa` (+ `cod_articol` = poziția din listă); cifra = `cantitate` și `cantitate_plansa`; în plus `categorie` (decide dacă rândul e „de rețea” în grafic / H2) și `licitatie_id`.
- Statusuri: CHECK `extras | validat | diferenta | revizuit_clarificare`; în BD `extras` 1.080, `validat` 4 (2, 3, 4, 9 — toate lic. 3), `diferenta` 2. `diferenta` e deja statusul „de reverificat” (bifa ✓ îl duce în `validat`).
- **Nu există tabel de istoric / audit** pentru cantități (există doar `_backup_cantitati_*` manuale și `ofertare_acoperire_istoric` pentru alt obiect). RLS pe tabelă: politica `cant_all` (authenticated, `auth.uid() IS NOT NULL`). Trigger existent: `trg_categorie_cantitate` (BEFORE INSERT OR UPDATE OF denumire: recalculează `categorie` pe rândurile `extras_de_ai`).

### 8.2 Inventarul căilor care scriu aceste coloane

| Cale | Ce scrie | Pe rând validat | Acum (cod) |
|---|---|---|---|
| 📋 Cantități `saveC` (`src/OfertareCantitati.jsx`) | obiect, categorie, denumire, um, cantitate, specificatii, sursa, nota (fără status) | scria tăcut peste aprobare | `aplicaRegulaAprobare` + confirmare explicită („Aprobarea veche nu mai e valabilă… Continui?”; refuz = editarea anulată); gardă pe `updated_at` (rândul schimbat între timp nu se suprascrie) |
| 📋 Cantități `valideazaC` (✓ / ↩) | doar `status` (+ `updated_at`) | — | **validarea explicită**: neatinsă de regulă; acum cu gardă pe `updated_at` (nu se validează o cifră nevăzută) și refuzată cât rândul are editări nesalvate |
| 📋 Cantități `addC` / `delC` | INSERT `extras` / DELETE | DELETE pierdea aprobarea | în BD: istoric „sters” (trigger-ul propus) |
| Transferul din planșă (`treciInCantitati` → RPC `ofertare_transfer_plansa_cantitati`) | `cantitate_plansa`, `diferenta_nota`, `status`; INSERT rânduri noi | cifra: `cifraSchimbata` (runda 4); coliziunea: validat rămânea validat | `cifraSchimbata` prin regula comună; **coliziunea ⇒ varianta B (§8.5)**; plasa finală: orice patch pe un rând validat trece prin `aplicaRegulaAprobare` înainte de RPC |
| Citirea CAD (`api/cad-parse.js` → `_cadCantitate.js`) | `cantitate_plansa` (+ `cantitate`, `um` pe nevalidat), nota, status | runda 4: `cifraSchimbata` | `cifraSchimbata` prin regula comună (`_cantitatiInvalidare.js`); select-ul citește și `um` |
| `ofertare-cantitati-extrage` (edge) | doar INSERT `extras` | — | — |
| RPC-uri / funcții (`pg_proc.prosrc` cu tabela) | `ofertare_transfer_plansa_cantitati` (UPDATE / INSERT, de mai sus); `ofertare_clarificare_planse_auto` (doar citește) | — | RPC neschimbat (patch-ul poartă statusul) |
| Trigger `trg_categorie_cantitate` | `categorie` la UPDATE OF denumire | recalcul tăcut | trigger-ul propus rulează DUPĂ el (nume „zz”) și vede categoria nouă |
| SQL-uri propuse în `docs/` (R5 v2 A / B / RA / RB, lic. 3, lic. 95) | manual, cu GO | lic. 3: validat → diferenta | cu trigger-ul activ: istoric „redeschis”; rollback-ul lic. 3 rămâne exact (PGlite) |

Nicio cale nu salvează atribute + `status='validat'` în **același** UPDATE pe un rând deja validat (✓ e doar status), deci nu e nevoie de un „marcaj de validare”: validarea explicită ulterioară = UPDATE doar de status, trece mereu.

### 8.3 Regula (o singură sursă: `src/ofertareCantitatiInvalidare.js`)

Copii identice, verificate octet cu octet de test: `api/_cantitatiInvalidare.js` (CAD, Vercel) și `supabase/functions/ofertare-plansa-citeste/invalidare.js` (deploy-ul pe folder nu garantează `../_shared`).
- câmpurile aprobării: `licitatie_id, um, cantitate, cantitate_plansa, denumire, specificatii, obiect, categorie, tip_sursa, sursa, cod_articol`;
- cifre: |Δ| ≥ 1 pe unitățile de lungime (m / ml / fără unitate), orice Δ pe celelalte (buc); apariția / dispariția cifrei contează; **cifra din planșă se compară efectiv** (`cantitate_plansa`, altfel `cantitate`) — exact `cifraSchimbata` din runda 4, deci prima cifră din planșă egală cu memoriul nu e o schimbare;
- texte: orice schimbare după normalizare (spații comasate, majuscule); ce e doar de formă = „sub prag”;
- rând validat + schimbare relevantă ⇒ `status='diferenta'`, nota începe cu **„Rândul era VALIDAT — aprobarea veche (cantitate 1.100 m, cifra din planșă 2.210 m, ultima scriere 2026-09-15 15:19) nu mai e valabilă: s-a schimbat Dn 180 → 160; denumirea („…” → „…”). Valoarea și aprobarea veche rămân în istoric; validarea se reface.”** + nota veche (prefixul nu se adună la cicluri repetate); Dn / material / SDR se citesc din text doar ca să **numească** schimbarea;
- un patch care își pune singur alt status (transferul, CAD) rămâne cum e.

### 8.4 Migrarea propusă (NEAPLICATĂ): `docs/R5_MIGRARE_PROPUSA_aprobare_istoric.sql` + `_ROLLBACK.sql`

- **`ofertare_cantitati_istoric`**: `cantitate_id` (fără FK — supraviețuiește ștergerii), `motiv` (`validat` — runda 5, referința regulii — `| invalidat | redeschis | modificat_sub_prag | sters`), `status_vechi / status_cerut / status_nou`, `campuri` (relevante, față de valoarea aprobată) + `campuri_sub_prag`, `valori_vechi` (rândul întreg), `valori_noi` (câmpurile schimbate; la `validat` rândul întreg), `aprobare_veche` (status, ultima scriere, cifre, um, nota, sursa referinței, valorile aprobate), `nota`, `autor` (`auth.uid()`), `rol`. RLS; SELECT pentru authenticated cu `auth.uid() IS NOT NULL`; nicio politică de scriere (scrie doar trigger-ul); runda 5: `REVOKE ALL` de la PUBLIC / anon / authenticated (tabel + secvență) înainte de `GRANT SELECT` — default privileges dădeau și TRUNCATE; service_role ALL.
- **`fn_trg_ofertare_cantitati_aprobare`** (SECURITY DEFINER, `search_path = public, pg_temp`, REVOKE EXECUTE PUBLIC / anon / authenticated) pe **`trg_zz_…` BEFORE UPDATE** și **`trg_zz_…_del` AFTER DELETE**: aceeași regulă ca în JS — **nota SQL = nota JS octet cu octet** pe toate cele 16 scenarii (PGlite); `ofertare_cantitati_atribute(text)` și `ofertare_fmt_ro(numeric)` ajutătoare, verificate față de JS pe 20 de texte (15 reale) și 9 numere.
- **La aplicare nu se schimbă niciun rând** (PGlite: toate coloanele, toate rândurile lic. 3 + lic. 95, identice). Cele 4 rânduri validate de azi (2, 3, 4, 9) rămân validate până la prima scriere relevantă.
- Scenarii PGlite (48/48): invalidare pe fiecare atribut (um, Dn, material, SDR, obiect, categorie, tip_sursa, sursa, cod_articol, licitatie_id, cantitate, cifra din planșă, golire; TOTAL; m → buc; CAD 35.700 fără status); validare explicită ulterioară; al doilea ciclu fără prefix dublu; ↩ = „redeschis”; sub prag = validarea rămâne + istoric; editorul care retrimite aceleași valori = nimic; transferul cu status în patch = doar istoric; coliziunea B = „redeschis”; SQL-ul lic. 3 + rollback-ul lui exact; DELETE = „sters”; RLS (authenticated citește, nu scrie; anon nimic; autorul = `auth.uid()`); rollback R1 (trigger-e + funcții scoase, istoric păstrat, `trg_categorie_cantitate` neatins); R2 refuzat pe istoric nevid; reaplicare idempotentă.
- Preview (SELECT, 26.09): rândurile validate = 2, 3, 4, 9 (lic. 3).

### 8.5 Decizia pe coliziune (luată pe principiul Copilot, fără să aștepte): varianta B

Două grupuri sigure (Dn, material) pe o singură poziție **validată**: planșa nu confirmă cifra aprobată (identitate ambiguă = conflict vizibil). Până acum (R4 runda 6 + rebase-ul 2) rândul rămânea „validat” cu o notă — graficul și H2 foloseau în continuare cifra ca aprobată (verificatorul a reprodus-o pe lic. 3, rândul 2). Acum: `cantitate_plansa` rămâne neatinsă (nimic nu se adună / suprascrie), dar statusul trece pe `diferenta`, cu prefixul **„Rândul era VALIDAT cu cifra din planșă X m; planșa … dă N grupuri sigure pe aceeași poziție, fără să confirme cifra — validarea se reface.”** + nota R4 a coliziunii. Teste adaptate: R4 „runda 6 MAJOR transfer: … pe un rând validat: iese din „validat” (R5 varianta B)” și R5 „R5 varianta B: coliziune pe un rând VALIDAT …” (fost „rebase R4×R5: coliziune pe un rând VALIDAT … validarea rămâne”); control nou: coliziunea pe un rând `diferenta` nu dublează prefixul. Poziția cu cifra **altei** planșe rămâne neatinsă (nu e citirea ei), raportată ca ambiguă — acum și în UI (§9).

### 8.6 R5 pas B — eticheta (fost „handler.ts l.931–938 la 7fccfbe”; refăcută în runda 6, §11.1)

Pasul B (docs/R5_RECONCILIERE_470_V2.md §7.3, pe 1756) pune valoarea verificată de om în **`cantitate`** și marcajul **„ | R5 pas B (…): …”** la sfârșitul notei; `cantitate_plansa` rămâne istoricul extragerii automate. Runda 5 recunoștea doar „ | R5 v2 pas B” și trata cifra din planșă drept cea corectată — pe nota reală a lui B `corectieR5B` dădea `false`, iar marcajul se pierdea la orice recitire (RB refuza). Acum (`MARCAJE_R5B`, `pozMarcajR5B`, `valoareVerificataR5B`, `etichetaVerificataR5B`):
- ambele marcaje sunt recunoscute; eticheta: „valoarea verificată e cantitatea 13.740 m, verificată de om pe imaginea planșei (pasul B din R5)” — **fără literalul vreunui marcaj** (gărzile SQL caută `LIKE '%R5 pas B%'` / `strpos(…, ' | R5 pas B')`);
- recitirea se compară cu **cantitatea**: o confirmă ⇒ „recitirea o confirmă”, statusul neatins; diferă ⇒ `diferenta` (pe un rând validat: prefixul „Rândul era VALIDAT cu cantitatea 13.740 m (verificată de om …) … — validarea se reface.”); `cantitate` nu se scrie niciodată din transfer; `cantitate_plansa` (extragerea) se actualizează — nu și pe un rând VALIDAT pe care recitirea îl confirmă;
- ramura „doar de verificat” nu golește cifra rândului pas B și numește cantitatea „neatinsă”;
- marcajul (și tot ce urmează după el) rămâne la sfârșitul notei, o singură dată (plasa finală `plasaAprobare`): după recitire, `left(nota, strpos(nota, ' | R5 pas B') - 1)` = nota codului, iar sufixul = cel scris de SQL (RB funcționează).

## 9. Condiția 2 (Copilot 26.09): rândul invalidat / nevalidat nu dispare tacit

> „Rândul invalidat nu trebuie să dispară tacit din ofertă prin filtrul status='validat': consumatorul semnalează că lipsește o cantitate necesară și cere reverificarea rezultatelor dependente. Altfel înlocuim o valoare greșită cu un subtotal incomplet, aparent verde.”

Inventar: grep pe `src/`, `api/`, `supabase/functions/`, `worker/` după `ofertare_cantitati`, `esteAprobata`, `status='validat'` / `'validat'`, plus catalogul BD (§1). Singurul loc din cod care filtrează rândurile de cantități pe `validat` e `src/ofertareCantitatiAprobare.js` (grafic); în BD, v6 (`ofertare_clarificare_planse_auto`, propusă).

| Consumator | Folosește doar validate? | Semnal de lipsă (i) | Rezultat marcat incomplet (ii) | Poartă (iii) | Test |
|---|---|---|---|---|---|
| Poarta graficului, rândul „cant” | da (fronturi) | „lipsesc N rânduri necesare nevalidate (X m / cantitate pe unitate): #id „denumire” (status, cifră) …” + **lista completă** sub rând (și înghețată în `grafic_versiuni.poarta`); include rândurile INVALIDATE ieșite din rețea (ex. m → ml), rândurile TOTAL invalidate (referință, nu se adună; runda 6) și rândurile neaprobate ieșite din rețea prin schimbarea unității („unitate schimbată”; runda 6) | — | **BLOCK** (generarea e dezactivată; recalcul la îngheț) | `ofertareCantitatiAprobare.test.js` „condiția 2”, `graficPoartaCalcul.test.js` |
| Poarta graficului, rândul „front” | da (referința = rețeaua validată) | aceeași listă, scurtă | „INCOMPLET, de reverificat — …” + eticheta galbenă; niciodată `ok` cât lipsesc rânduri (warn / block) | prin „cant” | idem |
| „Propune din cantități” | da | toast cu lista rândurilor lipsă | „fronturi … INCOMPLETE” | — | `fronturiDinCantitati` în test |
| H2 (poarta propunerii / semnare, `controlCantitati`) | nu filtrează sumele (F3 = toate rândurile); F3 nevalidată = block (R5) | rândurile **invalidate ieșite din rețea** (`invalidate_in_afara_retea(_m)`), cele **fără tip de sursă** (`fara_tip_nevalidate(_m)`) și, din runda 6, **TOTAL invalidate** (`total_invalidate(_m)`), **neaprobate cu unitatea schimbată** (`unitate_schimbata_in_afara_retea(_m)`) și **de rețea cu unitatea ≠ exact „m”** (`um_de_normalizat(_m, _f3)`, pe care `v_ofertare_pt_stare.qm` nu le adună), cu metri; lista: 📋 Cantități → „N nevalidate” | fără tip ⇒ `ok` devine **WARN „INCOMPLET, de reverificat”**; view vechi ⇒ „control parțial” (warn) | invalidate ieșite / TOTAL invalidate / unitate schimbată / unitate de normalizat în F3 ⇒ **BLOCK** (în afara F3 ⇒ WARN) | `ofertareControale.test.js` „H2 R5 condiția 2” + „H2 runda 6”, PGlite `test_view_conditia2.mjs` |
| Cardul din fișă (`OfertareLicitatii` l.≈3353) | citește doar `v_ofertare_pt_stare` | — | H2 = „nu putem verifica” = block (neschimbat, §1) | block | — |
| `ofertare_clarificare_planse_auto` v6 (propusă) | totalul F3 doar din validate | cât există F3 nevalidată (inclusiv invalidată / cu unitatea schimbată, runda 6) sau rânduri F3 cu unitatea ≠ exact „m”, textul **nu dă total** (cere corespondența) — nu apare un subtotal parțial, nici unul umflat | — | — | PGlite `test_r5_runda4.mjs` |
| Generatorul de clarificări (`core.ts`) | nu (toate rândurile cu notă, cu `status_validat`) | limita de 40 **nu mai taie tacit**: „40 din N — LISTA E TRUNCHIATĂ …” în prompt | — | — | `core_test.ts` (+1) |
| Transferul planșă → cantități | nu e consumator de „validat” | `ambigue` / `doar_de_verificat` erau doar în JSON ⇒ acum **insignă „⚠ transfer: N nescrise, M de verificat”** pe document (lista în tooltip) + text în mesajul de după citire (`src/ofertareTransferRaport.js`) — task-ul semnalat de verificator | — | — | `ofertareTransferRaport.test.js` |
| 📋 Cantități | arată tot | contor „N nevalidate” devine **filtru** (click) — lista la care trimit poarta graficului și H2 | — | — | build |
| Poarta propunerii, rândul „grafic” (runda 5) | versiunea înghețată | fronturile versiunii înghețate reverificate față de cantitățile de ACUM (`reverificareGraficInghetat` = `controlFronturiGrafic`): rând-sursă invalidat după îngheț, rânduri necesare nevalidate | **WARN „DE REVERIFICAT față de cantitățile de acum (rezultat incomplet)”**; reverificare eșuată = WARN „control indisponibil” | warn (nu block) | `ofertareCantitatiAprobare.test.js` „minorul 5” |
| Generatorul PT, pachetul, verificarea finală | indirect (grafic / semnătura porții) | prin poarta graficului, H2 și (runda 5) rândul „grafic” al porții propunerii | — | limită: poarta graficului blochează doar când e redeschisă; după îngheț, semnalul ajunge la propunere ca WARN (rândul „grafic”), iar totalurile rămân legate de H2 (F3 = fronturi). Verificarea finală și pachetul nu recalculează nimic din cantități | — |
| Calcul financiar / devize / F3 export / RFQ | nu citesc tabela (§1, rândurile 14–15) | — | — | — | — |

Preview-ul view-ului extins (corpul rulat ca SELECT pe producție, 26.09): lic. 3 — 3 rânduri de rețea nevalidate (23.630 m; memoriu), 0 invalidate ieșite; lic. 5 — 47 F3 nevalidate (6.520 m), 94 / 94 de rețea nevalidate (28.862 m); lic. 95 — **6 fără tip, 48.195 m**; lic. 102 — **2 fără tip, 167.200 m**; nicăieri rânduri invalidate ieșite (regula nu rulează încă în BD).

## 10. Runda 5 (verificatorul condițiilor 1–2, 26.09.2026)

**MAJOR 1 — starea „invalidat” depindea de textul notei.** Lanțul reprodus: rând validat → „m” → „ml” (invalidat, prefix în notă) → o recitire a planșei (sau CAD) rescrie nota oricărui rând nevalidat → prefixul dispare → rândul „ml” nu mai e nici în rețea, nici „invalidat” → poarta „cant” verde, `invalidate_in_afara_retea` = 0, fronturile / F3 / H2 consecvente și verzi fără metrii lui. Reparat pe două căi, independente:
- **Istoricul (sursa principală, în BD):** un rând e invalidat dacă ultimul lui eveniment din `ofertare_cantitati_istoric` e `invalidat` / `redeschis` (fără `validat` după el). Folosit de `v_ofertare_cantitati_nevalidate` (+ v6) și, în UI, de `marcheazaInvalidate` (GraficPoarta la încărcare și la îngheț, poarta propunerii). Istoricul se citește tolerant: tabel lipsă = doar sursa a doua.
- **Prefixul păstrat (până la aplicarea migrării):** `pastreazaInvalidarea` — transferul (plasa finală `plasaAprobare`) și CAD pun prefixul vechi („Rândul era VALIDAT … validarea se reface.”) în fața notei rescrise a unui rând nevalidat.
- Varianta „ml intră în rețea” n-am ales-o: ar fi acoperit doar unitatea, nu și categoria / „total” în obiect, și ar fi schimbat `v_ofertare_pt_stare.qm` (view live).

**MAJOR 2 — pragul de 1 m se ocolea prin pași mici.** Regula compara cu valoarea de dinainte. Acum compară cu **valoarea APROBATĂ**: trigger-ul înregistrează fiecare validare (`motiv 'validat'`, rândul întreg în `valori_noi`); referința = ultima validare, altfel rândul dinaintea primei scrieri înregistrate (rândurile validate înainte de migrare, ex. 2, 3, 4, 9 ale lic. 3), altfel rândul curent. Aceeași regulă în JS (`referinteDinIstoric` + al treilea parametru `referinta` al `aplicaRegulaAprobare` / `schimbariRelevante`), citită din istoric de editor (`saveC`), transfer (`referinteAprobare`, doar pentru rândurile validate) și CAD. Nota numește valoarea aprobată („aprobarea veche (cantitate 5.250 m …) … cantitatea (5.250 m → 5.251,98 m)”); nota SQL = nota JS octet cu octet, inclusiv cu referința citită din istoricul scris de trigger (PGlite). Repro-ul verificatorului (rândul 3, 5 × +0,99) iese acum din „validat” la pasul 2. Limită: până la aplicarea migrării, aplicația n-are unde păstra referința (rămâne comparația cu rândul de acum).

**Minore:**
- **`um` vs filtrul rețelei** (*înlocuit în runda 6, §11.3: unitatea normalizată*): `um` se compară exact (orice schimbare e relevantă), aceeași definiție ca `qm` / view / v6 (`um = 'm'`). Repro A5 („m” → „M” pe un F3 validat) ⇒ invalidat, numărat în `invalidate_in_afara_retea` (700 m), H2 block. v6 numără și rândurile F3 invalidate ieșite din `qm` ⇒ fără total citat (nu mai omite tacit rândul).
- **TRUNCATE pe istoric:** `REVOKE ALL … FROM PUBLIC, anon, authenticated` (tabel + secvență) înainte de `GRANT SELECT`; la fel pe view. Testul PGlite emulează `ALTER DEFAULT PRIVILEGES` ca în Supabase: authenticated are doar SELECT; TRUNCATE refuzat (pe migrarea veche: permis).
- **Poarta propunerii după îngheț:** rândul „grafic” rulează `controlFronturiGrafic` pe fronturile versiunii înghețate față de cantitățile de acum ⇒ WARN „de reverificat” (§9). Formularea „prin ele” din §9 a fost corectată (limita e scrisă explicit).
- **Plasa finală a transferului** e acum `plasaAprobare` (exportată) și e acoperită: ramură simulată (specificații schimbate fără status ⇒ `diferenta` + prefix), capăt la capăt (cifra aprobată 1.000 → 1.000,9 sub prag → recitire 1.001,5 ⇒ `diferenta` DOAR prin plasă), fără istoric ⇒ comportamentul vechi, fără excepții. Mutațiile pe plasă pică 1–2 teste.
- **NBSP:** `public.ofertare_norm_text` (SQL) = `normText` (JS, exportat): aceeași listă de spații Unicode → spațiu, spațiile ASCII comasate, trim, lower. Paritate pe 9 texte; NBSP în denumire = sub prag în ambele.
- **Ramurile „doar de verificat” / MY-T4 / „total cu Dn”** pe un rând VALIDAT: neschimbate în runda 5; **decise în runda 6** (varianta B, §11.2).

**Fișiere:** `src/ofertareCantitatiInvalidare.js` (+ cele 2 copii identice), `src/ofertareCantitatiAprobare.js`, `src/ofertarePoarta.js`, `src/OfertarePropunere.jsx`, `src/GraficPoarta.jsx`, `src/OfertareCantitati.jsx`, `api/_cadCantitate.js`, `api/cad-parse.js`, `supabase/functions/ofertare-plansa-citeste/handler.ts`; migrările `docs/R5_MIGRARE_PROPUSA_aprobare_istoric.sql` (+ `_ROLLBACK.sql`) și `docs/R5_MIGRARE_PROPUSA_cantitati_nevalidate.sql` — tot **neaplicate**. BD: doar SELECT (26.09: `grafic_versiuni.snapshot` jsonb există; `ofertare_cantitati_istoric` nu există încă).

## 11. Runda finală (6, 26.09.2026) — decizii luate în audit pe principiile Copilot (reversibile)

Principiul: **niciodată pierdere sau umflare TĂCUTĂ; identitate ambiguă = conflict vizibil; ce nu e sigur = „de verificat”.** Fiecare decizie de mai jos e luată în audit, fără să aștepte, și e **reversibilă** (de confirmat sau răsturnat de Razvan, §6.10). Codul: `10e107d`; migrările rămân **neaplicate**.

### 11.1 Marcajul pasului B
Codul recunoaște marcajul din SQL-ul documentat („ | R5 pas B”) și pe cel vechi („ | R5 v2 pas B”); eticheta numește **cantitatea** drept valoarea verificată de om (§8.6). Teste: `cantitati_nevalidate_test.ts` — 3 scenarii × 2 marcaje + compatibilitatea cu RB + controlul fără marcaj.

### 11.2 Varianta B pe ramurile „doar de verificat”, MY-T4 și „total cu Dn”
Un rând VALIDAT căruia citirea nu-i dă nicio cifră sigură (deci nu-i confirmă cifra) iese din „validat”: `diferenta`, nota începe cu „Rândul era VALIDAT cu … ; planșa … {nu dă nicio cifră sigură pentru el (doar rânduri de verificat) | are pe DnX rânduri fără material care pot fi ale lui, iar grupul sigur de pe Dn nu i-a fost atribuit | nu-i atribuie nicio cifră (rând de total cu Dn, neatribuit)}, fără să confirme cifra — validarea se reface.”; cifra rămâne neatinsă; în BD, trigger-ul scrie „redeschis” în istoric (valoarea și aprobarea veche păstrate). Consecvent cu coliziunea (§8.5). Include rândul TOTAL validat fără nicio lungime sigură pe planșă. 7 teste R4 / R5 actualizate (TOTAL fără cifre sigure, MY-T4 ×2, NFL-DN, „total cu Dn” ×3). **Limită păstrată**: rândul care primește o cifră sigură și, pe lângă ea, un rest de verificat (ramura sigură, sub-ramurile „deja”) rămâne validat cu restul numit în notă — cifra lui e confirmată de un grup sigur.

### 11.3 Unitatea normalizată + „unitate schimbată”
- `normUm` = `normText` (trim, lower, spații Unicode; SQL: `ofertare_norm_text`, acum cu `GRANT EXECUTE` explicit pentru authenticated / service_role) în **regula de invalidare** (JS + trigger), în **`randuriFront` / `randuriLipsa`** și în **view / v6**: „m” → „M” / „m ” = sub prag (validarea rămâne, istoric `modificat_sub_prag`), „m” → „ml” = invalidare.
- **Rândul neaprobat** care își schimbă unitatea din / în „m” nu mai iese tacit din semnalul de lipsă (minorul 1 al verificatorului, repro `adv_r5r5.mjs`): trigger-ul scrie `unitate_schimbata` în istoric (rândul neatins; nu e referință de aprobare); până la migrare, editorul pune prefixul „Unitatea s-a schimbat („m” → „ml”) — rândul nu mai e o lungime de rețea în metri; de reverificat.”, păstrat de transfer și CAD ca prefixul invalidării. Semnal: `randuriLipsa` („unitate schimbată, ieșit din rețea”, poarta graficului BLOCK), view (`unitate_schimbata_in_afara_retea(_m)`, H2 BLOCK), v6 (numărat ⇒ fără total). Validarea (✓) îl închide. „Invalidat” din istoric ignoră `unitate_schimbata` (un rând invalidat căruia i se schimbă apoi unitatea rămâne invalidat).
- **`v_ofertare_pt_stare.qm` (live) nu e atins** (e un view mare, folosit de toată poarta; schimbarea lui cere o migrare separată): cere încă exact `um = 'm'`. Rândurile de rețea care intră în setul normalizat doar prin normalizare sunt **semnalate** (`um_de_normalizat(_m, _f3)`; H2 BLOCK dacă sunt în F3, altfel WARN), iar v6 **nu citează total** cât există rânduri F3 cu unitatea ≠ exact „m” (nici umflare — articolele de deviz „M” —, nici omisiune).
- **BD (SELECT, 26.09):** singurele rânduri afectate sunt cele 15 F3 „M” ale lic. 5 (1.227,89 m, `extras`, articole de deviz în „Conducte și montaj”) — §6.12.

### 11.4 Rândurile TOTAL invalidate — același tratament în JS și în view
Varianta care nu ascunde nimic: **listate și numărate, separat, fără să se adune**. `randuriLipsa` le listează cu motivul „invalidat, rând TOTAL (referință, nu se adună)” (`referinta: true`, în afara sumelor pe unitate); view-ul le numără în `total_invalidate(_m)`, iar `invalidate_in_afara_retea` e acum fără TOTAL (aceeași definiție în ambele: „total” în obiect / denumire / sursă, ca `qm`). Poarta graficului și H2: BLOCK. Teste: vitest „runda 6”, PGlite `test_view_conditia2.mjs` și `adv_r6.mjs` (paritatea view ↔ JS).

### 11.5 Istoricul citit descrescător și paginat
`citestePaginat` (în modulul regulii, copiile identice): `.order('id', { ascending: false }).range(a, b)`, avansează cu câte rânduri au venit (un plafon `db-max-rows` mai mic decât pagina nu sare rânduri), se oprește la pagina goală; peste 100.000 de evenimente întoarce eroare („istoric trunchiat”), nu o listă tăiată. Folosit în GraficPoarta (încărcare + îngheț), poarta propunerii (rândul „grafic”), editor (referința), CAD și transfer (`referinteAprobare`, pe loturi de 200 de id-uri). Cea mai recentă aprobare vine prima și nu se poate pierde; testul cu 2.502 evenimente și plafoane 1.000 / 7 o găsește, citirea veche (crescătoare, tăiată la 1.000) lua validarea veche. Tot aici: cantitățile din GraficPoarta și din poarta propunerii se citesc și ele paginat (în locul `limit(20000)` supus plafonului).

### 11.6 Cifrele de teste
§5.4 = cifrele reale ale capului `10e107d`; cifra „164/164” din antetul §2 era a lui `5ab80c1` (reverificat pe copie: 164 la `5ab80c1`, 168 la `6313184`) — corectată.

### 11.7 Ce rămâne
- GO pentru migrări (ordinea: istoricul, apoi view-ul + v6), push / PR / merge, deploy `ofertare-plansa-citeste` și `ofertare-clarificari-propune` — neatinse aici (regulile sarcinii).
- `v_ofertare_pt_stare.qm` cu unitatea normalizată: migrare separată, de decis (până atunci diferența e semnalată).
- JS `randuriFront` și `qm` diferă în continuare pe categorii (titlurile de secțiune, „total” doar în obiect la fronturi) — preexistent, documentat.
- Rândul care iese din rețea prin **categorie** sau **„total” adăugat în obiect** (fără schimbare de unitate), niciodată validat, iese încă fără semnal dedicat (minorul 1 al verificatorului, partea non-unitate); cere editare explicită.

## 12. Runda 1b — pragul exact (Copilot, închiderea R4/R5, 26.09.2026, condiția nr. 1)

Copilot: „aprobat 100 m, citire nouă 100,8 m → e ALTĂ cantitate. La bucăți / suprafețe / volume pragul «1 m» nici nu se aplică. Comparație EXACTĂ a valorii canonice aprobate (excepție doar diferențe pur reprezentative: 100 vs 100,000). Pragurile decid SEVERITATEA avertismentului, nu transferul aprobării asupra altei valori. Nu amesteci rolurile.”

### 12.1 Starea BD (doar SELECT, 26.09.2026)
- Migrarea 1 e **APLICATĂ** (`r5_aprobare_istoric`, 26.09 09:08 UTC). Definiția aplicată a `fn_trg_ofertare_cantitati_aprobare` (pg_get_functiondef, `md5(prosrc)` = `df25b1ba4f392f10789f6f4e18e8eaaf`) comparată cu `docs/R5_MIGRARE_PROPUSA_aprobare_istoric.sql`: **logica e identică** (fără comentarii și rânduri goale: md5 `9df9c135…` în ambele); `ofertare_cantitati_atribute` / `ofertare_fmt_ro` / `ofertare_norm_text` — identice octet cu octet. **Diferențe de formă:** textul aplicat = fișierul fără comentariile „--” și fără rânduri goale (md5 `c2839216…`, 14.749 caractere, reprodus exact), și **fără ultima instrucțiune** `COMMENT ON FUNCTION …fn_trg_ofertare_cantitati_aprobare()` — funcția n-are comentariu în producție. (Trigger-ele, CHECK-ul, drepturile: ca în fișier.)
- `ofertare_cantitati_istoric`: 0 rânduri. Validate: 2, 3, 4, 9 (lic. 3). Statusuri: extras 1.080 / validat 4 / diferenta 2. `cantitate` / `cantitate_plansa` = `numeric` fără scală fixă; scala maximă din date: 5 / 2; niciun rând cu peste 6 zecimale.

### 12.2 Regula (o singură sursă: `src/ofertareCantitatiInvalidare.js` + trigger-ul 1b, verificate împreună)
- **Cifrele** (cantitatea; cifra din planșă efectivă): comparație EXACTĂ a valorii canonice — SQL `round(x, 6)`, JS `valoareCanonica` (BigInt, pornind de la zecimalul cel mai scurt al numărului, adică exact ce pleacă în JSON și ajunge `numeric`; fără virgulă mobilă: 0,1 + 0,2 = 0,3). `aceeasiValoare` / `cifraDiferita` le folosesc toate căile. Pe **orice unitate**; **nicio conversie m ↔ ml** (unitatea rămâne comparată normalizat: „m” → „M” = formă, „m” → „ml” = invalidare).
- **Severitatea** (doar text, `descrieDiferenta` / blocul din trigger): „diferență mică: +0,8 m, +0,8 %” = sub 1 % relativ ȘI, pe unitățile de lungime (m / ml / fără unitate; cifra din planșă e mereu lungime), sub 1 m; altfel „diferență mare: …”. Pe buc / mp / mc / kg / to / ore … **doar pragul relativ** (niciun prag în metri). |Δ| exact (max. 6 zecimale: „+0,001 mc”), procentul la 2 zecimale („sub 0,01 %”); față de 0 fără procent (= mare); unități normalizate diferite (m → buc) = fără severitate (schimbarea de unitate e numită separat). Pe cifra din planșă comparată efectiv cu cantitatea: „…, efectiv 1.100 m → 1.100,3 m”. Cifrele din notă poartă unitatea lor (cantitatea — a rândului: „10 buc → 11 buc”, înainte „10 m → 11 m”).
- `modificat_sub_prag` rămâne numele motivului (e în CHECK și în view-ul propus), dar înseamnă acum **„doar formă”**: texte cu majuscule / spații, „m” → „M”, cifre egale canonic (zgomot sub a 6-a zecimală; 100 → 100,000 nici nu e scriere distinctă). Nicio cifră cu altă valoare nu mai ajunge acolo.
- **Roluri separate:** `cantitate` = valoarea aprobată / folosită; `cantitate_plansa` = observația-candidat a citirii. Transferul și CAD nu scriu `cantitate` pe un rând validat; o observație diferită îl scoate pe `diferenta`; valoarea aprobată rămâne în rând, e numită în notă (prefixul „Rândul era VALIDAT …”) și e în istoric (`aprobare_veche`, `valori_vechi` / `valori_aprobate` cu `sursa` / `tip_sursa`).

### 12.3 Căile aliniate (cod pe ramură)
- **Trigger** — `docs/R5_MIGRARE_1b_prag_exact.sql`: `CREATE OR REPLACE` doar al funcției (OID, proprietar, drepturi, SECURITY DEFINER, search_path neschimbate; trigger-ele nu se recreează; niciun rând nu se modifică); + comentariul funcției. Rollback: `_ROLLBACK.sql` = corpul din producție **octet cu octet** (md5 verificat) + `COMMENT … IS NULL`.
- **Editorul 📋 Cantități** (`saveC` → `aplicaRegulaAprobare`): confirmarea arată severitatea.
- **Transferul din planșă** (`handler.ts`): `cifraSchimbata` exact; pasul B „recitirea o confirmă” = aceeași valoare (13.739,6 nu mai confirmă 13.740); „Planșa confirmă” (memoriu / TOTAL) = aceeași valoare; „extras → diferenta” la orice diferență; pe rândul VALIDAT, comparația și nota **față de valoarea aprobată din istoric** (`refs`, ca trigger-ul; fără istoric — rândul de acum), prefixul cu severitate („… dă acum 13.140,4 m (diferență mică: +0,4 m, sub 0,01 %) — validarea se reface.”); cifra de referință afișată cu 2 zecimale (`fmtRo`) — pe 1 zecimală, 35.620,59 → 35.620,6 ar fi apărut „35.620,6 → 35.620,6”.
- **Citirea CAD** (`api/_cadCantitate.js`): la fel, cu referința aprobată pe rândul validat; `fmt` = `fmtRo`.
- **Poarta graficului** (`controlFronturiGrafic`): frontul față de cifra rândului-sursă — orice altă valoare cere repropunere (înainte ≥ 1 m).
- **`fmtRo`** (JS): rotunjire zecimală exactă = `ofertare_fmt_ro` (înainte `toFixed` + `toLocaleString`: 1,085 → „1,08” în JS, „1,09” în SQL — notele puteau diferi SQL / JS pe astfel de valori; și depindea de ICU-ul browserului).
- **Neschimbate:** tabelul, CHECK-ul, funcțiile ajutătoare, migrarea 2 (view + v6; testată cu 1b, compatibilă); `docs/R5_LIC3_VALIDARI_INAINTE_DE_TRANSFER_PROPUS.sql` păstrează `abs(cp − cant) ≥ 1` pe id-urile fixe 2, 3, 4 (același rezultat ca exact).

### 12.4 Compatibilitate cu codul publicat și ordinea
1b poate fi aplicată **înainte sau după** deploy-ul codului — ambele combinații sunt sigure (mai strict, niciodată mai permisiv):
- **1b + codul publicat (main):** trigger-ul scoate singur din „validat” orice cifră schimbată; codul publicat nu citește istoricul și nu parsează nota regulii; nicio coloană / semnătură nouă.
- **Codul nou + BD fără 1b:** aplicația pune singură `diferenta` (patch cu status) — trigger-ul de azi scrie `redeschis` (nu `invalidat`) pentru diferențele sub 1 m; nota aplicației rămâne. Paritatea notelor SQL = JS cere 1b (fuzz-ul `adv_r6`: fără 1b 148 / 600 dezacorduri, cu 1b 0).

### 12.5 Teste (26.09.2026, pe worktree)
- **PGlite `scratchpad/pglite/test_1b_prag_exact.mjs` — 66/66:** 37 de rânduri reale (lic. 3 + lic. 95 + 22 de eșantion, câte unul pe unitate), fiecare verificat md5 pe textul jsonb față de producție; migrarea 1 reconstruită = textul aplicat (md5) și funcția = producția (md5 prosrc); repro „azi”: 2.210 → 2.210,8 rămâne validat; aplicarea 1b: 0 rânduri schimbate, istoric gol, trigger-e / drepturi / OID neschimbate, idempotentă; **setul comun** `src/ofertareCantitati1b.cazuri.js` (35 de cazuri, rulat și de vitest): SQL = JS (status, motiv, notă octet cu octet) — 2.210 → 2.210,8 invalidat (mică), 2.210 → „2210.000” nimic, buc 16 → 17 (mare, +6,25 %), BUCATA 76 → 76,5 (mică: sub 1 unitate, dar invalidat), mc 1,084 → 1,085 (+0,001 mc), %, NULL, „M”, „sute m”, ore, „M CUB” la a 7-a zecimală, m → buc (fără severitate), ↩, rând nevalidat; istoricul păstrează valoarea aprobată + sursa, rândul își păstrează cantitatea; transferul (patch cu status + notă cu severitate); pașii mici — primul pas invalidează, revalidarea = referință nouă; **lot aleator 400/400** SQL = JS (0–7 zecimale, 0, negative, 12 unități); `fmtRo` = `ofertare_fmt_ro` pe 67 de valori; `valoareCanonica` = `round(x, 6)`; rollback: md5(prosrc) = producția, fără comentariu, datele neatinse, regula veche revine; reaplicare.
- **Controale negative:** fără 1b (trigger-ul de azi + JS nou) pică 35 de verificări din 66; JS-ul vechi (`a15a62a`) + 1b pică 29; **10 mutanți** ai SQL-ului 1b (pragurile `<` → `<=`, `round(…, 5)`, toleranța înapoi, procent trunchiat, unitatea fixă „m”, formatul lui Δ, sufixul „efectiv”, garda cifrei din planșă, unitatea veche) — toți prinși; 3 mutanți ai transferului (confirmarea pasului B, „confirmă” pe memoriu, referința aprobată) — toți prinși de deno.
- **Suitele anterioare cu 1b aplicat** (variantele `_1b` din `scratchpad/pglite`): `test_aprobare_istoric` 66/66 (cele 7 așteptări „sub prag” înlocuite cu regula nouă), `test_view_conditia2` 18/18, `test_r5_runda4` 16/16 (migrarea 2 + v6 compatibile cu 1b), `adv_r6` 13/13 (fuzz 600 de pași: 0 dezacorduri SQL / JS; invariantul „rândul rămas validat are EXACT valoarea aprobată”).
- **vitest 537/537** (față de 494: testele „sub prag” adaptate + 43 noi), **deno 194/194** (față de 192), `vite build` OK, `deno check` OK; `deno.lock` neatins.

### 12.6 Rămâne / de decis (Razvan)
1. **GO pentru 1b** (`apply_migration`, nume propus `r5_1b_prag_exact`), cu preview / sanity din antetul fișierului; independent de merge (§12.4).
2. **Decizii luate pe principiul Copilot, reversibile:** (a) valoarea canonică = 6 zecimale (datele au max. 5; sub a 6-a zecimală = zgomot de reprezentare); (b) pragurile severității: 1 m (lungimi) și 1 % (relativ), stricte; (c) „Planșa confirmă” și „extras → diferenta” la recitire sunt acum tot exacte — o recitire cu 500,4 față de memoriul 500 trece rândul pe „diferenta” (înainte „confirmă”); (d) pe rândul validat, transferul / CAD numesc în notă valoarea aprobată din istoric.
3. Cantitățile Vâlcelele / lic. 95 — validarea de conținut rămâne deschisă (verdictul Copilot pct. 4); 1b nu schimbă niciun rând existent.

## 13. Sarcina 2 — condiția Copilot nr. 2 și observațiile (închiderea R4/R5, 26.09.2026)

Verdictul (pct. 2): „Conflictul NU produce rând în ofertare_cantitati, dar există cantități validate → view-ul / poarta trebuie să vadă incompletitudinea SURSEI chiar dacă nr. rândurilor nevalidate e zero. «Nimic scris» ≠ «nicio problemă».” + testul „aprobat, folosit în grafic / PT, apoi modificat” + view indisponibil ⇒ „nu putem verifica” + unitățile + „de reverificat” ≠ „aprobarea era greșită” + v6 + compatibilitatea. Codul e pe ramură; migrarea 2 e extinsă, **neaplicată**; pe BD doar SELECT.

### 13.1 Starea BD (doar SELECT, 26.09.2026)
- Rezultatul transferului stătea doar în **jurnalul scriitorului**: `analiza.citire_ai.sumar.cantitati` (`ambigue`, `doar_de_verificat`, `amanat` / `eroare`) + lease-ul `citire_ai.transfer`. Niciun consumator de poartă nu le citea: un conflict fără rând era invizibil dacă rândurile existente erau validate.
- 8 documente cu jurnal (130, 470–475, 1035), toate scrise de edge v25 (= `main`, byte cu byte — md5 pe `index.ts`, `handler.ts`, `concurenta.ts`, `poarta.ts`): `ambigue: []`, fără `doar_de_verificat`. Derivarea (aceleași reguli ca mai jos) dă conflicte deschise **doar pe doc 470 (lic. 95): 2** — Dn60 nestandard 110 m și 3 adnotări pe Dn absent (1.770 m).
- **Unde se persistă:** `analiza.plansa` e înlocuită întreagă la retăiere (`api/plansa-felii.js`, `{ ...a, plansa: plansaNoua }`), iar `citire_ai.transfer` e rescris de lease-ul transferului următor ⇒ înregistrarea stă la nivelul 1: **`analiza.transfer_cantitati`**. Toate scrierile lui `analiza` (handler, plansa-felii, RPC-ul de transfer, `ofertare-document-nou-citeste`) fac `{ ...analiza, … }` / `jsonb_set` pe `citire_ai` ⇒ cheia se păstrează (verificat în codul publicat și pe PGlite). Singura suprascriere întreagă e `api/cad-parse.js`, doar pe documentele DXF (nu au transfer din planșă). Fără tabel / coloană nouă.
- Politici pe `ofertare_documente_atribuire`: SELECT `auth.uid() IS NOT NULL`, UPDATE `fn_are_acces_ofertare()`. Modelul reluat pentru confirmare: `fn_ofertare_doc_bifa_relevanta` (SECURITY DEFINER + verificarea rolului în corp, notă ≥ 5 caractere).
- Clarificări: singura ciornă `auto_planse` e #63 (lic. 95, `de_trimis`, text editat de om, sursa = planșele 475..471). Cron-urile atinse de clarificări (`ofertare_clarificari_reminder`, rapoartele Ofertare) scriu doar notificări interne; niciun trigger de transmitere pe `ofertare_clarificari` / `notifications` (doar `fn_notificari_ruteaza_ofertare`, care pune `modul`).

### 13.2 (a) Conflictele fără rând — persistate, văzute, închise explicit
- **Ce e conflict** (`conflicteTransfer`, `handler.ts`): grupuri ambigue (inclusiv coliziunea și poziția altei planșe); Dn-uri doar „de verificat” (fără dublura `ambiguu`); **mai multe rânduri TOTAL** (nou în rezultat: `totaluri_multiple` — scrierea nu se schimbă, decizia lic. 5 A/B/C rămâne la Razvan); identitatea (fără identitate / conflicte de citire); secvența Nr incompletă (max. 6 + „încă N”); Nr fără lungime; tronsoane sigure fără Dn; Dn nestandard; adnotări pe Dn absent; transferul **amânat** (zone căzute / lipsă) sau **căzut** = „neefectuat”. Ce n-are metri e marcat `fara_cantitate` (nu „0 m”). Neinclus: `necorelare_unitate` (scrisă după transfer, de pasul notelor; rămâne ⚠ în text).
- **Înregistrarea** (`transfer_conflicte.ts`): `{ v, id, la, cod, cod_transfer, rulare, citire, plansa, stare, n, conflicte (≤ 40, + trunchiat), confirmat_de / confirmat_la / confirmare_nota, inchis_prin?, inchide?, anterior, istoric (≤ 10) }`. Scrisă la eliberarea lease-ului, **în aceeași scriere CAS** cu jurnalul (`sumar.cantitati.inregistrare_id = id`), și la transferul amânat, în scrierea citirii. Reconstruită peste documentul proaspăt la fiecare reîncercare CAS.
- **Închiderea — niciodată ștergere tăcută:** (1) recitire fără conflicte ⇒ `fara_conflicte`, `inchis_prin: 'recitire_fara_conflicte'`, precedenta în `anterior` (cu conflictele) + `istoric`; (2) confirmare umană ⇒ **`ofertare_transfer_conflicte_confirma(doc, token, notă)`**: acces Ofertare, notă ≥ 5 caractere, token = ce a văzut omul (alt token ⇒ refuz), autor = `auth.uid()` (nu o valoare de la client), conflictele rămân (doar confirmate), `citire_ai.rev` nou (orice CAS concurent — citire, retăiere, rezervări — recitește, nu suprascrie). O recitire CU conflicte după confirmare ⇒ înregistrare nouă, deschisă (confirmarea veche nu o acoperă, rămâne în istoric). Transferul căzut / amânat nu șterge conflictele vechi (e el însuși deschis, ele stau în `anterior`).
- **SQL (migrarea 2):** `ofertare_transfer_stare(jsonb)` — o singură definiție: înregistrarea legată de jurnal (sau fără jurnal: citire pe runde) / **legacy** = jurnalul codului vechi, numărat cu aceleași reguli (**paritate JS ↔ SQL 327 / 327**) / transfer în curs (< 10 min: starea precedentă; peste: „neefectuat”) / formă necunoscută = **deschis**. `v_ofertare_transfer_conflicte` (per document, `security_invoker`) și, în `v_ofertare_cantitati_nevalidate`, `transfer_conflicte_docs / _n / _lista / transfer_in_curs` — cu **FULL JOIN**: licitația apare și fără niciun rând de cantități.
- **Consumatorii:** poarta graficului — „cant” **BLOCK** „sursă incompletă: N conflicte nerezolvate la transferul din K planșe (…)” chiar cu toate rândurile validate; „front” INCOMPLET (nu `ok`); „Propune din cantități” numește sursa incompletă; se recitește la îngheț. **H2** — WARN „INCOMPLET, de reverificat — sursă incompletă …” (planșele sunt surse informative, F3 rămâne referința — ca rândurile fără tip). **Generatorul de clarificări** (`core.ts`) — secțiune „CONFLICTE NEREZOLVATE LA TRANSFERUL DIN PLANȘE”. **v6** — `transfer_conflicte_deschise` în rezultat. **Documente** — „⚠ transfer: N conflicte nerezolvate” + „✋ confirmă conflictele” (notă obligatorie; lista în prompt), „✓ conflicte transfer confirmate”.
- **„De reverificat”, nu „aprobarea era greșită” (varianta B din runda 7 verificată și ajustată):** prefixul citirilor automate pe un rând validat (transferul — coliziune, doar „de verificat”, MY-T4, „total cu Dn”, cifră schimbată, TOTAL, pas B; CAD) spunea doar „… — validarea se reface.” Acum: „… — **de reverificat: citirea automată nu infirmă aprobarea (valoarea și sursa aprobate rămân în rând și în istoric)**; validarea se reface.” (`DE_REVERIFICAT_CITIRE`, în modulul regulii și copiile identice; terminatorul rămâne ultimul — `prefixInvalidare`, view-ul și v6 îl recunosc). Transferul / CAD nu scriu niciodată `cantitate`, `sursa`, `tip_sursa`; istoricul are `aprobare_veche` / `valori_vechi`; un TOTAL ambiguu nu înlocuiește nimic (nescris, raportat). Nota regulii pentru o modificare REALĂ (editor / trigger: „aprobarea veche … nu mai e valabilă”) e neschimbată — paritatea SQL = JS a rămas octet cu octet.

### 13.3 (b) „Aprobat, folosit în grafic / PT, apoi modificat” (PGlite, rândurile reale lic. 3)
Fronturile propuse din rândurile validate 2 (Dn180, 1.100) și 3 (Dn160, 5.250) → versiunea 1 înghețată în `grafic_versiuni` + PT semnat (`ofertare_pt_poarta`). Apoi, ca utilizator autentificat:
- **cifra** 5.250 → 5.260 pe rândul 3: trigger-ul (1 + 1b) = regula JS pe același rând + referința din istoric — status `diferenta`, nota octet cu octet, istoric `invalidat` cu valoarea aprobată 5.250 și sursa ei, autorul; rândul rămâne în tabel;
- **atributul** Dn 180 → 160 (aceeași lungime) pe rândul 2: la fel, „Dn 180 → 160” în notă;
- consumatorii pe aceleași date: „cant” BLOCK cu rândul 3 listat (cifra de acum, nu eliminat); „front” (fronturile înghețate) BLOCK „#3 nu e validat”, INCOMPLET — subtotalul rămas nu e prezentat drept complet; reverificarea versiunii înghețate ⇒ rândul „grafic” al porții propunerii / PT = **„DE REVERIFICAT față de cantitățile de acum (rezultat incomplet)”**; view-ul (H2) numără rândul la memoriu / rețea nevalidate; versiunea și semnătura PT **nu se șterg**;
- revalidarea pe 5.260: frontul înghețat (5.250) cere tot repropunere — rezultatul dependent nu redevine verde tăcut.

### 13.4 (c) View / istoric indisponibil ⇒ „nu putem verifica”
- **H2**: `campuriCantitatiNevalidate` cu eroare ⇒ `cantitati_nevalidate_indisponibil` ⇒ BLOCK „nu putem verifica cantitățile nevalidate și sursa (planșele): … — nu înseamnă zero restanțe”, **și fără F3** (înainte: doar „lipsește F3”).
- **Poarta graficului**: `sursa` e obligatorie în `calculeazaPoartaGrafic` (necitită = BLOCK); eroarea la `v_ofertare_transfer_conflicte` sau la istoric ⇒ „cant” BLOCK cu motivul — înainte istoricul eșuat era `[]` tăcut (rândurile invalidate ieșeau din lipsă). La îngheț se recitesc cantitățile, istoricul și conflictele.
- **Poarta propunerii** (rândul „grafic”): istoricul eșuat ⇒ `grafic_reverificare_eroare` ⇒ WARN „control indisponibil” (înainte `[]`).
- **Editorul** (📋 Cantități): istoricul eșuat pe un rând validat ⇒ confirmare explicită înainte de salvare (în BD trigger-ul compară oricum cu valoarea aprobată).
- **Documente**: view-ul lipsă ⇒ „⚠ transfer: nu putem verifica conflictele”. **Generatorul de clarificări**: „NU AU PUTUT FI CITITE … nu presupune că nu există”.
- Scriitorii (transfer, CAD) păstrează fallback-ul pe rândul de acum: decizia finală o ia trigger-ul (1b) față de istoric.
- PGlite: erorile REALE — „relation "v_ofertare_cantitati_nevalidate" does not exist” (înainte de migrarea 2) și „permission denied for table ofertare_cantitati_istoric” — trecute prin consumatori ⇒ BLOCK.

### 13.5 (d) Unitățile
- JS (`ofertareCantitatiAprobare.js`): `clasaUnitate` / `umAfisata` / `grupeDinRanduri` / `grupeDinView` / `textCantitatiPeUnitati` — lungimi (m, ml, km, sute m), suprafețe (mp, ha, sute mp), volume (mc, l, sute mc), bucăți, alte unități, fără unitate; unitățile rămân **separate** în clasă (fără m ↔ ml: „1.000 m + 300 ml”); doar sinonimele exacte se unesc (bucata = buc, m cub = mc, m2 = mp, m.l. = ml); rândurile fără cantitate = „N poziții fără cantitate determinată”. Folosit de `randuriLipsa` / `textLipsa` (poarta graficului, „Propune”, „front”, reverificarea înghețatului).
- Rândurile de rețea nevalidate **fără cantitate** (lic. 3 reală: #6, #7 „Tub protecție … Dn250”, m, NULL) nu mai dispar din lipsa JS (`randuriFront` le sărea; view-ul le număra).
- View: pentru grupurile din AFARA rețelei (invalidate ieșite, TOTAL invalidate, unitate schimbată) `*_m` = **doar rândurile în m** (înainte: 300 ml + 3 buc = „303 m”), `*_pe_um` = defalcarea `{um: {suma, randuri, fara_cantitate}}`, `*_fara_cant` pentru F3 / fără tip / unitate de normalizat. H2 scrie din `*_pe_um` („lungimi 264,69 ml; bucăți 38,65 buc”); view vechi ⇒ „X m (doar rândurile în m; restul unităților necunoscut)”.

### 13.6 (e) Clarificări v6 — ciornele umane protejate, nicio transmitere
- **Constatare:** v5 live (și v6 de până acum) **rescria textul standard al unei ciorne APROBATE** (`de_trimis`) când se schimbau totalul F3 / motivele și o lăsa `de_trimis` — omul aprobase alt text decât cel „de trimis” (reprodus pe PGlite: textul aprobat „1.000 m” devine „1.200.0 m”, status `de_trimis`) — și o **retrăgea** când planșele deveneau citibile.
- **v6 (sarcina 2):** protejată = text editat de om SAU `de_trimis`. Textul și statusul **nu se ating**; lista planșelor din sursă se actualizează (ca în v5), iar ciorna primește **o singură dată** marcajul `,revizie_planse_auto` (exclusă din adresa generată — `necesitaRevizie` — până la revizie) + o notificare internă („NU s-a trimis nimic”). În Clarificări, „✓ revizuită” scoate doar marcajul platformei. Ciorna mașinii (standard, `propunere`) se comportă ca înainte (rescrisă / retrasă). Pentru o ciornă standard aprobată al cărei text generat s-ar schimba: omul o editează (devine „editată”) sau o trece înapoi la `propunere` (acceptă textul regenerat).
- **Nicio transmitere:** nimic nu trece în `trimisa` / `raspunsa`; v6 scrie doar în `ofertare_clarificari` și `notifications` (interne) — md5 identic pe cantități / documente / istoric / grafic / PT înainte și după 3 apeluri. #63 azi: „neschimbat”.

### 13.7 (f) Compatibilitatea cu codul publicat
- **Migrarea 2 (+ 1b) cu `main` publicat (edge v25 = main, frontend main):** codul vechi nu citește niciun obiect nou (`git grep origin/main`: 0 referințe la `v_ofertare_cantitati_nevalidate`, `v_ofertare_transfer_conflicte`, `transfer_cantitati`, `ofertare_transfer_stare`, `inregistrare_id`, istoric); transferurile lui = jurnal legacy, citit cu aceleași reguli; scrierile lui păstrează cheia (PGlite: citire, retăiere, **RPC-ul de transfer publicat** — scrie, închide lease-ul, înregistrarea neatinsă); v6 are aceeași semnătură (câmpuri doar în plus), iar protecția ciornelor se aplică și apelurilor vechi (doar mai strict). `ofertare-clarificari-propune` publicat (v5 al funcției) nu citește view-ul. 1b: §12.4 (sigur în ambele ordini).
- **Codul nou + BD fără migrarea 2:** H2 BLOCK „nu putem verifica” pe orice licitație, poarta graficului BLOCK „nu putem verifica sursa” (generarea graficului imposibilă), Documente „nu putem verifica conflictele”, confirmarea ⇒ eroare vizibilă, generatorul de clarificări ⇒ „NU AU PUTUT FI CITITE”. Transferul automat nou scrie înregistrarea în jsonb, fără schemă ⇒ **nu trebuie suspendat niciun transfer automat**; costul ferestrei e blocarea porților (fail-closed) ⇒ ordinea rămâne: **migrarea 2 înainte de merge / deploy**.
- **Rollback-ul migrării 2:** view-urile și funcțiile noi scoase, v6 = live (md5 `0875c220…`), datele neatinse (înregistrările rămân în `analiza`, ignorate de codul vechi); reaplicarea le recitește. Codul nou după rollback ⇒ fail-closed (H2 „nu putem verifica”).

### 13.8 Condițiile de închidere Copilot → dovada
> Înlocuit de **§14.2** (reparația rundei 1: jurnalele legacy, v6 cu amprentă, ADDENDUM 2 Copilot). Tabelul de mai jos = starea de la `be63d25`.

| Condiția (verdictul 26.09) | Ce s-a făcut | Dovada |
|---|---|---|
| 2a — conflictul NU produce rând, există cantități validate ⇒ view-ul / poarta văd incompletitudinea sursei | înregistrarea pe document + `ofertare_transfer_stare` + `v_ofertare_transfer_conflicte` + câmpurile din `v_ofertare_cantitati_nevalidate`; poarta graficului BLOCK, H2 WARN, clarificări, Documente | deno E2E „sarcina 2 (a)” ×4 (ambiguu pe 2 poziții VALIDATE — 0 rânduri scrise, 0 nevalidate, înregistrare deschisă; amânat; căzut; TOTAL multiplu); PGlite lic. 900 (copii ale rândurilor reale 2, 3): view `retea_nevalidate 0`, `transfer_conflicte_docs 1`; poarta graficului / H2 / „Propune” pe aceleași date; licitație fără niciun rând; paritate 327 / 327 |
| închiderea: recitire fără conflicte SAU confirmare umană, fără ștergere tăcută | `inchis_prin` + `anterior` / `istoric`; `ofertare_transfer_conflicte_confirma` | deno pur „închiderea” + E2E recitire curată; PGlite: 4 refuzuri (fără sesiune / fără acces / notă / token vechi), autor = `auth.uid()`, conflictele păstrate, `rev` nou ⇒ CAS pe rev vechi = 0 rânduri, recitire cu conflicte redeschide, legacy confirmat și legat |
| 2b — aprobat, folosit în grafic / PT, apoi modificat | trigger + JS consecvente; consumatorii „de reverificat” | PGlite lic. 3 reală: cifră și atribut — status, notă (octet), istoric, rândul păstrat; „cant” / „front” BLOCK, INCOMPLET; rândul „grafic” al porții propunerii „DE REVERIFICAT”; versiunea / semnătura PT păstrate; revalidarea nu redevine verde |
| view indisponibil / eroare ⇒ „nu putem verifica”, nu zero | H2, poarta graficului, poarta propunerii, editorul, Documente, generatorul | vitest „sarcina 2 (c)”; PGlite cu erorile reale PG (relație inexistentă, drepturi refuzate) |
| mesajele nu agregă „X m” peste unități | clase de unități, `*_pe_um`, `*_fara_cant`, rândurile fără cantitate | vitest „sarcina 2 (d)”; PGlite lic. 902 (ml / bucata / buc — JS = view) și lic. 3 (#6, #7) |
| conflictul recitirii = „de reverificat”, valoarea / sursa aprobate recuperabile | `DE_REVERIFICAT_CITIRE` pe toate prefixele citirilor automate; `cantitate` / `sursa` / `tip_sursa` neatinse de transfer / CAD; istoric | deno 203 / 203 și vitest (prefixele actualizate); PGlite (b): istoricul are valoarea aprobată + sursa |
| v6: textul / statusul ciornelor umane protejate, nicio transmitere automată | protecție + marcaj + notificare; „✓ revizuită” | PGlite (e): #63 real neschimbat; planșă nouă ⇒ text / status neatinse, marcaj o dată, 1 notificare; ciorna standard aprobată nerescrisă și neretrasă; ciorna mașinii ca înainte; control negativ pe v5 live |
| 3 — schema compatibilă cu versiunea încă publicată | legacy + cheia păstrată + semnături neschimbate | PGlite (f): scrierile codului vechi, RPC-ul publicat, v6 apelată vechi, rollback + reaplicare |

### 13.9 Teste (26.09.2026, pe worktree)
| Comandă | Rezultat |
|---|---|
| `deno test --node-modules-dir=none --no-lock -A supabase/functions/` | **226 / 226** (înainte 216): `ofertare-plansa-citeste` **203 / 203** (194 + 5 pure `transfer_conflicte_test.ts` + 4 E2E), `ofertare-clarificari-propune` 6 / 6 (+1) |
| `concurenta_test.ts` ×10 | **10 / 10**, 92 / 92 la fiecare rulare |
| `deno check` pe `ofertare-plansa-citeste/index.ts`, `ofertare-clarificari-propune/index.ts`, `worker/ofertare/clarificari.ts` | curat |
| `npx vitest run` | **549 / 549** (înainte 537; +12 `src/ofertareSarcina2.test.js`) |
| `npm install && npx vite build` | up to date, build OK; `package-lock.json` neschimbat |
| `node scripts/test-cas-felii.mjs` | 34 / 34 |
| PGlite `scratchpad/pglite/test_sarcina2.mjs` (`node --experimental-strip-types`) | **75 / 75** — fixture-le reale md5 = producția (37 de rânduri, 8 documente); (c) înainte de migrare; aplicare fără schimbări de date, idempotentă, drepturi; (a) real + paritate 327 / 327 + scenariul Copilot + confirmarea; (b); (d); (e); (f) + rollback |
| PGlite `test_view_conditia2_s2` / `adv_r6_s2` / `test_r5_runda4_s2` (variantele sarcinii 2 ale suitelor anterioare: tabela documentelor în schemă, `*_m` doar m, v6 protejată) | 18 / 18, 13 / 13, 17 / 17 |
| PGlite `test_1b_prag_exact` / `test_aprobare_istoric_1b` | 66 / 66, 66 / 66 (neschimbate) |
| control negativ: consumatorii JS de la `124b2ce` + SQL nou | pică 10 verificări (toate cele ale sarcinii din JS) |
| control negativ: migrarea 2 de la `124b2ce` + JS nou | se oprește la prima citire a conflictelor („relation "v_ofertare_transfer_conflicte" does not exist”) |
| control negativ: handler-ul de la `124b2ce` cu testele noi | 3 / 3 E2E pică; testul pur nu se încarcă (`conflicteTransfer` inexistent) |
| mutații SQL (`scratchpad/s2/mutanti.py`, 12) | **12 / 12 prinse** (protecția v6, retragerea ciornei aprobate, Nr lipsă în legacy, tokenul, `rev`, FULL JOIN, `*_m` peste unități, legătura ignorată, „în curs” fără limită, accesul, forma necunoscută, confirmarea care șterge conflictele) |
| mutații handler (`mutanti_handler.py`, 7) | **7 / 7 prinse** |
| mutații consumatori JS (`mutanti_js.py`, 10) | **10 / 10 prinse** |
| `md5sum deno.lock` | `875e293d073b359f7433064914937c86`, neschimbat |

### 13.10 Rămâne / de decis (Razvan)
1. **GO pentru migrarea 2** (acum cu sarcina 2), înaintea merge-ului; ordinea: 1 (aplicată) → 1b → 2 → merge → deploy `ofertare-plansa-citeste` + `ofertare-clarificari-propune`. După aplicare: preview-urile P3 / P4 din antet, `get_advisors`, `registru_automatizari` (funcția nouă de confirmare: nu citește conținut extern, scrie doar `analiza.transfer_cantitati` pe un document, cu rol verificat în corp și notă; view-urile: doar citire).
2. **Decizii luate pe principiile Copilot, reversibile:** (a) conflictele deschise ⇒ poarta graficului BLOCK, H2 **WARN** (planșele sunt surse informative); (b) conflictele includ Dn nestandard și adnotările pe Dn absent (după aplicare, lic. 95 / doc 470 are 2 deschise — de confirmat cu ✋ sau prin recitire); (c) transferul amânat = conflict deschis („neefectuat”); (d) ciorna standard APROBATĂ e protejată ca una editată (marcaj, nu rescriere / retragere); (e) `*_m` din afara rețelei = doar m.
3. Limite: sursa rândului tot nu poartă id-ul documentului (§7); `ofertare-document-nou-citeste` scrie `analiza` fără CAS (fereastră mică, doar la documentele noi); `necorelare_unitate` nu e în lista de conflicte (e în text ⚠); un TOTAL multiplu e doar raportat (decizia lic. 5).
4. Neschimbate: validarea de conținut lic. 95 (verdict pct. 4), lic. 3 rândurile 2–4 și 9, `v_ofertare_pt_stare.qm`.

## 14. Reparația rundei 1 — verificatorii (BD-migrare, consumatori-UI) + ADDENDUM 2 Copilot (26.09.2026)

Pornire: capul `be63d25`. Verificatorii au găsit 2 MAJORE (v6 suprascria o ciornă umană editată sub antet; jurnalele codului vechi citite ca „fără conflicte”), 3 condiții neîndeplinite (2a pe jurnalele legacy; v6 ×2) și 13 minore. Între timp Copilot a răspuns (ADDENDUM 2, „obligatoriu pentru orice reparație de acum încolo”): aprobarea finală, restanțe distincte, închiderea conflictelor cu drept de decizie, recitirea parțială nu închide, TOTAL multiplu fără valoare nouă. Totul e în cod pe ramură și în fișierele de migrare; **nimic aplicat, nimic pushat**; pe BD doar SELECT.

### 14.1 Starea BD (doar SELECT, 26.09.2026)
- Aceleași 8 documente cu jurnal (130, 470–475, 1035), **niciunul** cu `analiza.transfer_cantitati` și **niciunul** cu marcajele R5 (`identitate_randuri`) — md5 al jurnalelor = fixture-ul PGlite (`legacy:f705061c…` pentru 130, `legacy:497f1376…` pentru 470). 471–475: 0 tronsoane (planșele ilizibile din clarificarea #63).
- Clarificări `auto_planse`: doar #63 (lic. 95, `de_trimis`, text editat de om, sursa `planse_auto:475,474,473,472,471`, fără tokeni).
- **Poarta server-side a aprobării finale există**: trigger-ul `trg_ofertare_pt_pachet_poarta_documentatie` (BEFORE INSERT OR UPDATE OF stare pe `ofertare_pt_pachet`, → `aprobat` / `depus`), funcția `fn_ofertare_pt_pachet_poarta_documentatie` (SECURITY DEFINER, md5(prosrc) `a45ccdb8…`, ACL postgres + service_role) — verifica DOAR `v_ofertare_seap_completitudine`. 0 pachete în BD.
- **Dreptul de decizie existent**: `fn_ofertare_source_pack_poate_decide(licitatie)` = owner / responsabilul licitației / admin Ofertare (`user_module_access.access_level = 'admin'`, 1 persoană); EXECUTE authenticated.
- Politicile pe `ofertare_documente_atribuire`: SELECT (sesiune), INSERT / UPDATE / DELETE (`fn_are_acces_ofertare`) — orice utilizator Ofertare putea scrie `analiza` direct.

### 14.2 Condițiile de închidere Copilot (verdict + ADDENDUM 2) → dovada — înlocuiește tabelul §13.8
| Condiția | Ce s-a făcut | Dovada (testul ar fi picat înainte) |
|---|---|---|
| 2a pe jurnalele LEGACY (verificatorul: D9, doc 130) | SQL: jurnal fără marcaje R5 = **`legacy_partial` DESCHIS** (restanța `evaluare_partiala`, și lângă conflictele numărate); JS: aceeași regulă (`stareLegacy`) + **conversia** jurnalului vechi într-o înregistrare „legacy” la prima scriere a codului nou (nu mai dispare când citirea nouă îl suprascrie); **reevaluarea fără AI** (`mod: 'reevalueaza'`) pe citirea salvată | PGlite `test_sarcina2_r1`: cele 8 documente reale toate deschise (7 `legacy_partial`, 470 `conflicte` n = 3); paritate JS ↔ SQL 333 / 333 (stare, n, deschis, restanțe pe tip); deno: `stareLegacy` pe 130 / 470 / 471, conversia, E2E „reevaluează” (0 apeluri AI, 0 cost, proveniența păstrată, refuz la citire incompletă / zonă căzută); control: migrarea de la `be63d25` — oprită la primul obiect lipsă |
| 3 — v6 păstrează textul / statusul ciornelor umane (G1, S5(i)–(ii)) | „a mașinii” = textul e EXACT ultimul text generat: amprenta `gen_<md5[:12]>` în sursa (la INSERT și la rescriere); fără amprentă (ciornă v5) = standard doar dacă textul e identic cu cel generat acum; aceeași regulă la retragere și la marcare | PGlite R1-v6: corp editat sub antet ⇒ „neschimbat”, text intact; planșă nouă ⇒ protejată, marcaj + 1 notificare; planșe citibile ⇒ NU retrasă; ciorna mașinii se retrage ca înainte; **control negativ: v6 de la `be63d25` o rescrie** |
| ADDENDUM c — apeluri repetate nu multiplică notificarea | amprenta EVENIMENTULUI `ev_<md5(text generat \| planșe)[:12]>`: marcaj + notificare o singură dată pe eveniment, și după „✓ revizuită” pe aceeași stare | PGlite: 3 apeluri + „✓ revizuită” + 3 apeluri = 1 notificare; eveniment nou (planșe citibile) = încă 1, apoi nimic; #63 real: la fel; ciornă standard APROBATĂ cu textul generat schimbat (drumul care ajunge la fiecare apel în ramura „protejat”): 3 + „✓” + 3 apeluri = nicio notificare în plus |
| ADDENDUM a — aprobarea finală blocată de un conflict deschis, chiar cu H2 WARN (testul final 1) | UI: rândul nou **„sursa_cantitati”** în `evalueazaPoarta` (BLOCK pe restanțe deschise / transfer în curs / control indisponibil) — păzește semnarea „gata de depus” și „🔏 Aprobă pachetul”; BD: **funcția existentă a trigger-ului pachetului EXTINSĂ** (nu un trigger nou): restanțele deschise, transferul în curs sau view-ul indisponibil ⇒ RAISE; partea de documentație neschimbată | vitest „testul final 1”; PGlite R1-final: lic. 970 (rândurile reale 2, 3 validate) + Dn60 deschis ⇒ H2 WARN, „sursa_cantitati” BLOCK, `UPDATE stare='aprobat'` refuzat; după confirmarea ca excepție ⇒ trece; o rulare nouă cu conflict ⇒ din nou refuzat |
| ADDENDUM — view indisponibil + confirmarea omului ⇒ draft permis, aprobarea finală blocată (testul final 2) | draftul: capitolele, parametrii / fronturile graficului („Propune din cantități” cu avertismentul numit), editorul de cantități (confirmare explicită, existentă) nu trec prin poartă; finalul: H2 + „sursa_cantitati” BLOCK, trigger-ul pachetului RAISE „nu putem verifica” | vitest „testul final 2”; PGlite: view redenumit ⇒ pachet „propus” inserat (draft), aprobarea refuzată în BD, poarta UI block |
| ADDENDUM — închiderea = REZOLVARE sau EXCEPȚIE JUSTIFICATĂ, legată de conflictul exact, drept de decizie, nu validează cantități, nu acoperă conflicte noi | `ofertare_transfer_conflicte_confirma(doc, token, tip, notă)`: tip `rezolvat` (notă ≥ 5) / `exceptie` (≥ 20: de ce nu afectează oferta și ce sursă o acoperă); dreptul = `fn_ofertare_source_pack_poate_decide` (nu `fn_are_acces_ofertare`); `confirmat_token` = id-ul rulării; o confirmare contează DOAR validă (uuid, ISO, tip, notă, token = id) — în SQL și în JS | PGlite: 7 refuzuri (fără sesiune, alt modul, editor Ofertare fără decizie, notă scurtă, token vechi, tip necunoscut, excepție nejustificată); cantitățile md5 identice după confirmare; rulare nouă ⇒ deschis; deno: 9 forme invalide ⇒ deschis |
| ADDENDUM — recitirea „fără conflicte” nu închide dacă a pierdut pozițiile / zonele problematice (testul final 3) | fiecare conflict are **ancore** (zone, Dn, poziții evaluate); fiecare transfer — **acoperirea** (citire completă, zonele citite, Dn-urile observate, cantitățile evaluate); neacoperit ⇒ purtat `nerezolvat_la_recitire` (fără cuiburi); fără ancore ⇒ închis doar de o citire completă cu observații | deno: recitire golită de Dn110 / Dn60 / Dn48 ⇒ deschis; incompletă ⇒ deschis; rezultat gol ⇒ deschis (și pe 471–475 reale); acoperire totală ⇒ închis; E2E handler: recitirea doar cu Dn160 NU mai închide ambiguitatea pe Dn110 (sarcina 2 o închidea); PGlite R1-acoperire (SQL) |
| ADDENDUM e — TOTAL multiplu fără valoare nouă aleasă automat (testul final 4) | cu ≥ 2 rânduri TOTAL, **niciunul** nu primește totalul planșei (înainte: primul); valori / statusuri / note / istoric neatinse; conflictul `total_ambiguu` (decizie Răzvan A/B/C) | deno E2E: 0 scrieri pe #4 / #5, TOTAL validat neatins; „runda 9 TOTAL-b” adaptat (`cantitate_plansa` #4 = NULL, nu 1.100) |
| ADDENDUM b — restanțe DISTINCTE, cu cauza și acțiunea; fără clarificări către AC din erori interne | `src/ofertareTransferRestante.js` (copie identică în `ofertare-clarificari-propune/restante.js`): 16 tipuri, 3 categorii (procesare internă / decizie internă / de verificat pe planșă); view: `restante` pe document, `transfer_restante` pe licitație; poarta graficului, H2, „sursa_cantitati”, Documente le numesc; generatorul de clarificări: „NU formula clarificări către autoritate din ele” (înainte: „pot justifica o clarificare de tip C”) | vitest (dicționar complet, texte, copia identică); deno `core_test` (transfer amânat = eroare internă; Dn nestandard ≠ imposibil; adnotare ≠ tronson suplimentar) |
| ADDENDUM d — `_m` doar pe m; lungimile din conflicte nu sunt „metri lipsă” | `*_m` doar m (neschimbat, sarcina 2); textele spun „lungimile din conflicte sunt observații pe planșă (pot fi suprapuse), nu metri lipsă”; cantitățile din mesajele de lipsă EXACTE (0,004 mc nu mai e „0 mc”) | vitest (0,004 mc / 0,004 km); PGlite (d) |

### 14.3 Minorii verificatorilor → reparația
| Minor | Reparația | Dovada |
|---|---|---|
| un jurnal corupt cădea H2 pe TOATE licitațiile (D4b); `confirmat_de` ne-uuid (D4) | `ofertare_transfer_stare` defensivă: `ofertare_ts_valid` (regex ISO + cast protejat), n limitat la 9 cifre, tipuri verificate, bloc EXCEPTION ⇒ „necunoscut” DESCHIS cu motivul „date corupte”; câmpurile întoarse deja validate (cast-urile view-ului nu mai pot cădea) | PGlite R1-corupt: 5 documente corupte, view-urile se citesc pe toate licitațiile; excepție simulată ⇒ „necunoscut” |
| confirmarea falsificată prin UPDATE direct (D5c / S2); `confirmat_la` oarecare închidea | trigger nou `trg_ofertare_doc_chei_server` (SECURITY INVOKER intenționat): la o scriere DIRECTĂ a unui utilizator (authenticated / anon) cheile serverului `transfer_cantitati` și `citire_ai` păstrează valorile din BD (la INSERT se scot); service_role / funcțiile DEFINER scriu normal; + validitatea confirmării (14.2) | PGlite R1-chei (confirmare falsă, jurnal „curățat”, INSERT forjat; service_role scrie; **control: fără trigger falsificarea intră**) |
| **găsit de mutații (26.09):** `confirmat_la` valid + `confirmat_de` LIPSĂ ⇒ `v_conf` NULL ⇒ `deschis` NULL ⇒ documentul NU se număra ca deschis (fail-open, doar pentru o scriere de server incompletă) | `coalesce(…, false)` pe validitatea confirmării + în view `coalesce(deschis, true)` (fail-closed) | PGlite R1-confirmare (6 forme, SQL = JS): validă ⇒ închis; token altă rulare / fără token / autor ne-uuid / autor număr / fără autor ⇒ deschis; mutantul „fără coalesce” — prins |
| `cad-parse` publicat scrie `validat` (re-validare mașinală în fereastra migrare → merge, D7) | deja reparat pe ramură (`_cadCantitate.js` scrie `extras`) — operațional: **fără import CAD între aplicarea migrărilor și merge** (sau merge imediat) | §14.4 |
| JS tăia pe UTF-16 (surogat singur) | `scurt` pe puncte de cod (`Array.from`), ca `left()` | setul comun 1b: caz nou cu emoji la pozițiile 59–60 (SQL = JS) |
| severitatea: procentul rotunjit înainte de prag; cifrele pe 2 zecimale | pragul pe valoarea exactă (|Δ| × 100 < |a|), procentul afișat trunchiat; cifrele din notă exacte (max. 6 zecimale) — în JS și în 1b | setul comun: 0,999 % ⇒ „mică … +0,99 %”; „100 m → 100,000001 m”; „2,5 mc → 2,500001 mc” (SQL = JS, 74 / 74) |
| nota trigger-ului „nu mai e valabilă” pe o simplă observație | doar cifra din planșă schimbată ⇒ „e de reverificat … o citire nouă a planșei nu infirmă aprobarea” (JS = SQL) | setul comun (#2, #4, #9, observație + denumire ⇒ „nu mai e valabilă”) |
| `ofertare-document-nou-citeste` scria `analiza` din instantaneul de dinainte de AI | recitire + doar cheia proprie (`citire_noi`) + CAS pe `citire_ai.rev`, 3 încercări, apoi 409 (modul `scriere.ts`) | deno `scriere_test.ts`: transferul scris în fereastră rămâne; scriitor continuu ⇒ nimic suprascris |
| comasări neconfirmate / perechi neîmperecheate doar avertismente | conflict `identitate_incerta` (Copilot 2a: „identitate incertă”), ancorat pe zone, numărat și în derivarea SQL | deno + paritate |
| `fmtQ` rotunjea la 2 zecimale | `fmtExact` | vitest |
| insigna / butonul doar pe `tip = 'plansa'` | orice document din view | cod (`OfertareLicitatii.jsx`) |
| bannerul număra tokenii ca planșe | doar id-urile numerice; „⚠ necesită revizie” dacă ciorna e marcată | cod |
| workerul NAS nu era în lista de deploy | workerul se actualizează singur la un commit nou pe `main` (`worker/ofertare/main.ts` `comitNou` → ieșire → `entrypoint.sh` fetch + reset + restart) — după merge: verificat SHA-ul în heartbeat | §14.4 |

### 14.4 Compatibilitatea cu codul publicat și ordinea A (actualizată)
- **1b** — ca în §12.4 (sigur în ambele ordini); nota nouă păstrează prefixul și terminatorul recunoscute.
- **Migrarea 2 cu codul publicat** (edge v25 = main, frontend main): codul vechi nu citește nimic nou; jurnalele lui ⇒ `legacy`, acum **deschise** (`legacy_partial` fără conflicte numărate). Trigger-ul cheilor serverului nu atinge codul vechi: UI-ul de pe main nu scrie `analiza`, edge-ul / `/api` scriu cu service_role, RPC-ul de transfer e DEFINER. Trigger-ul pachetului extins se aplică și UI-ului vechi („Aprobă pachetul”) — refuză cu mesajul lui.
- **Efect imediat după aplicare** (preview-ul pe cele 8 documente reale): lic. 3 — 1 restanță (doc 130, evaluare parțială), lic. 95 — 6 documente / 8 restanțe (Dn60, adnotări, 6 × evaluare parțială), lic. 102 — 1. Pe aceste licitații poarta graficului e BLOCK și aprobarea finală e blocată până la **„♻ reevaluează (fără AI)”** (după deploy-ul edge; 130 și 1035 au citiri cu date — se pot închide; 470 își păstrează Dn60 / adnotările din codul nou; 471–475 au citiri goale — rămân deschise până la ✋, de regulă „excepție: planșă ilizibilă, cerută clarificarea #63”) sau până la ✋ confirmare. 0 pachete în BD, deci nimic aprobat nu e afectat retroactiv.
- **Ferestre (între aplicarea migrărilor și merge / deploy)**: (1) fără import CAD (cad-parse publicat scrie `validat`, D7); (2) fără citiri de planșe cu edge v25 (ar scrie jurnale legacy — sigur, fail-closed, dar blochează până la reevaluare); (3) deploy edge `ofertare-plansa-citeste` + `ofertare-clarificari-propune` + `ofertare-document-nou-citeste` imediat după merge; workerul NAS se actualizează singur.
- **Ordinea A**: 1 (aplicată) → 1b → 2 → merge → deploy (3 edge-uri) → reevaluarea fără AI pe cele 8 documente (GO pentru scrierile în rânduri) → test LIVE controlat. După aplicare: preview-urile P3 / P4, `get_advisors`, **`registru_automatizari`** (trigger-ul nou al cheilor serverului; confirmarea cu drept de decizie; modul „reevaluează” al edge-ului: nu citește conținut extern nou — doar citirea salvată —, scrie în cantități ca transferul, poarta pe cheltuială owner / responsabil; extinderea trigger-ului pachetului).

### 14.5 Teste (26.09.2026, pe worktree)
| Comandă | Rezultat |
|---|---|
| `deno test --node-modules-dir=none --no-lock -A supabase/functions/` | **234 / 234** (înainte 226): `transfer_conflicte_test` 9 (+4: testul 3, confirmarea validă, legacy, identitate incertă), `concurenta_test` 94 (+2 „reevaluează”), `ofertare-document-nou-citeste/scriere_test` 2 (nou), `core_test` actualizat |
| `concurenta_test.ts` ×10 | 10 / 10, 94 / 94 la fiecare rulare |
| `deno check` (plansa-citeste, clarificari-propune, document-nou-citeste, `worker/ofertare/clarificari.ts`) | curat; `deno.lock` neatins (`875e293d…`) |
| `npx vitest run` | **565 / 565** (înainte 549) |
| `npm install && npx vite build` | up to date, build OK |
| `node scripts/test-cas-felii.mjs` | 34 / 34 |
| PGlite `test_sarcina2_r1.mjs` | **102 / 102** — fixture-le reale md5 = producția; poarta pachetului live reconstruită (md5); R1-v6, R1-corupt, R1-confirmare, R1-chei, R1-acoperire, R1-final + secțiunile sarcinii 2 adaptate; rollback exact (v6 live, poarta pachetului live, ACL) |
| PGlite `test_1b_prag_exact` | **74 / 74** (setul comun 43 de cazuri, SQL = JS octet cu octet) |
| PGlite `test_aprobare_istoric_r1` / `test_view_conditia2_r1` / `adv_r6_r1` / `test_r5_runda4_r1` | 66 / 66, 18 / 18, 13 / 13, 17 / 17 |
| control negativ: `test_sarcina2_r1` cu migrarea 2 de la `be63d25` | se oprește la primul obiect nou lipsă (confirmarea cu 4 parametri) |
| control negativ: `test_sarcina2_r1` cu consumatorii JS de la `be63d25` | pică (textul restanțelor, rândul „sursa_cantitati” inexistent) |
| mutații (`scratchpad/r1/mutanti.py`) | **29 / 29 prinse** (14 SQL, 15 JS / TS). Prima rulare: 25 / 28 — 3 scăpate (notificarea pe eveniment pe drumul fără întoarcere timpurie; confirmarea fără token; autorul ne-uuid) ⇒ 2 teste noi PGlite, care au prins și **un defect real** (§14.3) ⇒ reparat + mutantul lui |

### 14.6 Rămâne / de decis (Razvan)
1. **GO pentru 1b și migrarea 2** (ordinea A, §14.4) și, după deploy, **GO pentru reevaluarea fără AI** pe cele 8 documente (scrie în cantități).
2. **De decis (reversibil):** (a) jurnalele vechi fără marcaje = deschise (`legacy_partial`) — blochează lic. 3 / 95 / 102 până la reevaluare / ✋; (b) o citire goală nu închide o evaluare parțială (471–475 ⇒ ✋ „excepție”); (c) dreptul de confirmare = owner / responsabil / admin Ofertare (funcția existentă a „source pack”); (d) excepția cere ≥ 20 de caractere; (e) aprobarea finală blocată și de un transfer în curs.
3. Limite: ștergerea unui document (politica DELETE, orice utilizator Ofertare) îi scoate și conflictele — acțiune explicită, vizibilă, dar fără urmă în view; `necorelare_unitate` rămâne în text, nu în conflicte; ancorele unui conflict vechi pe un Dn citit acum altfel (Dn60 → Dn63) nu se consideră acoperite ⇒ ✋.
4. Neschimbate: validarea de conținut lic. 95 (verdict pct. 4), decizia TOTAL lic. 5 (A/B/C), R7 / 770 (fluxuri separate).
