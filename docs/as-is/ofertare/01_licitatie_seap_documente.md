# AS-IS — Ofertare: licitație → radar/veghe SEAP → import → documente de atribuire → revizii → go/no-go → status

Repo `/home/user/pontaj-pro` @ `55779b5` (main). DB live `dxczwkbciseqniprspcu` (citit prin MCP, 23.09.2026). Factual, fără propuneri.

Legendă fișiere: `OL` = `src/OfertareLicitatii.jsx`; `EF/<n>` = `supabase/functions/<n>/index.ts`; `API/seap` = `api/seap-import.js`.

## Fapte de bază (DB live)

- `ofertare_licitatii.status` CHECK: `identificata|analiza|go|in_lucru|depusa|castigata|pierduta|abandonata` (default `'identificata'`). Coloane relevante: `decizie_go` (CHECK `go|no_go`), `decizie_motivare/decizie_de/decizie_la`, `derogare_depunere bool default false`, `responsabil_id`, `c_notice_id`, `sys_notice_type_id`, `documentatie_adusa_la`, `termen_depunere`, `contract_id`, `nr_anunt UNIQUE`.
- Trigger pe `ofertare_licitatii`: **doar** `trg_gate_depunere BEFORE UPDATE FOR EACH ROW → fn_gate_depunere()`. Nu există trigger de tranziții de status, nu există `updated_at` trigger.
- RLS `ofertare_licitatii`: policy `ofertare_licitatii_all` `FOR ALL USING (auth.uid() IS NOT NULL) WITH CHECK (auth.uid() IS NOT NULL)` → **orice utilizator autentificat poate UPDATE orice coloană (inclusiv `status`, `decizie_go`, `derogare_depunere`)**.
- RLS `ofertare_documente_atribuire`: select `auth.uid() IS NOT NULL`; insert/update/delete `fn_are_acces_ofertare()` (owner sau `user_module_access.module='ofertare'`).
- `ofertare_documente_atribuire` coloane: `id, licitatie_id, fisier_path (NOT NULL), nume_original, tip (CHECK fisa_date|cs_volum|plansa|lista_cantitati|formular|model_contract|raspuns_clarificare|alta, default 'alta'), antet jsonb, revizie text, text_extras, ocr, pagini, size_bytes, procesat_la, eroare, created_at, status_procesare (CHECK neprocesat|in_lucru|procesat|partial|eroare|ignorat), pagini_procesate, pagini_felie, sursa (default 'upload', fără CHECK), analiza jsonb, analiza_la, pagini_necitite int[], pagina_offset, aparut_ulterior bool, procesat_de, seap_cod, seap_meta jsonb`. Indexuri: `ofertare_doc_seap_cod_unic UNIQUE (licitatie_id, seap_cod) WHERE seap_cod IS NOT NULL`; nu există unique pe `(licitatie_id, nume_original)` nici pe `fisier_path`. **Nu există coloană de hash/sha256** pe documentele de atribuire (există `sha256` doar pe `ofertare_pt_pachet_fisiere`).
- Trigger pe `ofertare_documente_atribuire`: `trg_pt_invalideaza_la_revizie_document AFTER UPDATE OF revizie → fn_pt_invalideaza_la_revizie_document()`.
- Cron (`cron.job`): `ofertare_radar_0500` (`0 5`, body `{zile:2}`), `ofertare_radar_1230` (`30 12`, `{zile:1}`), `ofertare_seap_veghe_0520` / `_1250` (secret din Vault + `Authorization: Bearer <SUPABASE_ANON_JWT>`), `ofertare_alerte_0540` / `_1310`, `ofertare_participari_sapt` (`20 4 * * 1`, `{batch:true}`), `ofertare_raport_zilnic_0430` (`{actiune:'raport_zilnic'}`), plus `ofertare_alerte_atentie_0630` (SQL `ofertare_alerte_atentie()`), `ofertare_ingest_worker` (`* * * * *` → `ofertare_ingest_tick()`), `ofertare_extragere_worker`, `ofertare_clarificari_reminder_0600`, `ofertare_alerta_pagini_goale_0615`. Notă factuală: joburile radar/alerte/participari/raport_zilnic au secretul `x-radar-secret` scris **literal** în `cron.job.command` (valoare `gzp-98d7…`), în timp ce veghea îl ia din Vault.
- Stare live: status → pierduta 34, castigata 17, abandonata 16, identificata 10, in_lucru 3, depusa 3, analiza 1, go 1. Din 54 licitații `depusa|castigata|pierduta`: **0 cu `derogare_depunere=true`, 53 fără nicio cerință în `ofertare_cerinte`**. `ofertare_pt_pachet`: 0 rânduri; `ofertare_acoperire_revizii`: 2 rânduri. Documente: 389 rânduri; 16 placeholder (`/neincarcat/`), 23 cu `seap_cod`, 59 cu `revizie` non-null, 97 `aparut_ulterior`, 1 rând cu `eroare` `INLOCUIT de doc 211 — reurcat manual … Rand pastrat pentru istoric, 11.09.2026.` (id 202, lic 15), 2 cu `seap_meta.inlocuieste`, 54 „bucăți" (`— pN_pag…`).

---

## (A) PIPELINE

| Step_ID | Stage | Trigger | UI/component | Function/API/RPC | Tables/views | Input | Output | Status_before | Status_after | Human_gate | AI_action | Versioning | Provenance | Downstream_consumer | Failure/retry |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| L01 | Radar — scan SEAP | cron `ofertare_radar_0500/1230` (`x-radar-secret`) sau buton UI | `OL` `RadarLicitatii.scaneaza` L3350-3357 (`functions.invoke('ofertare-radar-scan',{zile:2})`) | `EF/ofertare-radar-scan` L88-299; auth `secretOk` (RPC `fn_verifica_radar_secret` = Vault `RADAR_SECRET`, fără `_VECHI`) sau JWT valid (orice user, L93-97) | `ofertare_radar` (insert L154-157), `ofertare_radar_scan_log`, `ofertare_experienta` (profil), SEAP `GetCNoticeList/GetSection3View/GetSection4View/GetDfNoticeSectionFiles` | `zile≤14`, `maxDetalii≤15`, `scor` | rânduri noi în `ofertare_radar` cu `relevant` (regex L71-77), `segment`, apoi `sectiune3_text`, `documente` (max 60, cu `noticeDocumentUrl` temporar salvat! L190), `detalii_extrase` | radar: n/a | radar.status='nou' (default) | NONE (cron); UI: orice JWT valid | `claude-haiku-4-5-20251001`, max_tokens 1600, JSON `{scor,motiv,lipsuri}` L246-277; scor recuperat din JSON trunchiat L264-275 | `ofertare_radar_scan_log` per rulare; rândul radar se suprascrie (`update` L198, L277) | `nr_seap` (dedup L131), `c_notice_id`, `sys_notice_type_id`, `link` | L02, L03 | erori acumulate în `rezumat.erori` → `scan_log.eroare`; `detalii_extrase=true` doar dacă a extras ceva (L196-203); reîncercare scor max 5/rulare (L219-230) |
| L02 | Radar — alertă mail | cron `ofertare_alerte_0540/1310` | — | `EF/ofertare-alerte-mail` L154-204 (doar `x-radar-secret`) | `ofertare_radar` (`relevant=true, alerta_trimisa=false`), Resend | — | mail către `razvan.trusu@…, office@…` cu anunțuri `scor≥50` (max 30); `alerta_trimisa=true` și pentru cele sub prag (L171-173) | — | — | NONE | NONE (folosește scorul din L01) | n/a | `nr_seap`, `link` | om (decizie de promovare) | Resend eșuat → `{ok:false}` și **nu** se marchează `alerta_trimisa` (retrimite la următorul cron) |
| L03 | Promovare radar → licitație | click „Promovează" | `OL` `RadarLicitatii.promoveaza` L3368-3388 | `supabase.from('ofertare_licitatii').insert(...)` L3370-3381; `ofertare_radar.update({status:'preluat', licitatie_id})` L3383-3384 | `ofertare_licitatii`, `ofertare_radar` | rând radar | licitație nouă `status:'identificata'`, `canal` din prefix nr, `c_notice_id`/`sys_notice_type_id` din radar sau `deduIdSeap` L70-75, `observatii: 'Radar (scor X): …'` | — | `identificata` | `window.confirm` (orice user cu acces modul) | NONE | n/a | `nr_anunt = radar.nr_seap` (UNIQUE), `link_seap`, `radar.licitatie_id` | L06 | insert eșuat → toast; dacă insert reușește dar update radar eșuează nu e tratat (fără verificare de eroare L3383) |
| L04 | Înregistrare manuală (E0) + autofill | click „＋ Licitație nouă" / drag PDF fișă | `OL` `LicitatieFormModal` L455-590; `citesteFisa` L473-503; `OfertareLicitatiiTab.salveaza` L221-277 | `storage.upload('e0/<ts>_<rand>_<safe>')` L480; `functions.invoke('ofertare-e0-autofill')` L482; `ofertare_licitatii.insert/update` L256-262; `ofertare_documente_atribuire.insert({tip:'fisa_date', procesat_la, procesat_de})` L266-270 | `ofertare_licitatii`, `ofertare_documente_atribuire`, bucket `ofertare` | formular + PDF opțional | licitație (`status` default `identificata` — nu e trimis în payload) + rând doc fișă | — | `identificata` | garda anti-dublură `window.confirm` L238-248 (suprapunere autoritate≥0.5 & obiect≥0.6); butonul „Înregistrează" | e0-autofill (edge, în afara scope-ului citit) propune câmpuri; „AI propune, omul verifică" L470-471 | n/a; la editare `update` suprascrie câmpurile (L256) | `nr_anunt`, `link_seap`; docul fișă: `fisier_path` `e0/...` (NU sub `<lic>/atribuire/`) | L06 | erori → toast; fișa neatașată → toast `warn` L271 |
| L05 | identificata → analiza | click „Marchează În analiză" | `OL` `LicitatieDetailModal` tab `detalii`, `next.map` L2986-2988 → `onStatus` = `schimbaStatus` L279-285 | `ofertare_licitatii.update({status, updated_at})` L280-281 | `ofertare_licitatii` | status țintă din `TRANZITII` L50-57 | status | `identificata` | `analiza` | click (orice user cu acces modul; fără confirm) | NONE | n/a | — | L07 | eroare → toast |
| L06 | Import documentație din SEAP (edge, per-fișier + rezervă arhivă) | click „⬇️ Adu din SEAP" (UI) sau apel din veghe (L11, `Bearer SERVICE`) | `OL` `DocumenteSection.aduDinSeap` L769-838 (buclă ≤12 runde `de_la_index`) | `EF/ofertare-seap-import` L330-553; auth: `x-radar-secret` sau JWT (orice user) sau `jwt===SERVICE` L339-347 | `ofertare_licitatii` (select + `update documentatie_adusa_la` L548), `ofertare_documente_atribuire` (insert/update L370-377), bucket `ofertare` `<lic>/atribuire/<base36 ts>_<safe>`; SEAP `GetDfNoticeSectionFiles` (5 liste) + `files/noticedoc/<hash>` cu cookie de sesiune; rezervă `DownloadArchive` | `licitatie_id`, `de_la_index` | rânduri doc `sursa:'seap'`, `tip: ghicesteTip(nume)` L100-110, `status_procesare` `neprocesat`/`ignorat` (non-PDF, L393), `size_bytes`; placeholder-ele `/neincarcat/` se **completează** (update pe același id, L371-373) | orice | neschimbat (`documentatie_adusa_la` doar la `!continua` L547-550) | click (UI) / NONE (veghe) | NONE | **fără**: dacă `cheieNume(nume)` există deja printre cele urcate → `sarite_existente` L440-444 (nu se aduce versiunea nouă); `Date.now()` în path evită coliziuni de storage | `licitatie_id`, `nume_original` (real), cheie de dedup `cheieNume` L81-82 (fără spații/virgule/paranteze, fără `.p7s`); **linkul SEAP nu se salvează** (temporar); **fără hash** | L08, L09, L10 | buget 240s / 12MB → `continua=true,next_index`; per-doc eroare → `raport.erori` + `motivRezerva` → cade pe arhivă; >20MB → `lasate_pentru_vercel`; CHECK `tip` eșuat → `erori` (fișierul rămâne în storage fără rând L49-50); `curataOrfani` L309-328 șterge din storage obiectele fără rând (doar la final complet) |
| L07 | Import documentație — treapta grea (Vercel) | din `aduDinSeap` dacă rămân placeholder-e L810-833; din veghe L472-476 | `OL` L816-820 (`fetch('/api/seap-import', Bearer JWT)`) | `API/seap` L115-224; auth `x-import-secret === SEAP_IMPORT_SECRET` sau JWT (orice user) L128-133 | aceleași; **numai** `DownloadArchive` (arhiva integrală) | `licitatie_id` | ca L06; dedup pe `nume_original` **exact** (L150, L180 — nu `cheieNume`) | orice | neschimbat; `documentatie_adusa_la` doar dacă `raport.erori` gol L220-222 | click / NONE | NONE | fără (skip pe nume exact) | `nume_original`, `sursa:'seap'`; `estePdf` doar după extensie (L191, fără semnătură `%PDF-`) | L08 | erori în `raport.erori`; flux întrerupt → `break` |
| L08 | Upload manual (folder/fișiere) | „📁 Urcă folder" / „📄 Urcă fișiere" | `OL` `DocumenteSection.urca` L720-767 | `storage.upload('<lic>/atribuire/<ts>_<safe>')` L742; insert/update `ofertare_documente_atribuire` L754-755; `sparge()` auto >20MB non-planșă L758 | `ofertare_documente_atribuire`, bucket | FileList | rânduri `sursa` default `'upload'`, `tip: ghicesteTip` L614-626 (**regex diferit de edge**), `status_procesare` după semnătură `%PDF-` L712-718 | orice | neschimbat | click (RLS: acces modul) | NONE | **fără**: dedup pe `nume_original|size` față de urcate L733-741 → dacă același nume și altă mărime **se inserează rând nou**, fără legătură cu vechiul; placeholder cu același nume → update L752-754 | `nume_original` = cale relativă fără folder rădăcină L738; fără hash | L09 | eroare upload → `setWarn` și `continue`; insert fără verificare de eroare L755 |
| L09 | Procesare (extragere text + antet + revizie) | „🤖 Procesează" (browser, 3 paralel) / „☁️ Pe server" (coadă) | `OL` `proceseaza` L886-932, `proceseazaDoc` L840-856, `proceseazaPeServer` L697-702; server: `ofertare_ingest_tick()` (cron 1/min) | `EF/ofertare-ingest-doc` (`upd.revizie = antet?.revizie || row.revizie || null` L301; `status_procesare` L299) | `ofertare_documente_atribuire` (`text_extras, pagini, pagini_procesate, pagini_necitite, antet, revizie, ocr, status_procesare, eroare, procesat_la, procesat_de`), `ai_usage_log`, `ofertare_ingest_coada/lansari` | `doc_id` | text + antet JSON `{obiectiv,beneficiar,proiectant,proiect_nr,revizie,data,pare_scanat}` | doc: `neprocesat/in_lucru/eroare` | doc: `in_lucru` → `procesat`/`partial`/`eroare` | **poarta pe cheltuială** `poatePorniProcesarea(profile, licitatie)` = owner sau `responsabil_id` (`src/OfertareTriere.jsx` L36-37) — **doar în UI**; `ofertare_ingest_tick` nu reverifică rolul (coada e scrisă de UI) | citire PDF cu AI (model din ingest-doc), extrage `revizie` din antet | `revizie` (text) e suprascrisă la fiecare reprocesare de la pagina 0 (L301) — **declanșează** `trg_pt_invalideaza_la_revizie_document` doar dacă valoarea diferă | `doc.id`; `procesat_de` (identitate verificată) | L13 (cerințe), L17 (pt_dovezi), etapa1-mail | UI: auto-retry ×2 + a doua trecere + notificare `modul:'Comercial'` L920-929; server: lease 3 min, `MAX_INCERCARI=4`, oprire dacă worker NAS are heartbeat <10 min |
| L10 | Veghe SEAP (documente noi / răspunsuri / termen) | cron `ofertare_seap_veghe_0520/1250` (`x-veghe-secret` Vault `SEAP_VEGHE_SECRET(_VECHI)`) sau „🔄 Verifică SEAP acum" | `OL` `DocumenteNoiSection.verificaAcum` L2698-2708 (`invoke('ofertare-seap-veghe',{licitatie_id})`) | `EF/ofertare-seap-veghe` L230-624 | `ofertare_licitatii` (select `c_notice_id not null` & `termen_depunere ≥ ieri` L260-264, **update `termen_depunere`** L430-431), `ofertare_documente_atribuire` (insert răspunsuri L357-366, update `aparut_ulterior` L502-504, insert placeholder L512-517), `notifications` (`modul:'Comercial'`), `profiles`, Resend, apel intern L06 (×4) și L07 | opțional `licitatie_id` | (a) docuri noi în listă vs. `cheieNume` din BD → import L06/L07 → marcare `aparut_ulterior` → placeholder pentru neaduse; (b) răspunsuri din `NoticeDocument/GetAll` inserate cu `seap_cod`, `seap_meta{titlu,publicat,inlocuieste}`, `tip` moștenit de la docul înlocuit L359; (c) termen nou din `GetSection4View`; notificări + mail (doar pentru `esteRaspuns`/răspunsuri) | orice (filtrare doar pe `c_notice_id` și termen, **nu pe status** — și licitațiile `depusa/castigata` cu termen ≥ ieri sunt vegheate) | neschimbat; `termen_depunere` actualizat automat | NONE (cron); UI click | NONE | **versiune nouă = rând nou** cu același `nume_original` (sau `"<titlu> — <fisier>"` L336), path `<lic>/atribuire/raspunsuri/<cod>_<safe>` cu `upsert:true` L353; rândul vechi rămâne neatins (nici `eroare`, nici flag) | `seap_cod` (unic partial), `seap_meta.inlocuieste = nume_original vechi` (text, nu id); pentru (a) doar nume; **fără hash** | L12 (citire nouă), L14 (raspuns-set), om | `fetchSeap` retry ×4 la 403/429/5xx L97-108; erori per licitație în `raport`; `IMPORT_SECRET` lipsă → Vercel sărit; notificare/mail eșuate → `raport` |
| L11 | Alerte atenție (SQL) | cron `ofertare_alerte_atentie_0630` | — | `ofertare_alerte_atentie(14)` (SQL dump L457-536) | `ofertare_licitatii` (`status IN ('in_lucru','go','identificata')` — **`analiza` exclus**), `ofertare_documente_atribuire` (arhive `.rar/.7z` neîncărcate), `ofertare_alerte_amprenta`, `notifications` (`modul:'Ofertare'`, spre deosebire de `Comercial` în veghe/UI) | — | notificări „fără responsabil ≤14 zile", „arhive de urcat manual" | — | — | NONE | NONE | amprentă md5 per (licitatie, fel) anti-repetare | `licitatie_id` | om | idempotent prin amprentă |
| L12 | Citire AI document nou din SEAP | „🤖 Citește cu AI" | `OL` `DocumenteNoiSection.citeste` L2687-2693; `src/OfertareClarificari.jsx` `urcaRaspuns` L222-241 (upload manual + citire) | `EF/ofertare-document-nou-citeste` L50-125; auth: `x-radar-secret` sau JWT + RPC `fn_are_acces_ofertare()` (L65-66) | `ofertare_documente_atribuire` (`analiza.citire_noi`, `analiza_la`, `tip` doar dacă era `'alta'` L119-123), `ai_usage_log` | `document_id` | `{tip, rezumat, modificari[], intrebari_raspunse[], termen_nou, data_document, model, citit_la, citit_de, tokens}` | doc: orice (placeholder refuzat L77-78) | neschimbat | click | `claude-sonnet-5`, max 4000, PDF ca document base64; conținutul declarat „DATE" în prompt L40 | `citire_noi` **suprascris** la recitire (L113) — versiunea anterioară a analizei se pierde | `doc.id`, `citit_de` | om; `termen_nou` e doar afișat (L2739) — **nu** scrie `ofertare_licitatii.termen_depunere` | erori business → `{error}`; `tip` CHECK fără `'erata'` → update separat ca să nu piardă analiza L117-123 |
| L13 | Decizie GO / NO-GO | „🟢 GO" / „⛔ NO-GO" (tab Detalii, doar când `l.status==='analiza' && profile.is_owner` L2973) | `OL` `LicitatieDetailModal` L2973-2982 → `decide` L288-299 | `ofertare_licitatii.update({decizie_go, decizie_motivare, decizie_de, decizie_la, status})` L290-295 | `ofertare_licitatii` | decizie + motivare (obligatorie doar la NO-GO, UI L2979) | `status: decizie==='go' ? 'go' : 'abandonata'` | `analiza` | `go` / `abandonata` | owner — verificat **doar în UI** (`if (!profile?.is_owner)` L289; RLS permite oricui) | NONE | n/a; a doua decizie ar suprascrie (`update` fără condiție pe `decizie_go IS NULL`) | `decizie_de`, `decizie_la` | L14, etapa1-mail (`decizie_go` în `stare()`), dashboard | eroare → toast |
| L14 | go → in_lucru | „Marchează În lucru" | `OL` L2986-2988 → `schimbaStatus` L279 | `update({status})` | `ofertare_licitatii` | — | — | `go` | `in_lucru` | click (orice user cu acces) | NONE | n/a | — | veghe/etapa1/reminder (`['go','in_lucru','analiza']` L392 etapa1-mail) | — |
| L15 | Aplicare răspuns la clarificări în registru (după depunere) | „Aplică" în DocumenteSection | `OL` `aplicaSelectia` L1076-1094: `dupaDepunere = licitatie.status === 'depusa'` L1078 → `prompt()` motiv obligatoriu | `EF/ofertare-raspuns-set` `actiune:'aplica'` L178-197 → RPC `fn_ofertare_raspuns_set_aplica(p_dupa_depunere, p_motiv, p_idempotency_key)` (SQL dump L82-322) | `ofertare_raspuns_set`, `ofertare_cerinte` (versiune nouă `inlocuita_de`, `acoperire_snapshot`), `ofertare_acoperire` (`reverificare_ceruta`) | set + op_ids | cerințe versionate; `aplicat_dupa_depunere`, `aplicat_motiv`, `status_licitatie_la_aplicare` | `identificata|analiza|go|in_lucru|depusa` | neschimbat | bifare + (dacă `depusa`) motiv scris; **server**: `IF v_status='depusa' AND (NOT p_dupa_depunere OR motiv gol) → RAISE`; `castigata/pierduta/abandonata → RAISE` | NONE la aplicare (AI e doar în fazele inventar/compara) | cerințe: `versiune+1`, rând vechi `inlocuita_de`; amprentă `fn_ofertare_cerinte_amprente` sub lock | `sursa_document_id` = documentul de răspuns, `raspuns_set_id` | acoperire, gate depunere | idempotency_key → rezultat reluat; lock_timeout 3s; conflicte raportate în `rezultat.conflicte` |
| L16 | in_lucru → depusa | „Marchează Depusă" | `OL` L2986-2988 → `schimbaStatus` L279-285 (același handler ca L05/L14, fără confirm, fără dovadă) | `update({status:'depusa'})` → **`trg_gate_depunere` → `fn_gate_depunere()`** | `ofertare_licitatii`, `ofertare_cerinte`, `ofertare_acoperire`, `documente_firma` | — | status | `in_lucru` (UI); **orice** status (server) | `depusa` | click (orice user cu acces modul); server: `IF NEW.status='depusa' AND OLD.status IS DISTINCT FROM 'depusa' AND NOT COALESCE(NEW.derogare_depunere,false)` → numără `n_neconfirmate` (cerințe active `confirmata_de IS NULL`), `n_neacoperite` (cerințe active fără acoperire `acoperit/acoperit_partener`), `n_rosii` (acoperiri cu `documente_firma` neutilizabil/expirat < termen+0/90 zile); dacă vreuna >0 → `RAISE EXCEPTION 'BLOCAT LA DEPUNERE: …'` | NONE | n/a | **nimic**: nu cere `ofertare_pt_pachet`, nu cere `ofertare_verificari`, nu cere fișier depus, nu scrie `depus_la` | GbeEvidenta (`['castigata','depusa']` L217), raspuns-set (motiv obligatoriu), filtre | excepție trigger → `showToast('Eroare: BLOCAT LA DEPUNERE…')` L282; `derogare_depunere=true` setabil de oricine prin RLS (nu există UI pentru el — căutat `derogare_depunere` în `src/`: 0 rezultate) |
| L17 | depusa → castigata / pierduta | „Marchează Câștigată/Pierdută" | `OL` L2986-2988 → `schimbaStatus` | `update({status})` (trigger nu intervine: `NEW.status<>'depusa'`) | `ofertare_licitatii` | — | — | `depusa` | `castigata` / `pierduta` | click (orice user cu acces) | NONE | n/a | — | `ofertare_calibrari` (separat, `rezultat`), GbeEvidenta (`contract_id` L213), etapa1 `raport_zilnic` (exclude finale) | — |
| L18 | Raport zilnic + reminder ≤5 zile | cron `ofertare_raport_zilnic_0430` | — | `EF/ofertare-etapa1-mail` `raport_zilnic` L374-405 | `ofertare_licitatii` (`status NOT IN (castigata,pierduta,abandonata,ignorata)` — `ignorata` nu e în CHECK), `ofertare_raport_zilnic` (upsert `data,licitatie_id`), `ofertare_mailuri` (`tip:'reminder_depunere'` o singură dată) | — | rezumat text/JSON per licitație; mail reminder dacă `zile≤5 && status ∈ go,in_lucru,analiza` | — | — | NONE | NONE | upsert zilnic | `licitatie_id` | Claude rutină, echipă | erori mail în `rez.reminder_eroare` |
| L19 | Participări SEAP (câștigători) | cron `ofertare_participari_sapt` | `OL` `ReferinteFinanciare` L3518 (citire) | `EF/ofertare-participari-import` (doar `x-radar-secret`) | `ofertare_participari` (upsert manual pe `(entitate, nr_seap)` L368-375) | `{batch:true}` sau `{entity_id, entitate}` | rânduri cu `raw` JSON | — | — | NONE | NONE | update in place (fără istoric) | `nr_seap`, `ca_notice_id`, `entity_id_seap` | referințe/calibrări | erori per notice în `rap.erori` |
| L20 | Revizii acoperire (snapshot) | — | `OL` `AcoperireSection.incarcaRevizii/comparaRevizii` L2198-2212 (doar citire + RPC diff) | `fn_ofertare_revizie_noua(p_licitatie_id, p_motiv, p_document_id)` (SQL dump L597-637); `fn_ofertare_diff_revizii` L639-684 | `ofertare_acoperire_revizii`, `ofertare_acoperire_istoric` | — | snapshot al `ofertare_acoperire` pentru cerințe active; `document_id` opțional | — | — | — | NONE | `revizie = max+1` per licitație | `document_id` → `ofertare_documente_atribuire` (nume afișat L2200) | UI diff | **NOT IMPLEMENTED ca apel**: nicio funcție SQL din `public` și niciun fișier din repo nu apelează `fn_ofertare_revizie_noua` (căutat: `pg_get_functiondef` pe toate funcțiile `public`, `grep -r` în `src/ supabase/ api/`); există 2 rânduri în tabel (create manual/MCP) |

---

## (B) OBJECT LIFECYCLE

### `document` (`ofertare_documente_atribuire`)

| Aspect | AS-IS |
|---|---|
| where_created | (1) `OL` L266 — fișa E0 (`tip:'fisa_date'`, `fisier_path` `e0/...`, fără `sursa`, fără `size_bytes`); (2) `OL` `urca` L755 (`sursa` default `'upload'`); (3) `EF/ofertare-seap-import` L374 / `API/seap` L157 (`sursa:'seap'`); (4) `EF/ofertare-seap-veghe` L357 (răspunsuri, `sursa:'seap', aparut_ulterior:true, seap_cod, seap_meta`) și L512 (placeholder `/neincarcat/`, `status_procesare:'ignorat'`, `eroare:'Aparut nou in SEAP, dar nu a putut fi adus automat…'`); (5) `src/OfertareClarificari.jsx` L229 (răspuns urcat manual, `tip:'raspuns_clarificare', sursa:'upload', aparut_ulterior:true`); (6) `api/pdf-sparge.js` L84 (bucăți `— pN_pag…`); (7) `sursa:'drive'` există în date (4 rânduri) — scriitorul nu e în fișierele citite (tura de noapte/worker). |
| where_updated | placeholder → completat: `EF/seap-import` L371-373, `API/seap` L155-156, `OL` L754 (update pe id cu `randNou` — inclusiv `tip` recalculat); text/antet/revizie/status: `EF/ofertare-ingest-doc` L135, L157, L170, L188, L303; `analiza`: `EF/ofertare-document-nou-citeste` L114, L121 (`tip`), `api/plansa-felii.js` L110/126, `EF/ofertare-plansa-citeste` L359, `EF/ofertare-word-text` L154; `status_procesare/eroare`: `api/pdf-sparge.js` L47, L95, L101 (`tip:'plansa'`); `aparut_ulterior:true`: veghe L502-504 (pe `nume_original IN (...)`, deci **toate** rândurile cu acel nume). |
| how_versioned | **Nu există versiune a rândului.** Coloana `revizie text` = revizia *scrisă în antetul documentului* (extrasă de AI, L301 ingest-doc), nu o versiune a platformei. Republicarea SEAP (veghe L330-336) = rând **nou** cu `seap_cod` propriu și `seap_meta.inlocuieste=<nume vechi>`; rândul vechi nu e marcat. Reupload manual cu același nume + altă mărime = rând nou fără legătură (`OL` L737-741). Singurul marcaj de înlocuire găsit în date este manual: `eroare='INLOCUIT de doc 211 — …'` (1 rând). Bucățile (`pdf-sparge`) sunt rânduri noi; originalul primește `status_procesare:'ignorat'`, `eroare:'spart … în N bucăți'` — marcaj în text, „se pierde la re-import" (comentariu `OL` L864-867), de aceea UI deduce din realitate `areBucati` L874-879. |
| how_invalidated | (a) `trg_pt_invalideaza_la_revizie_document`: la `UPDATE OF revizie` cu valoare diferită → `ofertare_pt_legaturi.stare='atribuita', verificat_la_versiunea=NULL`, `constatare += 'INVALIDATA …'` pentru legăturile `fel='capitol'` `verificata|dovedita` care au `ofertare_pt_dovezi.document_id=NEW.id AND document_revizie IS DISTINCT FROM NEW.revizie`. (b) Nimic altceva: un document înlocuit prin veghe nu invalidează cerințele extrase din el (cerințele au `sursa_document_id`, dar nu există flux „document înlocuit → cerințe de reverificat"; reverificarea vine doar din L15 prin răspuns-set aplicat de om). (c) Ștergere: `curataOrfani` (seap-import L309-328) șterge obiecte din storage fără rând; nu există UI de ștergere a rândului (grep `.delete()` pe `ofertare_documente_atribuire` în `src/`: 0). |
| how_confirmed | `procesat_de` + `procesat_la` (cine a pornit citirea, ingest-doc); `citire_noi.citit_de` (document nou); nu există câmp „verificat de om" pe document. Tipul poate fi corectat de AI (`document-nou-citeste` L119-123) — nu de om în UI (grep `update({ tip` în `OL`: 0). |
| how_linked_to_source | SEAP: `seap_cod` (`noticeDocumentCode`, doar canalul răspunsuri, 23 rânduri) + `seap_meta{titlu,publicat,inlocuieste}`; restul: doar `nume_original` (comparat prin `cheieNume` în edge, exact în Vercel/UI) și `sursa:'seap'`. `noticeDocumentUrl` nu se salvează (token temporar). Radar: `ofertare_radar.documente[]` reține `url` temporar (radar-scan L190) — inutilizabil ulterior. **Fără hash de conținut** pe niciun rând (există `sha256` doar pe `ofertare_pt_pachet_fisiere`, 0 rânduri). |
| how_linked_to_final_package | `ofertare_pt_dovezi.document_id` + `document_revizie` (copiat din `d.revizie` la legare, `src/OfertarePropunere.jsx` L1711) → `ofertare_pt_legaturi` → propunere tehnică; pachetul final `ofertare_pt_pachet(_fisiere)` are `sha256`, `sursa_versiune`, `versiune` — dar **0 pachete în producție** și nicio legătură din `ofertare_licitatii.status` la pachet. |

### `licitatie` (`ofertare_licitatii`)

| Aspect | AS-IS |
|---|---|
| where_created | `OL` `promoveaza` L3370 (radar, `status:'identificata'` explicit) și `salveaza` L259-260 (manual, status = default coloană). |
| where_updated | `status`: `schimbaStatus` L280 (identificata→analiza, go→in_lucru, in_lucru→depusa, depusa→castigata/pierduta), `decide` L290 (analiza→go/abandonata). Alte câmpuri: `salveaza` L256 (edit formular, suprascrie `loturi` doar dacă `loturi_ai`), `responsabil_id` L3001, `regim_achizitie` L3007, `c_notice_id/sys_notice_type_id` L773 (`aduDinSeap`), `documentatie_adusa_la` (seap-import L548, Vercel L221), `termen_depunere` (veghe L430-431, automat din SEAP), `contract_id` (`src/GbeEvidenta.jsx` L213). Nu s-a găsit niciun scriitor pentru `derogare_depunere` în repo. |
| how_versioned | Nu. `updated_at` e setat de client (nu trigger). Nu există tabel de istoric al statusului. |
| how_invalidated | Ștergere `sterge` L301-308 (owner, UI-only; RLS permite oricui; cascade pe documente prin FK `ON DELETE CASCADE`). |
| how_confirmed | Decizie: `decizie_go/decizie_de/decizie_la/decizie_motivare` (L290-295). Depunere: doar `status='depusa'` + gate-ul de cerințe; nu există `depusa_de/depusa_la`, nu există legătură la pachet. |
| how_linked_to_source | `nr_anunt` (UNIQUE), `link_seap`, `c_notice_id`, `sys_notice_type_id`, `canal`; înapoi din radar: `ofertare_radar.licitatie_id`. |
| how_linked_to_final_package | `ofertare_pt_pachet.licitatie_id` (0 rânduri); `ofertare_verificari.licitatie_id` (verdict verde/galben/roșu, afișat în fișă L2863-2866 și în listă L4xx) — **fără efect pe status**. |

---

## (C) BREAK_POINTS

| # | Fel | Fișier / componentă | Tabel/view/RPC | Linie/regiune | Comportament curent (factual) |
|---|---|---|---|---|---|
| BP01 | Aceeași regulă în mai multe locuri (mapă tranziții UI vs. server) | `OL` `TRANZITII` L50-57 și `schimbaStatus` L279-285; DB `trg_gate_depunere` | `ofertare_licitatii` | L50-57, L279-285, `fn_gate_depunere` | UI: `identificata:['analiza'], analiza:[], go:['in_lucru'], in_lucru:['depusa'], depusa:['castigata','pierduta']`. Server: **nicio verificare a tranziției** — `fn_gate_depunere` verifică doar `NEW.status='depusa' AND OLD.status IS DISTINCT FROM 'depusa'`; RLS `USING (auth.uid() IS NOT NULL)`. Orice client autentificat poate face `update({status:'castigata'})` direct de la `identificata`. |
| BP02 | UI pretinde, serverul nu impune (owner-only GO/NO-GO) | `OL` `decide` | `ofertare_licitatii` | L289 `if (!profile?.is_owner) { showToast('Decizia GO/NO-GO e doar a ownerului.', 'err'); return }`; L2973 `l.status === 'analiza' && profile?.is_owner` | Verificarea de owner este exclusiv client-side. Policy `ofertare_licitatii_all` permite `decizie_go/decizie_de/status` oricărui utilizator autentificat. Nu există trigger care să valideze `decizie_de = auth.uid()` sau `is_owner`. |
| BP03 | `depusa` atinsă fără dovada pachetului depus | `OL` L2986-2988 → `schimbaStatus`; DB `fn_gate_depunere` | `ofertare_licitatii`, `ofertare_cerinte`, `ofertare_acoperire`, `documente_firma` | `fn_gate_depunere` (sursă completă în secțiunea „Fapte") | Gate-ul cere exclusiv: `n_neconfirmate=0`, `n_neacoperite=0`, `n_rosii=0` **pe cerințele existente**. Cu **0 cerințe** toate contoarele sunt 0 → trece. Nu cere `ofertare_verificari` (verdict), nu cere `ofertare_pt_pachet` (`stare`, `depus_la`, `sha256`), nu cere fișier/`nas_path`, nu cere `decizie_go='go'`, nu scrie cine/când a depus. Date live: 54 licitații `depusa|castigata|pierduta`, **53 fără nicio cerință**, 0 cu derogare; `ofertare_pt_pachet` gol. Exemplu: id 6 `SCN1174892` `depusa` cu 0 documente, 0 cerințe, `decizie_go NULL`. |
| BP04 | Derogare fără UI și fără poartă de rol | — | `ofertare_licitatii.derogare_depunere` | `fn_gate_depunere`: `AND NOT COALESCE(NEW.derogare_depunere, false)`; mesaj `'…setează derogare_depunere=true (doar cu decizia lui Razvan)'` | Câmpul e setabil de orice utilizator autentificat (RLS) în același UPDATE cu `status:'depusa'` → gate-ul sare. Grep `derogare_depunere` în `src/`: 0 rezultate (nu există buton, dar nici blocaj). „doar cu decizia lui Razvan" este text în mesajul de eroare, nu regulă în cod. |
| BP05 | `depusa` accesibilă din orice status (server) | `fn_gate_depunere` | `ofertare_licitatii` | condiția `OLD.status IS DISTINCT FROM 'depusa'` | Gate-ul nu verifică `OLD.status='in_lucru'`; `identificata→depusa` trece dacă nu există cerințe. |
| BP06 | Aceeași regulă în 3 copii divergente (`ghicesteTip`) | `OL` L614-626; `EF/ofertare-seap-import` L100-110; `API/seap` L36-46 | `ofertare_documente_atribuire.tip` | — | UI: `/clarificare|raspuns.*consolidat|erata/ → raspuns_clarificare` **înainte** de formular/contract; edge/Vercel: `/raspuns|clarificar/` **ultimul** și fără `erata`; UI `fisa_date` regex `/fisadate|fisa.de.date|instructiuni.?ofertanti/` vs edge `/fisa[_ -]?date|instructiuni_ofertanti/`; edge are `centralizator`, `topo`, `schema tehnologica`, UI are `palnse`. Același fișier primește `tip` diferit după calea de intrare. |
| BP07 | Dedup pe chei diferite (nume) în 3 locuri | `EF/seap-import` `cheieNume` L81-82 și veghe L134-135 (fără spații/`,()`/`.p7s`, lowercase); `API/seap` L150/L180 (`nume_original` **exact**); `OL` `urca` L733-741 (`nume|size` exact) | `ofertare_documente_atribuire` | citate: Vercel `const urcate = new Set(... .map((d) => d.nume_original))`; UI `existente.has(\`${rel}|${f.size}\`)` | Comentariul din edge (L74-79) documentează că numele SEAP normalizate produceau „placeholdere fantomă și duplicate"; Vercel nu a primit aceeași cheie. UI: același nume cu altă mărime → rând nou, fără marcaj de înlocuire. |
| BP08 | Versionare pierdută: `revizie` suprascrisă la reprocesare | `EF/ofertare-ingest-doc` | `ofertare_documente_atribuire.revizie`, `antet` | L301 `if (start === 0 && antet) { upd.antet = antet; upd.revizie = antet?.revizie \|\| row.revizie \|\| null; ... }` | La fiecare citire de la pagina 0, `antet` e înlocuit integral și `revizie` e rescrisă cu ce citește AI (sau păstrată dacă AI dă null). Nu există istoric al valorilor. Triggerul `trg_pt_invalideaza_la_revizie_document` reacționează la orice schimbare (inclusiv o citire AI diferită a aceluiași fișier). |
| BP09 | Versionare pierdută: analiza AI a documentului nou | `EF/ofertare-document-nou-citeste` | `ofertare_documente_atribuire.analiza.citire_noi` | L113 `const analiza = { ...(row.analiza ...), citire_noi: citire }`; L114 `update({ analiza, analiza_la })` | Recitirea suprascrie `citire_noi` (comentariu L17: „idempotentă (recitirea suprascrie citire_noi)"). Legarea la clarificări (`OfertareClarificari.jsx`) se face pe conținutul curent. |
| BP10 | Supersession fără legătură structurală | `EF/ofertare-seap-veghe` | `ofertare_documente_atribuire.seap_meta`, `eroare` | L335-336 `const inlocuit = numeCunoscute.get(cheieNume(fisier)); const nume = inlocuit && titlu && titlu !== fisier ? \`${titlu} — ${fisier}\` : fisier`; L364-365 `seap_meta: { ..., inlocuieste: inlocuit ? inlocuit.nume_original : null }` | Înlocuirea e notată doar pe rândul **nou**, ca text (`nume_original` vechi), nu ca `id`; rândul vechi rămâne `status_procesare='procesat'`, cu `text_extras` intact, eligibil pentru extragere cerințe/raspuns-set (`potIntra` `OL` L998 nu exclude înlocuitele). Singura marcare a rândului vechi găsită în date e manuală (`eroare='INLOCUIT de doc 211 …'`, id 202) — pattern nefolosit de niciun cod (grep `INLOCUIT` în `src/ supabase/ api/`: 0). |
| BP11 | Artefact fără identitate/hash | `EF/seap-import` L387-388, `API/seap` L198, `OL` L742, veghe L351-355 | bucket `ofertare`, `ofertare_documente_atribuire` | upload cu `contentType`, fără `sha256`; veghe `upsert: true` | Niciun rând de document de atribuire nu are hash; identitatea = `fisier_path` (cu `Date.now()` în nume, deci un fișier reurcat are altă cale) + `nume_original` + `size_bytes`. Veghe scrie cu `upsert:true` pe path `<lic>/atribuire/raspunsuri/<cod>_<safe>`: fără `cod`, două răspunsuri cu același nume **suprascriu același obiect** (comentariu L349 spune contrariul doar când există cod). Radar reține `noticeDocumentUrl` temporar (L190) — link expirat în BD. |
| BP12 | UI pretinde poartă pe cheltuială, serverul (parțial) nu | `src/OfertareTriere.jsx` `poatePorniProcesarea` L36-37; `OL` L1133-1141; `ofertare_ingest_tick()` | `ofertare_ingest_coada`, `ofertare_documente_atribuire` | UI: butoanele „Procesează"/„Pe server" ascunse dacă nu e owner/responsabil; `proceseazaPeServer` L697-702 face `upsert` direct în `ofertare_ingest_coada` | Coada e scrisă de client; `ofertare_ingest_tick` lansează `ofertare-ingest-doc` cu `SUPABASE_ANON_JWT` pentru orice rând `activ` — nu verifică `cerut_de` vs owner/responsabil. Poarta există doar în randare. (Edge `ofertare-ingest-doc` neinclus în fișierele cerute; verificarea lui de rol nu a fost citită.) |
| BP13 | Aceeași regulă, valori diferite: `modul` la notificări | veghe L575 `modul: 'Comercial'`; `OL` L922 `modul: 'Comercial'`; `ofertare_alerte_atentie` `'Ofertare'`; `ofertare_ingest_tick` `'Ofertare'`; App.jsx L955 `<NotificationBell excludeModul="Ofertare" />` | `notifications.modul` | citate în coloană | Comentariul din veghe L67-68: „pentru ofertare valoarea corectă e 'Comercial'. Cu 'ofertare' insertul pica silentios"; funcțiile SQL scriu `'Ofertare'`. Clopoțelul general exclude `'Ofertare'` (deci alertele SQL apar doar în clopoțelul modulului; cele din veghe/UI apar în ambele). |
| BP14 | Filtre de status divergente (ce e „activ") | `OL` L196 `FINALE=['castigata','pierduta','abandonata']`; L203 `in_lucru: ['go','in_lucru','analiza']`; etapa1-mail L375 `not.in (castigata,pierduta,abandonata,ignorata)`, L392 `['go','in_lucru','analiza']`; `ofertare_alerte_atentie` `status IN ('in_lucru','go','identificata')`; veghe: **fără filtru pe status** (L260-264, doar `c_notice_id` + termen); raspuns-set: `identificata|analiza|go|in_lucru` liber, `depusa` cu motiv, finale blocate | — | citate | `analiza` primește reminder dar nu alertă „fără responsabil"; `identificata` invers. `ignorata` nu există în CHECK. Veghea importă documente și mută `termen_depunere` și pe licitațiile `depusa/castigata/pierduta/abandonata` dacă termenul e ≥ ieri. |
| BP15 | Termen mutat automat, fără gate | `EF/ofertare-seap-veghe` | `ofertare_licitatii.termen_depunere` | L429-433 `if (iso && (!lic.termen_depunere \|\| Math.abs(... ) > 60000)) { await supa.from('ofertare_licitatii').update({ termen_depunere: iso }) ... }` | Un conținut extern (SEAP) rescrie termenul (folosit de `fn_gate_depunere` la `n_rosii` și de reminder) fără confirmare; notificarea e post-factum. `document-nou-citeste` extrage separat `termen_nou` (afișat `OL` L2739) și **nu** îl scrie — două surse de adevăr pentru termen. |
| BP16 | Decizie GO fără urmă de a doua decizie | `OL` `decide` | `ofertare_licitatii` | L290-295 `update({ decizie_go, decizie_motivare, decizie_de, decizie_la, status })` | Butoanele apar doar la `status==='analiza'`, dar handler-ul nu condiționează pe `decizie_go IS NULL`; un UPDATE direct rescrie decizia anterioară fără istoric. `status` `go` există live cu `decizie_go='go'` (id 15) dar și `depusa` cu `decizie_go NULL` (id 1, 6). |
| BP17 | Revizii acoperire: mecanism fără apelant | `fn_ofertare_revizie_noua`, `fn_ofertare_diff_revizii`; `OL` L2194-2212 | `ofertare_acoperire_revizii`, `ofertare_acoperire_istoric` | comentariu `OL` L2195-2197 „Fiecare rulare completă îngheață o revizie" | Niciun cod din repo sau din funcțiile `public` nu apelează `fn_ofertare_revizie_noua` (verificat `grep -r` + `pg_get_functiondef` pe toate funcțiile). Tabelul are 2 rânduri. UI-ul de comparare există, dar nu se produc revizii noi automat. NOT IMPLEMENTED (searched: repo `src/ supabase/ api/`, funcții SQL `public`). |
| BP18 | Auth pe edge-uri: JWT valid ≠ rol | `EF/ofertare-seap-import` L339-347, `EF/ofertare-seap-veghe` L245-253, `EF/ofertare-radar-scan` L92-98, `API/seap` L128-133 | — | citat seap-import: `const { data: u } = await anon.auth.getUser(jwt); if (!u?.user) return json({ error: 'unauthorized' }, 401);` | Orice utilizator autentificat al platformei (indiferent de modul) poate porni import SEAP / veghe / scan radar (apeluri SEAP + scrieri cu `service_role`). Doar `ofertare-document-nou-citeste` verifică `fn_are_acces_ofertare()` (L65-66). `fn_verifica_radar_secret` nu acceptă `_VECHI` (spre deosebire de `fn_verifica_secret`), contrar comentariilor din edge („accepta si valoarea precedenta"). |
| BP19 | `documentatie_adusa_la` cu semantici diferite | `EF/seap-import` L547-548 vs `API/seap` L220-222 | `ofertare_licitatii.documentatie_adusa_la` | edge: `if (!continua) update(...)` (chiar cu erori); Vercel: `if (!raport.erori.length) update(...)` | Câmpul e setat de edge și când `raport.erori` conține eșecuri; Vercel îl setează doar fără erori. Live: 4 din 8 licitații active au `documentatie_adusa_la NULL` deși au 19-61 documente (id 1, 6, 85, 15). |
| BP20 | Documentul E0 în afara prefixului licitației | `OL` `citesteFisa` L478-480 + `salveaza` L266-270 | bucket `ofertare`, `ofertare_documente_atribuire` | `path = \`e0/${Date.now()}_${rand}_${safe}\``; insert fără `sursa`, `size_bytes`, `status_procesare` (default `neprocesat`) | `curataOrfani` (seap-import) lucrează pe `<lic>/atribuire`; fișele E0 nu intră în acel prefix. Rândul e marcat `procesat_la` (deci considerat citit) deși nu are `text_extras` (L268-269 comentariu: „fișa a fost deja citită cu AI (E0 autofill)"); `ofertare_ingest_tick` îl va relansa (status `neprocesat`). |
