# Copiază în .env (chmod 600). Nimic de aici nu intră în repo.
# Token de abonament generat pe laptop cu `claude setup-token` (valabil 1 an); se pune prin SSH stdin, nu prin chat.
CLAUDE_CODE_OAUTH_TOKEN=
# Folderul licitației pe NAS (montat :ro în container ca /data)
LIC_FOLDER=/Volume1/Licitatii_Executate/Proiecte/58. Infiintare sistem de distrib gn in com MANASTIREA, JUD CALARASI
TASK_TIMEOUT_MIN=15
TASK_MAX_TURNS=20
TASK_MODEL=sonnet
TASK_EFFORT=medium
